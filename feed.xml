<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://alexdruso.github.io/feed.xml" rel="self" type="application/atom+xml" /><link href="https://alexdruso.github.io/" rel="alternate" type="text/html" /><updated>2026-06-19T20:43:03+00:00</updated><id>https://alexdruso.github.io/feed.xml</id><title type="html">Alessandro Sanvito</title><subtitle>Software Engineer at Optiver</subtitle><entry><title type="html">How Many Coin Tosses to Catch a Rigged Coin?</title><link href="https://alexdruso.github.io/how-many-tosses-rigged-coin/" rel="alternate" type="text/html" title="How Many Coin Tosses to Catch a Rigged Coin?" /><published>2026-06-19T00:00:00+00:00</published><updated>2026-06-19T00:00:00+00:00</updated><id>https://alexdruso.github.io/how-many-tosses-rigged-coin</id><content type="html" xml:base="https://alexdruso.github.io/how-many-tosses-rigged-coin/"><![CDATA[<style>
/* ============================================================
   Study Notes — shared design system
   Sleek/futuristic: cyan-teal accent, hairline borders, flat
   single-accent controls, monospace numerals, glow in dark only.
   Adaptive light + dark via tokens. Every selector is scoped
   under .study-note so nothing leaks to the rest of the site.
   ============================================================ */
.study-note {
  --sn-accent: #0891b2;
  --sn-accent-strong: #06b6d4;
  --sn-accent-soft: rgba(8, 145, 178, 0.10);
  --sn-accent-line: rgba(8, 145, 178, 0.30);
  --sn-on-accent: #ffffff;

  --sn-panel: #ffffff;
  --sn-panel-inset: #f6f8fa;
  --sn-section: #f7f9fb;
  --sn-border: rgba(15, 23, 42, 0.08);
  --sn-grid: rgba(15, 23, 42, 0.10);

  --sn-text: #1f2933;
  --sn-muted: #64748b;

  --sn-good: #0f9d8c;  --sn-good-bg: rgba(15, 157, 140, 0.10);
  --sn-warn: #b07d18;  --sn-warn-bg: rgba(176, 125, 24, 0.10);
  --sn-bad:  #b4544b;  --sn-bad-bg:  rgba(180, 84, 75, 0.10);

  --sn-radius: 12px;
  --sn-radius-sm: 8px;
  --sn-shadow: 0 1px 2px rgba(15, 23, 42, 0.04), 0 4px 16px rgba(15, 23, 42, 0.05);
  --sn-ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --sn-glow: none;
  --sn-mono: ui-monospace, "SF Mono", "JetBrains Mono", "Roboto Mono", Menlo, Consolas, monospace;
}
.dark-theme .study-note {
  --sn-accent: #22d3ee;
  --sn-accent-strong: #67e8f9;
  --sn-accent-soft: rgba(34, 211, 238, 0.12);
  --sn-accent-line: rgba(34, 211, 238, 0.35);
  --sn-on-accent: #06141a;

  --sn-panel: #11131c;
  --sn-panel-inset: #161a26;
  --sn-section: #14161f;
  --sn-border: rgba(148, 163, 184, 0.14);
  --sn-grid: rgba(148, 163, 184, 0.16);

  --sn-text: #e2e8f0;
  --sn-muted: #94a3b8;

  --sn-good: #34d399;  --sn-good-bg: rgba(52, 211, 153, 0.12);
  --sn-warn: #e2b04a;  --sn-warn-bg: rgba(226, 176, 74, 0.12);
  --sn-bad:  #f0857c;  --sn-bad-bg:  rgba(240, 133, 124, 0.12);

  --sn-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
  --sn-glow: 0 0 0 1px var(--sn-accent-line), 0 0 16px rgba(34, 211, 238, 0.18);
}

/* ---- sections / cards ---- */
.study-note .se-section,
.study-note .bm-section,
.study-note .mdp-section {
  margin: 2rem 0;
  padding: 1.5rem;
  border-radius: var(--sn-radius);
  background: var(--sn-section);
  border: 1px solid var(--sn-border);
  box-shadow: var(--sn-shadow);
}
.study-note .se-section h3,
.study-note .bm-section h3,
.study-note .mdp-section h3 {
  margin-top: 0;
  color: var(--sn-accent);
  letter-spacing: -0.01em;
}

/* ---- equation boxes ---- */
.study-note .se-equation,
.study-note .bm-equation,
.study-note .equation-box {
  padding: 1rem 1.5rem;
  margin: 1rem 0;
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  font-size: 1.1rem;
  text-align: center;
  overflow-x: auto;
}
.study-note .se-equation .big,
.study-note .bm-equation .big { font-size: 1.25rem; }

/* ---- callout / note ---- */
.study-note .se-note,
.study-note .bm-note {
  background: var(--sn-accent-soft);
  border-radius: var(--sn-radius-sm);
  border-left: 3px solid var(--sn-accent);
  padding: 0.9rem 1.1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- example box ---- */
.study-note .se-example,
.study-note .bm-example,
.study-note .example-box {
  background: var(--sn-panel-inset);
  border: 1px solid var(--sn-border);
  border-left: 3px solid var(--sn-warn);
  border-radius: var(--sn-radius-sm);
  padding: 1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- chart containers ---- */
.study-note .se-chartwrap,
.study-note .bm-chartwrap,
.study-note .graph-wrap {
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.5rem;
}
.study-note .graph-wrap { overflow-x: auto; }

/* ---- primary buttons ---- */
.study-note .se-btn,
.study-note .bm-btn,
.study-note .vi-btn {
  padding: 0.5rem 1.1rem;
  border: none;
  border-radius: var(--sn-radius-sm);
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-on-accent);
  background: var(--sn-accent);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.2s var(--sn-ease), background 0.2s var(--sn-ease);
}
.study-note .se-btn:hover:not(:disabled),
.study-note .bm-btn:hover:not(:disabled),
.study-note .vi-btn:hover:not(:disabled) {
  background: var(--sn-accent-strong);
  transform: translateY(-1px);
  box-shadow: var(--sn-glow);
}
.study-note .se-btn:disabled,
.study-note .bm-btn:disabled,
.study-note .vi-btn:disabled { opacity: 0.4; cursor: not-allowed; }
/* secondary variant */
.study-note .se-btn.sec,
.study-note .bm-btn.sec,
.study-note .vi-btn.sec {
  background: transparent;
  color: var(--sn-accent);
  box-shadow: inset 0 0 0 1px var(--sn-accent-line);
}
.study-note .se-btn.sec:hover:not(:disabled),
.study-note .bm-btn.sec:hover:not(:disabled),
.study-note .vi-btn.sec:hover:not(:disabled) {
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
}

/* ---- ghost / reset buttons ---- */
.study-note .se-reset,
.study-note .bm-reset,
.study-note .reset-link {
  background: none;
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.4rem 0.85rem;
  font-size: 0.85rem;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-muted);
  transition: background 0.15s var(--sn-ease), border-color 0.15s var(--sn-ease), color 0.15s var(--sn-ease);
}
.study-note .se-reset:hover,
.study-note .bm-reset:hover,
.study-note .reset-link:hover {
  background: var(--sn-accent-soft);
  border-color: var(--sn-accent-line);
  color: var(--sn-accent);
}

/* ---- range sliders: flat, single-accent ---- */
.study-note input[type="range"] {
  -webkit-appearance: none;
  appearance: none;
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
  outline: none;
  cursor: pointer;
}
.study-note input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.15s var(--sn-ease);
}
.study-note input[type="range"]::-webkit-slider-thumb:hover { transform: scale(1.15); }
.study-note input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
}
.study-note input[type="range"]::-moz-range-track {
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
}

/* ---- status pills ---- */
.study-note .se-tag,
.study-note .bm-tag {
  display: inline-block;
  padding: 0.22rem 0.65rem;
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 0.01em;
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
  transition: background 0.2s var(--sn-ease), color 0.2s var(--sn-ease);
}
.study-note .se-tag.good, .study-note .bm-tag.good { background: var(--sn-good-bg); color: var(--sn-good); }
.study-note .se-tag.warn, .study-note .bm-tag.warn { background: var(--sn-warn-bg); color: var(--sn-warn); }
.study-note .se-tag.hot,  .study-note .bm-tag.hot  { background: var(--sn-bad-bg);  color: var(--sn-bad); }

/* ---- numerals: monospace, tabular, accent (size kept per-note) ---- */
.study-note .se-stat .val,
.study-note .bm-stat .val,
.study-note .score-item .sval,
.study-note .compare-score {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  color: var(--sn-accent);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.02em;
}
.study-note .se-readout,
.study-note .bm-temp-readout {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  font-weight: 600;
  color: var(--sn-accent);
}

/* ---- svg text (scoped; replaces the old leaky global rule) ---- */
.study-note svg text { fill: var(--sn-muted); }
</style>


<div class="study-note">

<style>
/* Note-specific LAYOUT only — colors/components come from study-note-styles.html.
   Unique prefix `ct-` (coin toss) avoids collisions with se-/bm-/mdp- notes. */
.ct-flexwrap { display: flex; gap: 1.25rem; flex-wrap: wrap; align-items: flex-start; }
.ct-panel { flex: 1; min-width: 240px; }

.ct-controls { display: flex; gap: 0.6rem; align-items: center; flex-wrap: wrap; margin: 0.75rem 0; }

.ct-ctrlrow { display: grid; grid-template-columns: 124px 1fr 70px; gap: 0.6rem; align-items: center; margin: 0.55rem 0; }
.ct-ctrlrow .nm { font-size: 0.85rem; }
.ct-ctrlrow .pv { font-size: 0.85rem; text-align: right; font-family: var(--sn-mono); font-variant-numeric: tabular-nums; color: var(--sn-accent); font-weight: 600; }

.ct-readout { font-family: var(--sn-mono); font-variant-numeric: tabular-nums; font-weight: 600; color: var(--sn-accent); min-width: 3rem; display: inline-block; }

.ct-statrow { display: flex; gap: 2rem; justify-content: center; margin: 0.6rem 0; flex-wrap: wrap; }
.ct-stat { text-align: center; }
.ct-stat .lab { font-size: 0.68rem; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.55; }
.ct-stat .val { font-family: var(--sn-mono); font-variant-numeric: tabular-nums; color: var(--sn-accent); font-weight: 700; font-size: 1.7rem; line-height: 1.1; letter-spacing: -0.02em; }

.ct-legend { font-size: 0.78rem; opacity: 0.8; display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 0.4rem; justify-content: center; }
.ct-legend span::before { content: "■ "; }

/* coin face */
.ct-coin { width: 54px; height: 54px; border-radius: 50%; display: inline-flex; align-items: center; justify-content: center; font-size: 1.4rem; font-weight: 700; font-family: var(--sn-mono); border: 1px solid var(--sn-border); background: var(--sn-panel-inset); color: var(--sn-muted); transition: transform 0.18s var(--sn-ease), color 0.18s var(--sn-ease), background 0.18s var(--sn-ease); }
.ct-coin.flip { transform: rotateY(180deg) scale(1.08); }

/* progress + comparison bars */
.ct-bar { background: var(--sn-panel-inset); border: 1px solid var(--sn-border); border-radius: 8px; height: 22px; overflow: hidden; position: relative; }
.ct-bar > div { height: 100%; border-radius: 8px; transition: width 0.12s linear, background 0.2s ease; }
.ct-cmprow { display: grid; grid-template-columns: 110px 1fr 64px; gap: 0.6rem; align-items: center; margin: 0.5rem 0; }
.ct-cmprow .nm { font-size: 0.85rem; }
.ct-cmprow .pv { font-size: 0.9rem; text-align: right; font-family: var(--sn-mono); font-variant-numeric: tabular-nums; color: var(--sn-accent); font-weight: 700; }

.ct-bignum { font-family: var(--sn-mono); font-variant-numeric: tabular-nums; color: var(--sn-accent); font-weight: 800; font-size: 2.6rem; line-height: 1.05; letter-spacing: -0.03em; text-align: center; }
</style>

<p>
  A friend slaps a coin on the table and swears it's fair. You've watched it land heads a little too
  often and you're not so sure. Here's the deceptively simple question this note is about:
  <strong>how many times would you have to flip it before you could honestly say it's rigged?</strong>
</p>

<p>The answer is not a single number — it depends on two things — but it <em>is</em> a clean formula:</p>

<div class="ct-note se-note">
  <strong>It depends on how rigged the coin is and how sure you want to be.</strong> A wildly biased
  60/40 coin gives itself away in a couple hundred flips. A barely-rigged 51/49 coin can hide for
  <em>tens of thousands</em>. The cost of catching it explodes as the bias shrinks — and by the end
  you'll be able to read the exact number off one equation, then watch a simulation confirm it.
</div>

<!-- ============================================================ -->
<!-- 1. FLIP IT AND WATCH THE EVIDENCE                            -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🪙 Flip it and watch the evidence build</h3>
  <p style="margin-top:0">
    Set the coin's <em>true</em> bias with the slider (the universe knows it; you don't), then start
    flipping. The chart tracks the <strong>running fraction of heads</strong>, p&#770;, after every
    flip. The shaded funnel is the <strong>95% range a genuinely fair coin would stay inside</strong> —
    it narrows like 1/&radic;n. The moment your trace pokes out of the funnel and stays out, you've got
    visual evidence the coin isn't fair.
  </p>

  <div class="ct-flexwrap">
    <div class="ct-panel" style="flex:2">
      <div class="se-chartwrap">
        <svg id="ct1-svg" width="100%" height="260" viewBox="0 0 720 260" preserveAspectRatio="none" style="display:block"></svg>
      </div>
      <div class="ct-legend">
        <span style="color:var(--sn-accent)">running p&#770;</span>
        <span style="color:var(--sn-muted)">fair = 0.5</span>
        <span style="color:var(--sn-good)">true bias</span>
      </div>
    </div>
    <div class="ct-panel">
      <div class="ct-ctrlrow">
        <span class="nm">true p(heads)</span>
        <input type="range" id="ct1-p" min="2" max="98" value="55">
        <span class="pv" id="ct1-pv">0.55</span>
      </div>
      <div class="ct-controls">
        <button class="se-btn" id="ct1-f1">Flip ×1</button>
        <button class="se-btn sec" id="ct1-f100">Flip ×100</button>
        <button class="se-reset" id="ct1-reset">↺ Reset</button>
        <span class="ct-coin" id="ct1-coin">?</span>
      </div>
      <div class="ct-statrow">
        <div class="ct-stat"><div class="lab">flips n</div><div class="val" id="ct1-n">0</div></div>
        <div class="ct-stat"><div class="lab">heads</div><div class="val" id="ct1-h">0</div></div>
        <div class="ct-stat"><div class="lab">p&#770;</div><div class="val" id="ct1-phat">—</div></div>
      </div>
      <div style="text-align:center;margin-top:0.3rem"><span id="ct1-tag" class="se-tag">flip to begin</span></div>
    </div>
  </div>

  <div class="se-note" style="margin-bottom:0">
    <strong>Try this:</strong> set the bias to 0.55 and flip 100 at a time. Early on the trace lurches
    all over the place — small samples are <em>noisy</em> — and it often sits comfortably inside the
    fair funnel for a while. Push past a few hundred flips and it finally separates. Now set the bias to
    0.70 and watch it escape almost immediately. That gap in difficulty is the whole story.
  </div>
</div>

<!-- ============================================================ -->
<!-- 2. THE NULL HYPOTHESIS AND THE p-VALUE                       -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>⚖️ The skeptic's question: the null hypothesis &amp; the p-value</h3>
  <p style="margin-top:0">
    To <em>prove</em> rigging we start by assuming the opposite — that's how statistics stays honest.
    The <strong>null hypothesis</strong> is "the coin is fair," H<sub>0</sub>: p = 0.5. Under H<sub>0</sub>
    the number of heads in n flips follows a <strong>Binomial(n, 0.5)</strong> distribution, and for any
    decent n that's well approximated by a bell curve. We summarise how far our result lands from the
    fair center with the <strong>z-score</strong>:
  </p>
  <div class="se-equation">
    <span class="big">z = ( p&#770; &minus; 0.5 ) / &radic;( 0.25 / n )</span>
  </div>
  <p style="margin-top:0">
    The <strong>p-value</strong> is then the probability a perfectly fair coin would look
    <em>this extreme or more</em>, just by luck. Small p-value = "a fair coin almost never does this" =
    evidence of rigging. The convention is to raise the alarm when <strong>p &lt; 0.05</strong>. Drag the
    sample size and the observed fraction of heads and watch the tail area — the p-value — light up:
  </p>

  <div class="ct-flexwrap">
    <div class="ct-panel" style="flex:2">
      <div class="se-chartwrap">
        <svg id="ct2-svg" width="100%" height="250" viewBox="0 0 720 250" preserveAspectRatio="none" style="display:block"></svg>
      </div>
      <div class="ct-legend">
        <span style="color:var(--sn-accent)">fair-coin distribution</span>
        <span style="color:var(--sn-bad)">p-value (luck tails)</span>
      </div>
    </div>
    <div class="ct-panel">
      <div class="ct-ctrlrow">
        <span class="nm">flips n</span>
        <input type="range" id="ct2-n" min="10" max="1000" value="100">
        <span class="pv" id="ct2-nv">100</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">observed p&#770;</span>
        <input type="range" id="ct2-p" min="2" max="98" value="60">
        <span class="pv" id="ct2-pv">0.60</span>
      </div>
      <div class="ct-statrow">
        <div class="ct-stat"><div class="lab">heads</div><div class="val" id="ct2-k">60</div></div>
        <div class="ct-stat"><div class="lab">z-score</div><div class="val" id="ct2-z">—</div></div>
      </div>
      <div class="ct-statrow" style="margin-top:0">
        <div class="ct-stat"><div class="lab">p-value</div><div class="val" id="ct2-pval">—</div></div>
      </div>
      <div style="text-align:center;margin-top:0.3rem"><span id="ct2-tag" class="se-tag">—</span></div>
    </div>
  </div>

  <div class="ct-flexwrap" style="margin-top:0.5rem">
    <div class="se-example" style="flex:1;margin:0">
      <strong>60 heads in 100:</strong> looks lopsided, but a fair coin manages it about 5% of the time —
      right on the borderline. Not yet damning.
    </div>
    <div class="se-example" style="flex:1;margin:0">
      <strong>600 heads in 1000:</strong> the <em>same</em> 60% fraction, but now p is astronomically
      small. Same bias, ten times the flips, overwhelming proof. <strong>Evidence is about n, not just
      the ratio.</strong>
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 3. TWO WAYS TO BE WRONG: ALPHA AND POWER                     -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🎯 Two ways to be wrong: false alarms (&alpha;) and missed detections (power)</h3>
  <p style="margin-top:0">
    A test can fail in two directions. A <strong>false alarm</strong> (Type I error) is calling a fair
    coin rigged — its rate is the threshold <strong>&alpha;</strong> we picked above (e.g. 0.05). A
    <strong>missed detection</strong> (Type II error, rate &beta;) is letting a truly rigged coin walk
    free. What we really care about is <strong>power = 1 &minus; &beta;</strong>: the chance we
    <em>catch</em> a rigged coin when it really is rigged.
  </p>
  <p style="margin-top:0">
    Below are two bell curves over p&#770;: the <strong>null</strong> (fair, centered at 0.5) and the
    <strong>alternative</strong> (the coin's true bias). We reject "fair" whenever p&#770; lands past the
    critical lines. The <span style="color:var(--sn-bad)">red</span> tails under the null are &alpha;;
    the <span style="color:var(--sn-good)">green</span> area under the alternative past the line is the
    power. Crank up n or the bias and watch the curves pull apart — that separation <em>is</em> power.
  </p>

  <div class="ct-flexwrap">
    <div class="ct-panel" style="flex:2">
      <div class="se-chartwrap">
        <svg id="ct3-svg" width="100%" height="260" viewBox="0 0 720 260" preserveAspectRatio="none" style="display:block"></svg>
      </div>
      <div class="ct-legend">
        <span style="color:var(--sn-accent)">fair (null)</span>
        <span style="color:var(--sn-good)">true bias (alt) &amp; power</span>
        <span style="color:var(--sn-bad)">&alpha; (false alarm)</span>
      </div>
    </div>
    <div class="ct-panel">
      <div class="ct-ctrlrow">
        <span class="nm">true p(heads)</span>
        <input type="range" id="ct3-p" min="51" max="85" value="55">
        <span class="pv" id="ct3-pv">0.55</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">flips n</span>
        <input type="range" id="ct3-n" min="10" max="2000" value="200">
        <span class="pv" id="ct3-nv">200</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">&alpha; (×1000)</span>
        <input type="range" id="ct3-a" min="1" max="200" value="50">
        <span class="pv" id="ct3-av">0.050</span>
      </div>
      <div class="ct-statrow">
        <div class="ct-stat"><div class="lab">power</div><div class="val" id="ct3-pow">—</div></div>
        <div class="ct-stat"><div class="lab">&alpha;</div><div class="val" id="ct3-a2" style="color:var(--sn-muted)">—</div></div>
      </div>
      <div style="text-align:center;margin-top:0.3rem"><span id="ct3-tag" class="se-tag">—</span></div>
    </div>
  </div>

  <div class="se-note" style="margin-bottom:0">
    <strong>The tug-of-war:</strong> tightening &alpha; (fewer false alarms) pushes the critical lines
    outward and <em>lowers</em> power. The only way to win on both fronts at once is to collect more
    data. That trade-off is exactly what the sample-size formula below resolves.
  </div>
</div>

<!-- ============================================================ -->
<!-- 4. THE PURE-MATH ANSWER: THE SAMPLE-SIZE FORMULA            -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🧮 The pure-math answer: a formula for n</h3>
  <p style="margin-top:0">
    Now we just turn the picture into algebra. We want the critical line to sit far enough from the fair
    center to keep false alarms at &alpha;, <em>and</em> far enough below the true bias to catch it with
    the power we want. Writing z<sub>&alpha;/2</sub> and z<sub>&beta;</sub> for the standard-normal
    cutoffs, "push the two curves apart until both conditions hold" rearranges to:
  </p>
  <div class="se-equation">
    <span class="big">n &asymp; ( z<sub>&alpha;/2</sub> &middot; &radic;<span style="text-decoration:overline">&frac14;</span> &nbsp;+&nbsp; z<sub>&beta;</sub> &middot; &radic;<span style="text-decoration:overline">p(1&minus;p)</span> )<sup>2</sup> &nbsp;/&nbsp; ( p &minus; 0.5 )<sup>2</sup></span>
  </div>
  <p style="margin-top:0">
    Everything in that fraction is mild except the denominator. The <strong>effect size</strong>
    (p &minus; 0.5) is <em>squared</em> and sits on the bottom, so halving the bias you want to detect
    roughly <strong>quadruples</strong> the flips you need. Set your target bias, false-alarm rate, and
    power, and read off the budget:
  </p>

  <div class="ct-flexwrap">
    <div class="ct-panel">
      <div class="ct-ctrlrow">
        <span class="nm">detect p(heads)</span>
        <input type="range" id="ct4-p" min="51" max="80" value="55">
        <span class="pv" id="ct4-pv">0.55</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">&alpha; (×1000)</span>
        <input type="range" id="ct4-a" min="1" max="200" value="50">
        <span class="pv" id="ct4-av">0.050</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">power (%)</span>
        <input type="range" id="ct4-w" min="50" max="99" value="80">
        <span class="pv" id="ct4-wv">80%</span>
      </div>
      <div style="margin-top:1rem">
        <div style="font-size:0.68rem;text-transform:uppercase;letter-spacing:0.05em;opacity:0.55;text-align:center">flips required</div>
        <div class="ct-bignum" id="ct4-n">—</div>
      </div>
    </div>
    <div class="ct-panel" style="flex:1.4">
      <div class="se-chartwrap">
        <svg id="ct4-svg" width="100%" height="240" viewBox="0 0 460 240"></svg>
      </div>
      <div class="ct-legend"><span style="color:var(--sn-accent)">required n vs bias (log scale)</span></div>
    </div>
  </div>

  <div class="se-example" style="margin-bottom:0">
    <strong>Worked examples (&alpha; = 0.05, power = 80%):</strong> a <strong>60/40</strong> coin
    (p = 0.60) needs only about <strong>194</strong> flips. A <strong>55/45</strong> coin needs roughly
    <strong>783</strong>. A <strong>51/49</strong> coin? About <strong>19,600</strong> flips — the
    squared effect size in the denominator is merciless.
  </div>
</div>

<!-- ============================================================ -->
<!-- 5. SIMULATION MEETS MATH                                     -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🔬 Simulation meets math: does the formula actually hold?</h3>
  <p style="margin-top:0">
    A formula is only as good as its predictions. So let's <strong>brute-force it</strong>. Pick a true
    bias, a number of flips, and &alpha;. We run a thousand independent experiments — each one flips the
    coin n times and applies the p &lt; &alpha; test — and tally how often we correctly catch the rigged
    coin. That <strong>empirical power</strong> should land right on the curve's <strong>predicted
    power</strong>. Hit run and watch the two bars meet.
  </p>

  <div class="ct-flexwrap">
    <div class="ct-panel">
      <div class="ct-ctrlrow">
        <span class="nm">true p(heads)</span>
        <input type="range" id="ct5-p" min="51" max="85" value="55">
        <span class="pv" id="ct5-pv">0.55</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">flips n</span>
        <input type="range" id="ct5-n" min="10" max="2000" value="783">
        <span class="pv" id="ct5-nv">783</span>
      </div>
      <div class="ct-ctrlrow">
        <span class="nm">&alpha; (×1000)</span>
        <input type="range" id="ct5-a" min="1" max="200" value="50">
        <span class="pv" id="ct5-av">0.050</span>
      </div>
      <div class="ct-controls" style="justify-content:center;margin-top:0.8rem">
        <button class="se-btn" id="ct5-run">▶ Run 1000 experiments</button>
        <button class="se-reset" id="ct5-reset">↺ Reset</button>
      </div>
    </div>
    <div class="ct-panel" style="flex:1.3">
      <div style="font-size:0.78rem;opacity:0.7;margin-bottom:0.3rem">progress</div>
      <div class="ct-bar" style="margin-bottom:1rem"><div id="ct5-prog" style="width:0%;background:var(--sn-accent)"></div></div>

      <div class="ct-cmprow">
        <span class="nm">predicted</span>
        <div class="ct-bar"><div id="ct5-tbar" style="width:0%;background:var(--sn-muted)"></div></div>
        <span class="pv" id="ct5-tval" style="color:var(--sn-muted)">—</span>
      </div>
      <div class="ct-cmprow">
        <span class="nm">simulated</span>
        <div class="ct-bar"><div id="ct5-ebar" style="width:0%;background:var(--sn-good)"></div></div>
        <span class="pv" id="ct5-eval" style="color:var(--sn-good)">—</span>
      </div>

      <div class="ct-statrow" style="margin-top:0.8rem">
        <div class="ct-stat"><div class="lab">experiments</div><div class="val" id="ct5-done" style="font-size:1.3rem">0</div></div>
        <div class="ct-stat"><div class="lab">caught</div><div class="val" id="ct5-caught" style="font-size:1.3rem;color:var(--sn-good)">0</div></div>
      </div>
      <div style="text-align:center;margin-top:0.3rem"><span id="ct5-tag" class="se-tag">ready</span></div>
    </div>
  </div>

  <div class="se-note" style="margin-bottom:0">
    <strong>Try the boundary case:</strong> with p = 0.55, n = 783, &alpha; = 0.05 the formula was tuned
    for 80% power — so about <em>800 of the 1000</em> experiments should catch the coin, and one in five
    should miss it even though it really is rigged. Now slide n down to 200 and watch the simulated power
    collapse: too few flips, and the rigged coin usually escapes.
  </div>
</div>

<!-- ============================================================ -->
<!-- WRAP UP                                                      -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🎁 The whole story in one breath</h3>
  <ul style="line-height:1.7">
    <li><strong>Assume innocence.</strong> Start from H<sub>0</sub>: "the coin is fair," and ask how
      surprising your data would be if that were true — that's the <strong>p-value</strong>.</li>
    <li><strong>Two errors, one tension.</strong> &alpha; controls false alarms; <strong>power</strong>
      (1 &minus; &beta;) controls catches. You can't shrink both without more flips.</li>
    <li><strong>One formula.</strong> n &asymp; (z<sub>&alpha;/2</sub>&radic;&frac14; +
      z<sub>&beta;</sub>&radic;p(1&minus;p))<sup>2</sup> / (p &minus; 0.5)<sup>2</sup> turns "how sure"
      and "how rigged" into an exact flip budget.</li>
    <li><strong>Cost scales like 1/(p &minus; &frac12;)<sup>2</sup>.</strong> Obvious cheats are caught
      in a few hundred flips; subtle ones can demand tens of thousands. Detecting tiny bias is
      genuinely, quadratically expensive — and simulation confirms the math to the percent.</li>
  </ul>
  <p style="margin-bottom:0">
    So next time someone insists their coin is fair, you don't have to argue — you can tell them
    <em>exactly</em> how many flips it'll take to find out, before you throw the first one.
  </p>
</div>

<script>
(function () {
  "use strict";
  // theme-aware colors: read CSS tokens, re-read + redraw on light/dark toggle
  const SN = document.querySelector(".study-note");
  const readCols = () => {
    const cs = getComputedStyle(SN);
    const c = n => cs.getPropertyValue(n).trim();
    return { accent: c("--sn-accent"), good: c("--sn-good"), bad: c("--sn-bad"),
             warn: c("--sn-warn"), muted: c("--sn-muted"), grid: c("--sn-grid") };
  };
  let COL = readCols();
  const redrawFns = [];
  const onTheme = fn => { redrawFns.push(fn); return fn; };
  new MutationObserver(() => { COL = readCols(); redrawFns.forEach(f => f()); })
    .observe(document.body, { attributes: true, attributeFilter: ["class"] });

  // ---- math helpers (no dependencies) -------------------------------------
  const clamp = (x, a, b) => Math.max(a, Math.min(b, x));
  // standard normal CDF via Abramowitz & Stegun 7.1.26 erf approximation
  function erf(x) {
    const s = x < 0 ? -1 : 1; x = Math.abs(x);
    const t = 1 / (1 + 0.3275911 * x);
    const y = 1 - (((((1.061405429 * t - 1.453152027) * t) + 1.421413741) * t - 0.284496736) * t + 0.254829592) * t * Math.exp(-x * x);
    return s * y;
  }
  const Phi = z => 0.5 * (1 + erf(z / Math.SQRT2));
  // inverse normal CDF — Peter Acklam's rational approximation
  function zQuant(p) {
    p = clamp(p, 1e-9, 1 - 1e-9);
    const a = [-3.969683028665376e+01, 2.209460984245205e+02, -2.759285104469687e+02, 1.383577518672690e+02, -3.066479806614716e+01, 2.506628277459239e+00];
    const b = [-5.447609879822406e+01, 1.615858368580409e+02, -1.556989798598866e+02, 6.680131188771972e+01, -1.328068155288572e+01];
    const c = [-7.784894002430293e-03, -3.223964580411365e-01, -2.400758277161838e+00, -2.549732539343734e+00, 4.374664141464968e+00, 2.938163982698783e+00];
    const d = [7.784695709041462e-03, 3.224671290700398e-01, 2.445134137142996e+00, 3.754408661907416e+00];
    const pl = 0.02425, ph = 1 - pl; let q, r;
    if (p < pl) { q = Math.sqrt(-2 * Math.log(p)); return (((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5]) / ((((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1); }
    if (p <= ph) { q = p - 0.5; r = q * q; return (((((a[0] * r + a[1]) * r + a[2]) * r + a[3]) * r + a[4]) * r + a[5]) * q / (((((b[0] * r + b[1]) * r + b[2]) * r + b[3]) * r + b[4]) * r + 1); }
    q = Math.sqrt(-2 * Math.log(1 - p)); return -(((((c[0] * q + c[1]) * q + c[2]) * q + c[3]) * q + c[4]) * q + c[5]) / ((((d[0] * q + d[1]) * q + d[2]) * q + d[3]) * q + 1);
  }
  // required sample size for a two-sided test of p=0.5 vs p (power = 1-beta)
  function requiredN(p, alpha, power) {
    const za = zQuant(1 - alpha / 2), zb = zQuant(power);
    const num = za * Math.sqrt(0.25) + zb * Math.sqrt(p * (1 - p));
    return Math.ceil((num * num) / ((p - 0.5) * (p - 0.5)));
  }
  // power of the two-sided test at true bias p, n flips, level alpha
  function powerAt(p, n, alpha) {
    const za = zQuant(1 - alpha / 2);
    const s0 = Math.sqrt(0.25 / n), s1 = Math.sqrt(p * (1 - p) / n);
    const cHi = 0.5 + za * s0, cLo = 0.5 - za * s0;
    return Phi((cLo - p) / s1) + (1 - Phi((cHi - p) / s1));
  }

  // ============================================================
  // DEMO 1 — live flip simulator with shrinking fair funnel
  // ============================================================
  (function () {
    const svg = document.getElementById("ct1-svg");
    const pSlider = document.getElementById("ct1-p");
    const pv = document.getElementById("ct1-pv");
    const coin = document.getElementById("ct1-coin");
    const nEl = document.getElementById("ct1-n"), hEl = document.getElementById("ct1-h"), phatEl = document.getElementById("ct1-phat");
    const tag = document.getElementById("ct1-tag");

    let heads = 0, n = 0;
    const props = [];        // running p-hat after each flip
    const trueP = () => pSlider.value / 100;

    const W = 720, H = 260, padL = 40, padR = 14, padT = 14, padB = 26;
    const Y = pr => padT + (1 - pr) * (H - padT - padB);
    const X = (k, nMax) => padL + (Math.log(k) / Math.log(Math.max(nMax, 2))) * (W - padL - padR);
    const band = k => 1.95996 * Math.sqrt(0.25 / k);   // 95% half-width for a fair coin

    function flip(m) {
      const p = trueP();
      let last = 0;
      for (let i = 0; i < m; i++) { last = Math.random() < p ? 1 : 0; heads += last; n++; props.push(heads / n); }
      coin.textContent = last ? "H" : "T";
      coin.style.color = last ? COL.accent : COL.muted;
      coin.classList.remove("flip"); void coin.offsetWidth; coin.classList.add("flip");
      draw();
    }
    function reset() { heads = 0; n = 0; props.length = 0; coin.textContent = "?"; coin.style.color = ""; draw(); }

    function draw() {
      pv.textContent = trueP().toFixed(2);
      nEl.textContent = n; hEl.textContent = heads;
      phatEl.textContent = n ? (heads / n).toFixed(3) : "—";

      const nMax = Math.max(n, 60);
      // gridlines + axis labels (proportion)
      let grid = "";
      [0, 0.25, 0.5, 0.75, 1].forEach(pr => {
        grid += `<line x1="${padL}" y1="${Y(pr)}" x2="${W - padR}" y2="${Y(pr)}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${padL - 6}" y="${Y(pr) + 3}" font-size="10" text-anchor="end">${pr.toFixed(2)}</text>`;
      });
      // fair funnel (95% band around 0.5), filled translucent accent
      let up = "", lo = "";
      const K = 160;
      for (let i = 0; i <= K; i++) {
        const k = Math.exp(Math.log(nMax) * i / K);   // 1 .. nMax on log scale
        const x = X(Math.max(k, 1), nMax);
        up += (i === 0 ? "M" : "L") + x.toFixed(1) + " " + Y(clamp(0.5 + band(k), 0, 1)).toFixed(1) + " ";
        lo = "L" + x.toFixed(1) + " " + Y(clamp(0.5 - band(k), 0, 1)).toFixed(1) + " " + lo;
      }
      const funnel = `<path d="${up}${lo}Z" fill="${COL.accent}" fill-opacity="0.12" stroke="none"/>`;
      const fair = `<line x1="${padL}" y1="${Y(0.5)}" x2="${W - padR}" y2="${Y(0.5)}" stroke="${COL.muted}" stroke-width="1.5" stroke-dasharray="5 4"/>`;
      const trueLine = `<line x1="${padL}" y1="${Y(trueP())}" x2="${W - padR}" y2="${Y(trueP())}" stroke="${COL.good}" stroke-width="1.5" stroke-dasharray="2 3"/>`;

      // running p-hat trace (subsample to keep the path light)
      let d = "";
      if (props.length) {
        const step = Math.max(1, Math.floor(props.length / 600));
        let first = true;
        for (let k = 1; k <= props.length; k += step) {
          const x = X(k, nMax), y = Y(props[k - 1]);
          d += (first ? "M" : "L") + x.toFixed(1) + " " + y.toFixed(1) + " "; first = false;
        }
        const lx = X(props.length, nMax), ly = Y(props[props.length - 1]);
        d += "L" + lx.toFixed(1) + " " + ly.toFixed(1) + " ";
      }
      const trace = d ? `<path d="${d}" fill="none" stroke="${COL.accent}" stroke-width="2"/>` : "";
      const dot = props.length ? `<circle cx="${X(props.length, nMax).toFixed(1)}" cy="${Y(props[props.length - 1]).toFixed(1)}" r="3.5" fill="${COL.accent}"/>` : "";

      svg.innerHTML = grid + funnel + fair + trueLine + trace + dot +
        `<text x="${W - padR}" y="${Y(0.5) - 5}" font-size="10" text-anchor="end" fill="${COL.muted}">fair 0.5</text>` +
        `<text x="${padL}" y="${H - 6}" font-size="9" opacity="0.6">flips (log scale) →</text>`;

      if (!n) { tag.textContent = "flip to begin"; tag.className = "se-tag"; return; }
      const out = Math.abs(heads / n - 0.5) > band(n);
      if (out) { tag.textContent = "outside the fair band → looks rigged"; tag.className = "se-tag hot"; }
      else { tag.textContent = "still consistent with fair"; tag.className = "se-tag good"; }
    }

    pSlider.addEventListener("input", draw);
    document.getElementById("ct1-f1").addEventListener("click", () => flip(1));
    document.getElementById("ct1-f100").addEventListener("click", () => flip(100));
    document.getElementById("ct1-reset").addEventListener("click", reset);
    onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 2 — null distribution and two-sided p-value
  // ============================================================
  (function () {
    const svg = document.getElementById("ct2-svg");
    const nS = document.getElementById("ct2-n"), pS = document.getElementById("ct2-p");
    const nv = document.getElementById("ct2-nv"), pvv = document.getElementById("ct2-pv");
    const kEl = document.getElementById("ct2-k"), zEl = document.getElementById("ct2-z"), pvalEl = document.getElementById("ct2-pval");
    const tag = document.getElementById("ct2-tag");

    const W = 720, H = 250, padL = 16, padR = 16, padT = 16, padB = 30;

    function draw() {
      const n = +nS.value, phat = pS.value / 100, k = Math.round(phat * n);
      nv.textContent = n; pvv.textContent = phat.toFixed(2); kEl.textContent = k;

      const mu = n / 2, sigma = Math.sqrt(n) / 2;
      const z = (k - mu) / sigma;
      const zc = Math.max(0, (Math.abs(k - mu) - 0.5) / sigma);   // continuity correction
      const pval = clamp(2 * (1 - Phi(zc)), 0, 1);
      zEl.textContent = z.toFixed(2);
      pvalEl.textContent = pval < 0.001 ? pval.toExponential(1) : pval.toFixed(3);

      // count-axis window around the mean
      const loC = Math.max(0, mu - 4.2 * sigma), hiC = Math.min(n, mu + 4.2 * sigma);
      const X = c => padL + (c - loC) / (hiC - loC) * (W - padL - padR);
      const dens = c => Math.exp(-(c - mu) * (c - mu) / (2 * sigma * sigma));
      const yTop = H - padB, h0 = H - padT - padB;
      const Y = d => yTop - d * h0;

      const dev = Math.abs(k - mu), loCut = mu - dev, hiCut = mu + dev;
      const curve = (a, bb) => { let s = ""; const steps = 200; for (let i = 0; i <= steps; i++) { const c = a + (bb - a) * i / steps; s += (i === 0 ? "M" : "L") + X(c).toFixed(1) + " " + Y(dens(c)).toFixed(1) + " "; } return s; };

      // shaded p-value tails (luck regions)
      let shade = "";
      if (dev > 0) {
        shade += `<path d="M${X(loC).toFixed(1)} ${yTop} ${curve(loC, loCut)} L${X(loCut).toFixed(1)} ${yTop} Z" fill="${COL.bad}" fill-opacity="0.32"/>`;
        shade += `<path d="M${X(hiCut).toFixed(1)} ${yTop} ${curve(hiCut, hiC)} L${X(hiC).toFixed(1)} ${yTop} Z" fill="${COL.bad}" fill-opacity="0.32"/>`;
      }
      const body = `<path d="M${X(loC).toFixed(1)} ${yTop} ${curve(loC, hiC)} L${X(hiC).toFixed(1)} ${yTop} Z" fill="${COL.accent}" fill-opacity="0.10"/>` +
        `<path d="${curve(loC, hiC)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      const center = `<line x1="${X(mu)}" y1="${padT}" x2="${X(mu)}" y2="${yTop}" stroke="${COL.muted}" stroke-width="1" stroke-dasharray="4 4"/>`;
      const marker = `<line x1="${X(k)}" y1="${Y(dens(k)) - 4}" x2="${X(k)}" y2="${yTop}" stroke="${COL.bad}" stroke-width="2"/>` +
        `<circle cx="${X(k)}" cy="${Y(dens(k))}" r="5" fill="${COL.bad}"/>` +
        `<text x="${clamp(X(k), padL + 30, W - padR - 30)}" y="${padT + 12}" font-size="11" text-anchor="middle" fill="${COL.bad}" font-weight="700">observed: ${k}</text>`;
      // axis ticks (as proportions for intuition)
      let ax = "";
      [0.25, 0.5, 0.75].forEach(pr => { const c = pr * n; if (c >= loC && c <= hiC) ax += `<text x="${X(c)}" y="${H - 8}" font-size="10" text-anchor="middle">${pr}</text>`; });

      svg.innerHTML = shade + body + center + marker + ax +
        `<text x="${X(mu)}" y="${yTop + 14}" font-size="10" text-anchor="middle" fill="${COL.muted}">heads as a fraction of n</text>`;

      if (pval < 0.05) { tag.textContent = "p < 0.05 → reject fair (rigged)"; tag.className = "se-tag hot"; }
      else if (pval < 0.1) { tag.textContent = "borderline — suggestive, not proof"; tag.className = "se-tag warn"; }
      else { tag.textContent = "consistent with a fair coin"; tag.className = "se-tag good"; }
    }
    nS.addEventListener("input", draw); pS.addEventListener("input", draw);
    onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 3 — alpha vs power: two overlapping distributions
  // ============================================================
  (function () {
    const svg = document.getElementById("ct3-svg");
    const pS = document.getElementById("ct3-p"), nS = document.getElementById("ct3-n"), aS = document.getElementById("ct3-a");
    const pvv = document.getElementById("ct3-pv"), nvv = document.getElementById("ct3-nv"), avv = document.getElementById("ct3-av");
    const powEl = document.getElementById("ct3-pow"), a2El = document.getElementById("ct3-a2"), tag = document.getElementById("ct3-tag");

    const W = 720, H = 260, padL = 16, padR = 16, padT = 18, padB = 30;

    function draw() {
      const p = pS.value / 100, n = +nS.value, alpha = aS.value / 1000;
      pvv.textContent = p.toFixed(2); nvv.textContent = n; avv.textContent = alpha.toFixed(3);

      const s0 = Math.sqrt(0.25 / n), s1 = Math.sqrt(p * (1 - p) / n);
      const za = zQuant(1 - alpha / 2);
      const cHi = 0.5 + za * s0, cLo = 0.5 - za * s0;
      const power = powerAt(p, n, alpha);
      powEl.textContent = (power * 100).toFixed(1) + "%";
      a2El.textContent = (alpha * 100).toFixed(1) + "%";

      // shared p-hat window
      const lo = Math.min(0.5 - 4.2 * s0, p - 4.2 * s1);
      const hi = Math.max(0.5 + 4.2 * s0, p + 4.2 * s1);
      const X = v => padL + (v - lo) / (hi - lo) * (W - padL - padR);
      const yTop = H - padB, h0 = H - padT - padB;
      // densities normalised so the TALLER (null, smaller sigma) peaks near the top
      const peak0 = 1, peak1 = s0 / s1;   // relative heights (1/sigma)
      const g = (v, mu, s, pk) => pk * Math.exp(-(v - mu) * (v - mu) / (2 * s * s));
      const Y = d => yTop - d * h0 / Math.max(peak0, peak1);

      const curve = (mu, s, pk) => { let str = ""; const steps = 220; for (let i = 0; i <= steps; i++) { const v = lo + (hi - lo) * i / steps; str += (i === 0 ? "M" : "L") + X(v).toFixed(1) + " " + Y(g(v, mu, s, pk)).toFixed(1) + " "; } return str; };
      const fillUnder = (mu, s, pk, a, b, col, op) => { let str = `M${X(a).toFixed(1)} ${yTop} `; const steps = 120; for (let i = 0; i <= steps; i++) { const v = a + (b - a) * i / steps; str += "L" + X(v).toFixed(1) + " " + Y(g(v, mu, s, pk)).toFixed(1) + " "; } str += `L${X(b).toFixed(1)} ${yTop} Z`; return `<path d="${str}" fill="${col}" fill-opacity="${op}"/>`; };

      // power region (alt curve beyond critical lines) — green
      let shade = fillUnder(p, s1, peak1, cHi, hi, COL.good, 0.30);
      shade += fillUnder(p, s1, peak1, lo, cLo, COL.good, 0.30);
      // alpha region (null curve beyond critical lines) — red
      shade += fillUnder(0.5, s0, peak0, cHi, hi, COL.bad, 0.40);
      shade += fillUnder(0.5, s0, peak0, lo, cLo, COL.bad, 0.40);

      const curves =
        `<path d="${curve(p, s1, peak1)}" fill="none" stroke="${COL.good}" stroke-width="2.5"/>` +
        `<path d="${curve(0.5, s0, peak0)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      const crit = [cLo, cHi].map(c => (c > lo && c < hi)
        ? `<line x1="${X(c)}" y1="${padT}" x2="${X(c)}" y2="${yTop}" stroke="${COL.muted}" stroke-width="1.5" stroke-dasharray="4 3"/>` : "").join("");
      // axis ticks
      let ax = "";
      [0.4, 0.5, 0.6, 0.7, 0.8].forEach(t => { if (t > lo && t < hi) ax += `<text x="${X(t)}" y="${H - 8}" font-size="10" text-anchor="middle">${t}</text>`; });

      svg.innerHTML = shade + curves + crit + ax +
        `<text x="${X(0.5)}" y="${padT - 4}" font-size="10" text-anchor="middle" fill="${COL.accent}">fair</text>` +
        `<text x="${clamp(X(p), padL + 14, W - padR - 14)}" y="${padT - 4}" font-size="10" text-anchor="middle" fill="${COL.good}">true</text>`;

      if (power >= 0.8) { tag.textContent = "power ≥ 80% — reliable catch"; tag.className = "se-tag good"; }
      else if (power >= 0.5) { tag.textContent = "coin-flip odds of catching it"; tag.className = "se-tag warn"; }
      else { tag.textContent = "underpowered — it usually escapes"; tag.className = "se-tag hot"; }
    }
    [pS, nS, aS].forEach(s => s.addEventListener("input", draw));
    onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 4 — sample-size calculator + cost-vs-bias curve
  // ============================================================
  (function () {
    const svg = document.getElementById("ct4-svg");
    const pS = document.getElementById("ct4-p"), aS = document.getElementById("ct4-a"), wS = document.getElementById("ct4-w");
    const pvv = document.getElementById("ct4-pv"), avv = document.getElementById("ct4-av"), wvv = document.getElementById("ct4-wv");
    const nEl = document.getElementById("ct4-n");

    const W = 460, H = 240, padL = 50, padR = 16, padT = 16, padB = 36;

    function draw() {
      const p = pS.value / 100, alpha = aS.value / 1000, power = wS.value / 100;
      pvv.textContent = p.toFixed(2); avv.textContent = alpha.toFixed(3); wvv.textContent = (power * 100).toFixed(0) + "%";
      const nNow = requiredN(p, alpha, power);
      nEl.textContent = nNow.toLocaleString();

      // curve: required n across detectable biases, fixed alpha/power, log-y
      const pLo = 0.51, pHi = 0.80;
      const X = pp => padL + (pp - pLo) / (pHi - pLo) * (W - padL - padR);
      const yMaxLog = Math.log10(requiredN(pLo, alpha, power));
      const yMinLog = Math.log10(Math.max(2, requiredN(pHi, alpha, power)));
      const Y = nn => { const l = clamp(Math.log10(nn), yMinLog, yMaxLog); return H - padB - (l - yMinLog) / (yMaxLog - yMinLog) * (H - padT - padB); };

      let grid = "";
      // log gridlines at powers of ten
      for (let e = Math.floor(yMinLog); e <= Math.ceil(yMaxLog); e++) {
        const val = Math.pow(10, e); if (Math.log10(val) < yMinLog - 0.01 || Math.log10(val) > yMaxLog + 0.01) continue;
        grid += `<line x1="${padL}" y1="${Y(val)}" x2="${W - padR}" y2="${Y(val)}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${padL - 6}" y="${Y(val) + 3}" font-size="9" text-anchor="end">${val >= 1000 ? (val / 1000) + "k" : val}</text>`;
      }
      [0.55, 0.6, 0.65, 0.7, 0.75].forEach(t => { grid += `<text x="${X(t)}" y="${H - padB + 16}" font-size="9" text-anchor="middle">${t}</text>`; });

      let d = ""; const steps = 120;
      for (let i = 0; i <= steps; i++) { const pp = pLo + (pHi - pLo) * i / steps; d += (i === 0 ? "M" : "L") + X(pp).toFixed(1) + " " + Y(requiredN(pp, alpha, power)).toFixed(1) + " "; }
      const marker = (p >= pLo && p <= pHi)
        ? `<line x1="${X(p)}" y1="${Y(nNow)}" x2="${X(p)}" y2="${H - padB}" stroke="${COL.bad}" stroke-width="1.5" stroke-dasharray="3 3"/>` +
          `<circle cx="${X(p)}" cy="${Y(nNow)}" r="5" fill="${COL.bad}"/>` +
          `<text x="${clamp(X(p) + 8, padL, W - padR - 70)}" y="${clamp(Y(nNow) - 6, padT + 10, H - padB)}" font-size="11" fill="${COL.bad}" font-weight="700">${nNow.toLocaleString()}</text>`
        : "";

      svg.innerHTML = grid +
        `<path d="${d}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>` + marker +
        `<text x="${X((pLo + pHi) / 2)}" y="${H - 4}" font-size="11" text-anchor="middle">coin's true bias  p(heads)</text>` +
        `<text x="14" y="${padT + 84}" font-size="11" transform="rotate(-90 14 ${padT + 84})" text-anchor="middle">flips needed (log)</text>`;
    }
    [pS, aS, wS].forEach(s => s.addEventListener("input", draw));
    onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 5 — Monte-Carlo power check (simulation vs formula)
  // ============================================================
  (function () {
    const pS = document.getElementById("ct5-p"), nS = document.getElementById("ct5-n"), aS = document.getElementById("ct5-a");
    const pvv = document.getElementById("ct5-pv"), nvv = document.getElementById("ct5-nv"), avv = document.getElementById("ct5-av");
    const prog = document.getElementById("ct5-prog"), tbar = document.getElementById("ct5-tbar"), ebar = document.getElementById("ct5-ebar");
    const tval = document.getElementById("ct5-tval"), eval_ = document.getElementById("ct5-eval");
    const doneEl = document.getElementById("ct5-done"), caughtEl = document.getElementById("ct5-caught"), tag = document.getElementById("ct5-tag");
    const runBtn = document.getElementById("ct5-run");
    const TOTAL = 1000;

    let done = 0, caught = 0, running = false, raf = null;

    function theoryPower() { return powerAt(nS.value / 100, +nS.value, aS.value / 1000); }
    function reflectLabels() { pvv.textContent = (pS.value / 100).toFixed(2); nvv.textContent = nS.value; avv.textContent = (aS.value / 1000).toFixed(3); }

    function showTheory() {
      const t = theoryPower();
      tbar.style.width = (t * 100).toFixed(1) + "%"; tval.textContent = (t * 100).toFixed(1) + "%";
    }
    function reset() {
      stop(); done = 0; caught = 0;
      prog.style.width = "0%"; ebar.style.width = "0%"; eval_.textContent = "—";
      doneEl.textContent = "0"; caughtEl.textContent = "0"; tag.textContent = "ready"; tag.className = "se-tag";
      reflectLabels(); showTheory();
    }
    // one experiment: n flips at true p, reject if |k - n/2| beyond the critical deviation
    function oneExperiment(p, n, kc) {
      let k = 0; for (let i = 0; i < n; i++) if (Math.random() < p) k++;
      return Math.abs(k - n / 2) > kc;
    }
    function update() {
      prog.style.width = (done / TOTAL * 100).toFixed(1) + "%";
      doneEl.textContent = done; caughtEl.textContent = caught;
      const emp = done ? caught / done : 0;
      ebar.style.width = (emp * 100).toFixed(1) + "%";
      eval_.textContent = done ? (emp * 100).toFixed(1) + "%" : "—";
    }
    function loop() {
      if (!running) return;
      const p = pS.value / 100, n = +nS.value, alpha = aS.value / 1000;
      const kc = zQuant(1 - alpha / 2) * Math.sqrt(n) / 2;
      const batch = Math.min(25, TOTAL - done);
      for (let i = 0; i < batch; i++) { if (oneExperiment(p, n, kc)) caught++; done++; }
      update();
      if (done >= TOTAL) { finish(); return; }
      raf = requestAnimationFrame(loop);
    }
    function finish() {
      stop();
      const emp = caught / TOTAL, t = theoryPower();
      const diff = Math.abs(emp - t);
      tag.textContent = "simulated " + (emp * 100).toFixed(1) + "% vs predicted " + (t * 100).toFixed(1) + "%";
      tag.className = diff < 0.05 ? "se-tag good" : "se-tag warn";
    }
    function start() {
      if (done >= TOTAL) reset();
      running = true; runBtn.textContent = "⏳ Running…"; runBtn.disabled = true;
      showTheory(); raf = requestAnimationFrame(loop);
    }
    function stop() { running = false; runBtn.textContent = "▶ Run 1000 experiments"; runBtn.disabled = false; if (raf) cancelAnimationFrame(raf); }

    [pS, nS, aS].forEach(s => s.addEventListener("input", () => { reset(); }));
    runBtn.addEventListener("click", start);
    document.getElementById("ct5-reset").addEventListener("click", reset);
    onTheme(() => { showTheory(); });   // theory bar color/labels survive theme toggle
    reset();
  })();
})();
</script>

</div>]]></content><author><name></name></author><category term="Statistics" /><category term="Interactive" /><summary type="html"><![CDATA[A friend swears the coin is fair; you suspect it leans heads. How many flips would actually settle it? Flip a coin live and watch the evidence pile up, see a p-value light up, push the false-alarm and missed-detection curves apart, then read off the exact number from one clean formula — and confirm it with a thousand simulated experiments. The punchline: near-fair coins are brutally expensive to expose, and the cost scales like 1/(p−½)².]]></summary></entry><entry><title type="html">Options Theory, Visually: Payoffs, Black–Scholes, and the Greeks</title><link href="https://alexdruso.github.io/options-theory-visually/" rel="alternate" type="text/html" title="Options Theory, Visually: Payoffs, Black–Scholes, and the Greeks" /><published>2026-06-19T00:00:00+00:00</published><updated>2026-06-19T00:00:00+00:00</updated><id>https://alexdruso.github.io/options-theory-visually</id><content type="html" xml:base="https://alexdruso.github.io/options-theory-visually/"><![CDATA[<style>
/* ============================================================
   Study Notes — shared design system
   Sleek/futuristic: cyan-teal accent, hairline borders, flat
   single-accent controls, monospace numerals, glow in dark only.
   Adaptive light + dark via tokens. Every selector is scoped
   under .study-note so nothing leaks to the rest of the site.
   ============================================================ */
.study-note {
  --sn-accent: #0891b2;
  --sn-accent-strong: #06b6d4;
  --sn-accent-soft: rgba(8, 145, 178, 0.10);
  --sn-accent-line: rgba(8, 145, 178, 0.30);
  --sn-on-accent: #ffffff;

  --sn-panel: #ffffff;
  --sn-panel-inset: #f6f8fa;
  --sn-section: #f7f9fb;
  --sn-border: rgba(15, 23, 42, 0.08);
  --sn-grid: rgba(15, 23, 42, 0.10);

  --sn-text: #1f2933;
  --sn-muted: #64748b;

  --sn-good: #0f9d8c;  --sn-good-bg: rgba(15, 157, 140, 0.10);
  --sn-warn: #b07d18;  --sn-warn-bg: rgba(176, 125, 24, 0.10);
  --sn-bad:  #b4544b;  --sn-bad-bg:  rgba(180, 84, 75, 0.10);

  --sn-radius: 12px;
  --sn-radius-sm: 8px;
  --sn-shadow: 0 1px 2px rgba(15, 23, 42, 0.04), 0 4px 16px rgba(15, 23, 42, 0.05);
  --sn-ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --sn-glow: none;
  --sn-mono: ui-monospace, "SF Mono", "JetBrains Mono", "Roboto Mono", Menlo, Consolas, monospace;
}
.dark-theme .study-note {
  --sn-accent: #22d3ee;
  --sn-accent-strong: #67e8f9;
  --sn-accent-soft: rgba(34, 211, 238, 0.12);
  --sn-accent-line: rgba(34, 211, 238, 0.35);
  --sn-on-accent: #06141a;

  --sn-panel: #11131c;
  --sn-panel-inset: #161a26;
  --sn-section: #14161f;
  --sn-border: rgba(148, 163, 184, 0.14);
  --sn-grid: rgba(148, 163, 184, 0.16);

  --sn-text: #e2e8f0;
  --sn-muted: #94a3b8;

  --sn-good: #34d399;  --sn-good-bg: rgba(52, 211, 153, 0.12);
  --sn-warn: #e2b04a;  --sn-warn-bg: rgba(226, 176, 74, 0.12);
  --sn-bad:  #f0857c;  --sn-bad-bg:  rgba(240, 133, 124, 0.12);

  --sn-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
  --sn-glow: 0 0 0 1px var(--sn-accent-line), 0 0 16px rgba(34, 211, 238, 0.18);
}

/* ---- sections / cards ---- */
.study-note .se-section,
.study-note .bm-section,
.study-note .mdp-section {
  margin: 2rem 0;
  padding: 1.5rem;
  border-radius: var(--sn-radius);
  background: var(--sn-section);
  border: 1px solid var(--sn-border);
  box-shadow: var(--sn-shadow);
}
.study-note .se-section h3,
.study-note .bm-section h3,
.study-note .mdp-section h3 {
  margin-top: 0;
  color: var(--sn-accent);
  letter-spacing: -0.01em;
}

/* ---- equation boxes ---- */
.study-note .se-equation,
.study-note .bm-equation,
.study-note .equation-box {
  padding: 1rem 1.5rem;
  margin: 1rem 0;
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  font-size: 1.1rem;
  text-align: center;
  overflow-x: auto;
}
.study-note .se-equation .big,
.study-note .bm-equation .big { font-size: 1.25rem; }

/* ---- callout / note ---- */
.study-note .se-note,
.study-note .bm-note {
  background: var(--sn-accent-soft);
  border-radius: var(--sn-radius-sm);
  border-left: 3px solid var(--sn-accent);
  padding: 0.9rem 1.1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- example box ---- */
.study-note .se-example,
.study-note .bm-example,
.study-note .example-box {
  background: var(--sn-panel-inset);
  border: 1px solid var(--sn-border);
  border-left: 3px solid var(--sn-warn);
  border-radius: var(--sn-radius-sm);
  padding: 1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- chart containers ---- */
.study-note .se-chartwrap,
.study-note .bm-chartwrap,
.study-note .graph-wrap {
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.5rem;
}
.study-note .graph-wrap { overflow-x: auto; }

/* ---- primary buttons ---- */
.study-note .se-btn,
.study-note .bm-btn,
.study-note .vi-btn {
  padding: 0.5rem 1.1rem;
  border: none;
  border-radius: var(--sn-radius-sm);
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-on-accent);
  background: var(--sn-accent);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.2s var(--sn-ease), background 0.2s var(--sn-ease);
}
.study-note .se-btn:hover:not(:disabled),
.study-note .bm-btn:hover:not(:disabled),
.study-note .vi-btn:hover:not(:disabled) {
  background: var(--sn-accent-strong);
  transform: translateY(-1px);
  box-shadow: var(--sn-glow);
}
.study-note .se-btn:disabled,
.study-note .bm-btn:disabled,
.study-note .vi-btn:disabled { opacity: 0.4; cursor: not-allowed; }
/* secondary variant */
.study-note .se-btn.sec,
.study-note .bm-btn.sec,
.study-note .vi-btn.sec {
  background: transparent;
  color: var(--sn-accent);
  box-shadow: inset 0 0 0 1px var(--sn-accent-line);
}
.study-note .se-btn.sec:hover:not(:disabled),
.study-note .bm-btn.sec:hover:not(:disabled),
.study-note .vi-btn.sec:hover:not(:disabled) {
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
}

/* ---- ghost / reset buttons ---- */
.study-note .se-reset,
.study-note .bm-reset,
.study-note .reset-link {
  background: none;
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.4rem 0.85rem;
  font-size: 0.85rem;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-muted);
  transition: background 0.15s var(--sn-ease), border-color 0.15s var(--sn-ease), color 0.15s var(--sn-ease);
}
.study-note .se-reset:hover,
.study-note .bm-reset:hover,
.study-note .reset-link:hover {
  background: var(--sn-accent-soft);
  border-color: var(--sn-accent-line);
  color: var(--sn-accent);
}

/* ---- range sliders: flat, single-accent ---- */
.study-note input[type="range"] {
  -webkit-appearance: none;
  appearance: none;
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
  outline: none;
  cursor: pointer;
}
.study-note input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.15s var(--sn-ease);
}
.study-note input[type="range"]::-webkit-slider-thumb:hover { transform: scale(1.15); }
.study-note input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
}
.study-note input[type="range"]::-moz-range-track {
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
}

/* ---- status pills ---- */
.study-note .se-tag,
.study-note .bm-tag {
  display: inline-block;
  padding: 0.22rem 0.65rem;
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 0.01em;
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
  transition: background 0.2s var(--sn-ease), color 0.2s var(--sn-ease);
}
.study-note .se-tag.good, .study-note .bm-tag.good { background: var(--sn-good-bg); color: var(--sn-good); }
.study-note .se-tag.warn, .study-note .bm-tag.warn { background: var(--sn-warn-bg); color: var(--sn-warn); }
.study-note .se-tag.hot,  .study-note .bm-tag.hot  { background: var(--sn-bad-bg);  color: var(--sn-bad); }

/* ---- numerals: monospace, tabular, accent (size kept per-note) ---- */
.study-note .se-stat .val,
.study-note .bm-stat .val,
.study-note .score-item .sval,
.study-note .compare-score {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  color: var(--sn-accent);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.02em;
}
.study-note .se-readout,
.study-note .bm-temp-readout {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  font-weight: 600;
  color: var(--sn-accent);
}

/* ---- svg text (scoped; replaces the old leaky global rule) ---- */
.study-note svg text { fill: var(--sn-muted); }
</style>


<div class="study-note">

<style>
/* Note-specific layout only. Colors come from tokens; styled components
   (se-section, se-equation, se-note, se-example, se-chartwrap, se-btn,
   se-tag, se-stat, se-readout) come from study-note-styles.html. */
.ot-controls { display: flex; gap: 0.6rem; align-items: center; flex-wrap: wrap; margin: 0.7rem 0; }
.ot-controls .nm { font-size: 0.85rem; min-width: 3.2rem; }
.ot-readout { font-size: 1.0rem; min-width: 3.4rem; display: inline-block; text-align: right; }

.ot-flexwrap { display: flex; gap: 1.25rem; flex-wrap: wrap; align-items: flex-start; }
.ot-panel { flex: 1; min-width: 250px; }

.ot-stat { text-align: center; }
.ot-stat .lab { font-size: 0.66rem; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.55; }
.ot-stat .val { font-family: var(--sn-mono); font-variant-numeric: tabular-nums; color: var(--sn-accent); font-weight: 700; font-size: 1.5rem; line-height: 1.15; }
.ot-stat .val.good { color: var(--sn-good); }
.ot-stat .val.bad  { color: var(--sn-bad); }
.ot-statrow { display: flex; gap: 1.4rem; justify-content: center; flex-wrap: wrap; margin: 0.6rem 0; }

.ot-toggle { display: flex; gap: 0.4rem; flex-wrap: wrap; margin: 0.2rem 0 0.6rem; }
.ot-legend { font-size: 0.76rem; opacity: 0.78; display: flex; gap: 1.1rem; flex-wrap: wrap; margin-top: 0.45rem; }
.ot-legend span::before { content: "■ "; }

.ot-msg { font-size: 0.85rem; opacity: 0.88; text-align: center; min-height: 2.6em; margin-top: 0.4rem; }
</style>

<p>
  When I first met the <strong>Black–Scholes formula</strong> it arrived the way it usually does —
  <em>C = S·N(d₁) − K e<sup>−rT</sup>·N(d₂)</em>, scrawled on a board, with a promise that the two
  cumulative normals "fall out of the math." I memorised it, passed the exam, and could not have told
  you a week later <em>why</em> there were two of them. This post is the refresher I wish I'd had:
  every piece attached to something you can drag, widen, and run.
</p>

<p>Here's the whole story in one sentence, and everything below just unpacks it:</p>

<div class="se-note">
  <strong>An option's price is the discounted, risk-neutral expectation of its payoff.</strong>
  You hold the <em>right, not the obligation</em>, to trade at a fixed price, so your payoff is bent —
  all upside, capped downside. Because the payoff is convex, <strong>volatility creates value</strong>;
  and because a dynamic hedge can replicate the option, no-arbitrage pins the price to a single number.
  Black–Scholes is just that expectation, written in closed form.
</div>

<!-- ============================================================ -->
<!-- 1. PAYOFF & P/L                                              -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>📈 The bent payoff: calls, puts, and P/L</h3>
  <p style="margin-top:0">
    Owning the stock is a straight line: gain a dollar when it rises, lose one when it falls. An option
    <em>bends</em> that line at the <strong>strike</strong> K. A <strong>call</strong> is the right to
    <em>buy</em> at K; a <strong>put</strong> is the right to <em>sell</em> at K. At expiry only the
    intrinsic value survives:
  </p>
  <div class="se-equation">
    <span class="big">Call payoff = max(S<sub>T</sub> &minus; K, 0)</span> &nbsp;&nbsp;&middot;&nbsp;&nbsp;
    <span class="big">Put payoff = max(K &minus; S<sub>T</sub>, 0)</span>
  </div>
  <p style="margin-top:0">
    You pay a <strong>premium</strong> up front, so your <em>profit &amp; loss</em> is the payoff minus
    what you paid (and flipped if you're the seller). Toggle the contract, drag the strike, the premium,
    and the spot — the solid line is payoff at expiry, the dashed line is your P/L:
  </p>

  <div class="ot-toggle">
    <button class="se-btn" id="ot1-call">Call</button>
    <button class="se-btn sec" id="ot1-put">Put</button>
    <span style="width:0.6rem"></span>
    <button class="se-btn" id="ot1-long">Long (buy)</button>
    <button class="se-btn sec" id="ot1-short">Short (sell)</button>
  </div>

  <div class="ot-flexwrap">
    <div class="ot-panel" style="flex:2">
      <div class="se-chartwrap"><svg id="ot1-svg" width="100%" height="260" viewBox="0 0 480 260"></svg></div>
      <div class="ot-legend">
        <span style="color:var(--sn-accent)">payoff at expiry</span>
        <span style="color:var(--sn-good)">profit / loss</span>
        <span style="color:var(--sn-bad)">strike K</span>
      </div>
    </div>
    <div class="ot-panel">
      <div class="ot-controls"><span class="nm">spot S</span><input type="range" id="ot1-s" min="0" max="200" value="105" style="flex:1"><span class="ot-readout" id="ot1-sv">105</span></div>
      <div class="ot-controls"><span class="nm">strike K</span><input type="range" id="ot1-k" min="20" max="180" value="100" style="flex:1"><span class="ot-readout" id="ot1-kv">100</span></div>
      <div class="ot-controls"><span class="nm">premium</span><input type="range" id="ot1-prem" min="0" max="400" value="80" style="flex:1"><span class="ot-readout" id="ot1-pv">8.0</span></div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">moneyness</div><div class="val" id="ot1-money" style="font-size:1.1rem">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">breakeven</div><div class="val" id="ot1-be">&mdash;</div></div>
      </div>
      <div class="ot-msg" id="ot1-msg"></div>
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 2. INTRINSIC vs TIME VALUE                                   -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>⏳ Before expiry: intrinsic value plus time value</h3>
  <p style="margin-top:0">
    Before maturity an option is worth <em>more</em> than its intrinsic payoff, because the future is
    still uncertain — there's time for the bet to come good. The live price is a smooth curve sitting
    <strong>above</strong> the kinked payoff, and the gap is the <strong>time value</strong>:
  </p>
  <div class="se-equation"><span class="big">price = intrinsic value + time value</span></div>
  <p style="margin-top:0">
    Drag time-to-expiry T toward zero and watch the curve melt down onto the hockey stick — the time
    value bleeds away. That decay is <strong>theta</strong>, and it accelerates near expiry:
  </p>

  <div class="ot-flexwrap">
    <div class="ot-panel" style="flex:2">
      <div class="se-chartwrap"><svg id="ot2-svg" width="100%" height="260" viewBox="0 0 480 260"></svg></div>
      <div class="ot-legend">
        <span style="color:var(--sn-accent)">option price (T &gt; 0)</span>
        <span style="color:var(--sn-muted)">payoff at expiry</span>
        <span style="color:var(--sn-warn)">time value gap</span>
      </div>
    </div>
    <div class="ot-panel">
      <div class="ot-controls"><span class="nm">T (yrs)</span><input type="range" id="ot2-t" min="1" max="200" value="100" style="flex:1"><span class="ot-readout" id="ot2-tv">1.00</span></div>
      <div class="ot-controls"><span class="nm">vol &sigma;</span><input type="range" id="ot2-vol" min="5" max="80" value="25" style="flex:1"><span class="ot-readout" id="ot2-volv">0.25</span></div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">price (ATM)</div><div class="val" id="ot2-price">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">time value</div><div class="val" id="ot2-tval">&mdash;</div></div>
      </div>
      <div class="ot-msg" id="ot2-msg">K = 100, r = 2%. A call.</div>
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 3. VOLATILITY — THE KEYSTONE                                 -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🌪️ Volatility is the whole game</h3>
  <p style="margin-top:0">
    Here is the engine room. Under the <strong>risk-neutral</strong> measure the stock drifts at the
    risk-free rate and its terminal price is <strong>log-normal</strong>. The option is worth the
    discounted average payoff over that distribution:
  </p>
  <div class="se-equation">
    <span class="big">V = e<sup>&minus;rT</sup> &middot; E<sub>Q</sub>[ payoff(S<sub>T</sub>) ]</span>,
    &nbsp; with &nbsp; S<sub>T</sub> = S<sub>0</sub> e<sup>(r &minus; &sigma;&sup2;/2)T + &sigma;&radic;T &middot; Z</sup>, &nbsp; Z ~ 𝒩(0,1)
  </div>
  <p style="margin-top:0">
    Now the punchline. The payoff is <strong>convex</strong> — clipped at zero on the downside, open on
    the upside — so spreading the distribution wider <em>adds</em> expected payoff (big gains aren't
    cancelled by big losses, because the losses are capped). Drag <strong>&sigma;</strong>: the bell
    fattens, the shaded expected-payoff mass grows, and the price climbs. The readout computes
    e<sup>&minus;rT</sup>E[payoff] numerically — and it lands on the Black–Scholes price below.
  </p>

  <div class="ot-flexwrap">
    <div class="ot-panel" style="flex:2">
      <div class="se-chartwrap"><svg id="ot3-svg" width="100%" height="260" viewBox="0 0 480 260"></svg></div>
      <div class="ot-legend">
        <span style="color:var(--sn-accent)">density of S<sub>T</sub></span>
        <span style="color:var(--sn-good)">payoff × density (value)</span>
        <span style="color:var(--sn-bad)">strike K</span>
      </div>
    </div>
    <div class="ot-panel">
      <div class="ot-controls"><span class="nm">vol &sigma;</span><input type="range" id="ot3-vol" min="3" max="90" value="25" style="flex:1"><span class="ot-readout" id="ot3-volv">0.25</span></div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">E[payoff]·e<sup>&minus;rT</sup></div><div class="val" id="ot3-num">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">Black–Scholes</div><div class="val good" id="ot3-bs">&mdash;</div></div>
      </div>
      <div class="ot-msg" id="ot3-msg">S₀ = K = 100, r = 2%, T = 1. A call.</div>
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 4. BLACK–SCHOLES                                             -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🧮 Black–Scholes in closed form</h3>
  <p style="margin-top:0">
    Where does the formula come from? Build a portfolio of the option and &minus;&Delta; shares so the
    random moves cancel. A hedged, riskless portfolio must earn the risk-free rate — that no-arbitrage
    argument turns into the <strong>Black–Scholes PDE</strong>:
  </p>
  <div class="se-equation">
    <span class="big">&part;V/&part;t + &frac12;&sigma;&sup2;S&sup2; &part;&sup2;V/&part;S&sup2; + rS &part;V/&part;S &minus; rV = 0</span>
  </div>
  <p style="margin-top:0">
    Solve it with the terminal condition V(S, T) = payoff(S) and the discounted-expectation integral
    collapses into two cumulative normals:
  </p>
  <div class="se-equation">
    <span class="big">C = S·N(d<sub>1</sub>) &minus; K e<sup>&minus;rT</sup> N(d<sub>2</sub>)</span><br>
    <span style="font-size:0.95rem">d<sub>1</sub> = [ ln(S/K) + (r + &sigma;&sup2;/2)T ] / (&sigma;&radic;T), &nbsp;&nbsp; d<sub>2</sub> = d<sub>1</sub> &minus; &sigma;&radic;T</span>
  </div>
  <p style="margin-top:0">
    Read it as the bet itself: <strong>N(d<sub>2</sub>)</strong> is the risk-neutral probability the
    call finishes in the money; <strong>N(d<sub>1</sub>)</strong> is that probability re-weighted by how
    much stock you'd be holding. Drive all five inputs and watch the price and the curve respond:
  </p>

  <div class="ot-toggle">
    <button class="se-btn" id="ot4-call">Call</button>
    <button class="se-btn sec" id="ot4-put">Put</button>
  </div>
  <div class="ot-flexwrap">
    <div class="ot-panel" style="flex:2">
      <div class="se-chartwrap"><svg id="ot4-svg" width="100%" height="260" viewBox="0 0 480 260"></svg></div>
      <div class="ot-legend">
        <span style="color:var(--sn-accent)">option price vs spot</span>
        <span style="color:var(--sn-muted)">payoff at expiry</span>
        <span style="color:var(--sn-bad)">current spot S</span>
      </div>
    </div>
    <div class="ot-panel">
      <div class="ot-controls"><span class="nm">spot S</span><input type="range" id="ot4-s" min="20" max="180" value="100" style="flex:1"><span class="ot-readout" id="ot4-sv">100</span></div>
      <div class="ot-controls"><span class="nm">strike K</span><input type="range" id="ot4-k" min="20" max="180" value="100" style="flex:1"><span class="ot-readout" id="ot4-kv">100</span></div>
      <div class="ot-controls"><span class="nm">vol &sigma;</span><input type="range" id="ot4-vol" min="3" max="90" value="25" style="flex:1"><span class="ot-readout" id="ot4-volv">0.25</span></div>
      <div class="ot-controls"><span class="nm">T (yrs)</span><input type="range" id="ot4-t" min="1" max="200" value="100" style="flex:1"><span class="ot-readout" id="ot4-tv">1.00</span></div>
      <div class="ot-controls"><span class="nm">rate r</span><input type="range" id="ot4-r" min="0" max="120" value="20" style="flex:1"><span class="ot-readout" id="ot4-rv">0.02</span></div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">price</div><div class="val" id="ot4-price">&mdash;</div></div>
      </div>
      <div class="ot-statrow" style="gap:1.0rem;font-size:0.8rem">
        <div class="ot-stat"><div class="lab">d₁</div><div class="val" id="ot4-d1" style="font-size:1.0rem">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">d₂</div><div class="val" id="ot4-d2" style="font-size:1.0rem">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">N(d₁)</div><div class="val" id="ot4-nd1" style="font-size:1.0rem">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">N(d₂)</div><div class="val" id="ot4-nd2" style="font-size:1.0rem">&mdash;</div></div>
      </div>
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 5. THE GREEKS                                                -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🔢 The Greeks: slopes, curvature, and decay</h3>
  <p style="margin-top:0">
    The Greeks are just partial derivatives of the price — its sensitivities to each input. Two of them
    are right there in the picture: <strong>delta</strong> is the <em>slope</em> of the price curve, and
    <strong>gamma</strong> is its <em>curvature</em>. The tangent line below is delta; how fast that
    tangent rotates as you move spot is gamma.
  </p>
  <div class="se-equation">
    <span style="font-size:0.98rem">
      &Delta; = &part;V/&part;S = N(d<sub>1</sub>) &nbsp;&middot;&nbsp;
      &Gamma; = &part;&sup2;V/&part;S&sup2; = N'(d<sub>1</sub>)/(S&sigma;&radic;T) &nbsp;&middot;&nbsp;
      𝜈 = &part;V/&part;&sigma; = S·N'(d<sub>1</sub>)&radic;T
    </span><br>
    <span style="font-size:0.98rem">
      &Theta; = &part;V/&part;t = &minus;S·N'(d<sub>1</sub>)&sigma;/(2&radic;T) &minus; rK e<sup>&minus;rT</sup>N(d<sub>2</sub>) &nbsp;&middot;&nbsp;
      &rho; = &part;V/&part;r = K T e<sup>&minus;rT</sup>N(d<sub>2</sub>)
    </span>
  </div>
  <p style="margin-top:0">
    Slide spot, volatility, and time. <span class="se-tag good">Δ</span> a call's delta runs 0→1;
    <span class="se-tag good">Γ</span> gamma peaks at the money; <span class="se-tag hot">Θ</span> theta
    is negative (time hurts the owner); <span class="se-tag good">𝜈</span> vega is largest at the money
    and with more time left.
  </p>

  <div class="ot-flexwrap">
    <div class="ot-panel" style="flex:2">
      <div class="se-chartwrap"><svg id="ot5-svg" width="100%" height="260" viewBox="0 0 480 260"></svg></div>
      <div class="ot-legend">
        <span style="color:var(--sn-accent)">call price vs spot</span>
        <span style="color:var(--sn-warn)">tangent (slope = Δ)</span>
        <span style="color:var(--sn-bad)">current spot S</span>
      </div>
    </div>
    <div class="ot-panel">
      <div class="ot-controls"><span class="nm">spot S</span><input type="range" id="ot5-s" min="40" max="160" value="100" style="flex:1"><span class="ot-readout" id="ot5-sv">100</span></div>
      <div class="ot-controls"><span class="nm">vol &sigma;</span><input type="range" id="ot5-vol" min="5" max="90" value="25" style="flex:1"><span class="ot-readout" id="ot5-volv">0.25</span></div>
      <div class="ot-controls"><span class="nm">T (yrs)</span><input type="range" id="ot5-t" min="2" max="200" value="100" style="flex:1"><span class="ot-readout" id="ot5-tv">1.00</span></div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">Δ delta</div><div class="val" id="ot5-delta">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">Γ gamma</div><div class="val" id="ot5-gamma">&mdash;</div></div>
      </div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">Θ / day</div><div class="val" id="ot5-theta">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">𝜈 / 1%</div><div class="val" id="ot5-vega">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">ρ / 1%</div><div class="val" id="ot5-rho">&mdash;</div></div>
      </div>
      <div class="ot-msg" id="ot5-msg">K = 100, r = 2%. A call.</div>
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 6. MONTE CARLO — SHOWPIECE                                   -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🎲 Proof by simulation: Monte-Carlo onto Black–Scholes</h3>
  <p style="margin-top:0">
    If the price really is a risk-neutral expectation, we should be able to <em>just sample it</em>. Draw
    thousands of terminal prices S<sub>T</sub> from the log-normal, take each discounted payoff, and
    average. By the law of large numbers that running average must crawl onto the closed-form
    Black–Scholes price. Press start and watch the noisy estimate home in on the dashed line:
  </p>
  <div class="se-equation">
    <span class="big">Ĉ<sub>N</sub> = (e<sup>&minus;rT</sup>/N) &Sigma;<sub>i=1…N</sub> max(S<sub>T</sub><sup>(i)</sup> &minus; K, 0) &nbsp;&xrarr;&nbsp; C<sub>BS</sub></span>
  </div>

  <div class="ot-toggle">
    <button class="se-btn" id="ot6-start">▶ Start</button>
    <button class="se-btn sec" id="ot6-step">Step +500</button>
    <button class="se-reset" id="ot6-reset">Reset</button>
    <span style="flex:1"></span>
    <span class="nm" style="font-size:0.8rem;align-self:center">speed</span>
    <input type="range" id="ot6-speed" min="1" max="10" value="5" style="width:90px;align-self:center">
  </div>

  <div class="ot-flexwrap">
    <div class="ot-panel" style="flex:2">
      <div class="se-chartwrap"><svg id="ot6-svg" width="100%" height="260" viewBox="0 0 480 260"></svg></div>
      <div class="ot-legend">
        <span style="color:var(--sn-good)">running estimate Ĉ<sub>N</sub></span>
        <span style="color:var(--sn-accent)">Black–Scholes C</span>
        <span style="color:var(--sn-muted)">histogram of S<sub>T</sub></span>
      </div>
    </div>
    <div class="ot-panel">
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">samples N</div><div class="val" id="ot6-n" style="font-size:1.3rem">0</div></div>
        <div class="ot-stat"><div class="lab">estimate</div><div class="val good" id="ot6-est">&mdash;</div></div>
      </div>
      <div class="ot-statrow">
        <div class="ot-stat"><div class="lab">Black–Scholes</div><div class="val" id="ot6-bs">&mdash;</div></div>
        <div class="ot-stat"><div class="lab">rel. error</div><div class="val" id="ot6-err">&mdash;</div></div>
      </div>
      <div class="ot-msg" id="ot6-msg">S₀ = K = 100, r = 2%, &sigma; = 25%, T = 1. A call.</div>
    </div>
  </div>
</div>

<div class="se-note">
  <strong>The whole arc in one breath.</strong> A payoff is a bet bent at the strike → its fair price is
  the discounted, risk-neutral <em>expectation</em> of that bet → because the bet is convex, volatility
  is what you're really buying → a hedging argument turns that expectation into the Black–Scholes PDE and
  its two-normal solution → the Greeks are how that solution moves → and a few thousand random draws
  reproduce the price from scratch. No act of faith required.
</div>

<script>
(function () {
  "use strict";

  // ---- theme-aware colors ----
  const SN = document.querySelector(".study-note");
  const readCols = () => { const cs = getComputedStyle(SN); const c = n => cs.getPropertyValue(n).trim();
    return { accent: c("--sn-accent"), accentSoft: c("--sn-accent-soft"), good: c("--sn-good"),
             bad: c("--sn-bad"), warn: c("--sn-warn"), muted: c("--sn-muted"), grid: c("--sn-grid"),
             panel: c("--sn-panel") }; };
  let COL = readCols();
  const redrawFns = [];
  const onTheme = fn => { redrawFns.push(fn); return fn; };
  new MutationObserver(() => { COL = readCols(); redrawFns.forEach(f => f()); })
    .observe(document.body, { attributes: true, attributeFilter: ["class"] });

  // ---- math helpers ----
  const SQRT2PI = Math.sqrt(2 * Math.PI);
  function erf(x) { // Abramowitz & Stegun 7.1.26
    const s = x < 0 ? -1 : 1; x = Math.abs(x);
    const t = 1 / (1 + 0.3275911 * x);
    const y = 1 - (((((1.061405429 * t - 1.453152027) * t) + 1.421413741) * t - 0.284496736) * t + 0.254829592) * t * Math.exp(-x * x);
    return s * y;
  }
  const normCdf = x => 0.5 * (1 + erf(x / Math.SQRT2));
  const normPdf = x => Math.exp(-0.5 * x * x) / SQRT2PI;

  function bs(S, K, r, sig, T, type) {
    type = type || "call";
    if (T <= 1e-9 || sig <= 1e-9) {            // degenerate: pure intrinsic
      const intr = type === "call" ? Math.max(S - K, 0) : Math.max(K - S, 0);
      const delta = type === "call" ? (S > K ? 1 : 0) : (S < K ? -1 : 0);
      return { price: intr, d1: NaN, d2: NaN, delta: delta, gamma: 0, vega: 0, theta: 0, rho: 0, Nd1: NaN, Nd2: NaN };
    }
    const vt = sig * Math.sqrt(T);
    const d1 = (Math.log(S / K) + (r + 0.5 * sig * sig) * T) / vt;
    const d2 = d1 - vt;
    const Nd1 = normCdf(d1), Nd2 = normCdf(d2), nd1 = normPdf(d1);
    const disc = Math.exp(-r * T);
    let price, delta, theta, rho;
    if (type === "call") {
      price = S * Nd1 - K * disc * Nd2;
      delta = Nd1;
      theta = (-S * nd1 * sig) / (2 * Math.sqrt(T)) - r * K * disc * Nd2;
      rho = K * T * disc * Nd2;
    } else {
      price = K * disc * normCdf(-d2) - S * normCdf(-d1);
      delta = Nd1 - 1;
      theta = (-S * nd1 * sig) / (2 * Math.sqrt(T)) + r * K * disc * normCdf(-d2);
      rho = -K * T * disc * normCdf(-d2);
    }
    const gamma = nd1 / (S * vt);
    const vega = S * nd1 * Math.sqrt(T);
    return { price, d1, d2, delta, gamma, vega, theta, rho, Nd1, Nd2 };
  }

  // standard-normal sampler (Box–Muller)
  let spare = null;
  function randn() {
    if (spare !== null) { const v = spare; spare = null; return v; }
    let u, v, s;
    do { u = Math.random() * 2 - 1; v = Math.random() * 2 - 1; s = u * u + v * v; } while (s >= 1 || s === 0);
    const m = Math.sqrt(-2 * Math.log(s) / s);
    spare = v * m; return u * m;
  }

  // ---- small SVG plotting helpers ----
  const SVGNS = "http://www.w3.org/2000/svg";
  const pathOf = pts => pts.map((p, i) => (i ? "L" : "M") + p[0].toFixed(1) + " " + p[1].toFixed(1)).join(" ");
  function frame(W, H, padL, padR, padT, padB) {
    return { W, H, padL, padR, padT, padB,
      xAt(v, lo, hi) { return padL + (v - lo) / (hi - lo) * (W - padL - padR); },
      yAt(v, lo, hi) { return H - padB - (v - lo) / (hi - lo) * (H - padT - padB); } };
  }
  function axes(f, xticks, yticks, xfmt, yfmt) {
    let g = "";
    xticks.forEach(t => {
      g += `<line x1="${t.x}" y1="${f.padT}" x2="${t.x}" y2="${f.H - f.padB}" stroke="${COL.grid}" stroke-width="1"/>`;
      g += `<text x="${t.x}" y="${f.H - f.padB + 15}" font-size="10" text-anchor="middle">${xfmt(t.v)}</text>`;
    });
    yticks.forEach(t => {
      g += `<line x1="${f.padL}" y1="${t.y}" x2="${f.W - f.padR}" y2="${t.y}" stroke="${COL.grid}" stroke-width="1"/>`;
      g += `<text x="${f.padL - 6}" y="${t.y + 3}" font-size="10" text-anchor="end">${yfmt(t.v)}</text>`;
    });
    return g;
  }

  // ============================================================
  // DEMO 1 — payoff & P/L
  // ============================================================
  (function () {
    const svg = document.getElementById("ot1-svg");
    const sS = document.getElementById("ot1-s"), kS = document.getElementById("ot1-k"), pS = document.getElementById("ot1-prem");
    const sV = document.getElementById("ot1-sv"), kV = document.getElementById("ot1-kv"), pV = document.getElementById("ot1-pv");
    const moneyEl = document.getElementById("ot1-money"), beEl = document.getElementById("ot1-be"), msg = document.getElementById("ot1-msg");
    const bCall = document.getElementById("ot1-call"), bPut = document.getElementById("ot1-put");
    const bLong = document.getElementById("ot1-long"), bShort = document.getElementById("ot1-short");
    let type = "call", side = 1; // side 1 long, -1 short

    function setBtns() {
      bCall.className = type === "call" ? "se-btn" : "se-btn sec";
      bPut.className = type === "put" ? "se-btn" : "se-btn sec";
      bLong.className = side === 1 ? "se-btn" : "se-btn sec";
      bShort.className = side === -1 ? "se-btn" : "se-btn sec";
    }
    const payoff = (st, K) => type === "call" ? Math.max(st - K, 0) : Math.max(K - st, 0);

    const f = frame(480, 260, 46, 16, 16, 38);
    const SMAX = 200;

    function draw() {
      const S = +sS.value, K = +kS.value, prem = +pS.value / 10;
      sV.textContent = S; kV.textContent = K; pV.textContent = prem.toFixed(1);

      // y-range of P/L: include extremes
      const plAt = st => side * (payoff(st, K) - prem);
      let lo = Infinity, hi = -Infinity;
      for (let st = 0; st <= SMAX; st += 4) { const v = plAt(st); lo = Math.min(lo, v); hi = Math.max(hi, v); const py = payoff(st, K) * side; lo = Math.min(lo, py); hi = Math.max(hi, py); }
      lo = Math.min(lo, 0); hi = Math.max(hi, 0);
      const pad = (hi - lo) * 0.1 || 10; lo -= pad; hi += pad;

      const xT = [0, 50, 100, 150, 200].map(v => ({ v, x: f.xAt(v, 0, SMAX) }));
      const yvals = [];
      const step = niceStep((hi - lo) / 5);
      for (let v = Math.ceil(lo / step) * step; v <= hi; v += step) yvals.push(v);
      const yT = yvals.map(v => ({ v, y: f.yAt(v, lo, hi) }));

      const payPts = [], plPts = [];
      for (let st = 0; st <= SMAX; st += 2) { payPts.push([f.xAt(st, 0, SMAX), f.yAt(payoff(st, K) * side, lo, hi)]); plPts.push([f.xAt(st, 0, SMAX), f.yAt(plAt(st), lo, hi)]); }

      const zeroY = f.yAt(0, lo, hi);
      const kx = f.xAt(K, 0, SMAX), sx = f.xAt(S, 0, SMAX);

      let out = axes(f, xT, yT, v => v, v => v.toFixed(0));
      out += `<line x1="${f.padL}" y1="${zeroY}" x2="${f.W - f.padR}" y2="${zeroY}" stroke="${COL.muted}" stroke-width="1"/>`;
      out += `<line x1="${kx}" y1="${f.padT}" x2="${kx}" y2="${f.H - f.padB}" stroke="${COL.bad}" stroke-width="1.4" stroke-dasharray="3 3"/>`;
      out += `<text x="${kx}" y="${f.padT - 3}" font-size="10" fill="${COL.bad}" text-anchor="middle" font-weight="700">K</text>`;
      out += `<line x1="${sx}" y1="${f.padT}" x2="${sx}" y2="${f.H - f.padB}" stroke="${COL.accent}" stroke-width="1" stroke-dasharray="1 3" opacity="0.6"/>`;
      out += `<path d="${pathOf(payPts)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      out += `<path d="${pathOf(plPts)}" fill="none" stroke="${COL.good}" stroke-width="2" stroke-dasharray="5 4"/>`;
      out += `<text x="${(f.padL + f.W - f.padR) / 2}" y="${f.H - 4}" font-size="11" text-anchor="middle">stock price at expiry  Sₜ</text>`;
      svg.innerHTML = out;

      // moneyness (from the option holder's economic view, ignoring side)
      const itm = type === "call" ? S > K : S < K;
      const atm = Math.abs(S - K) <= 2;
      moneyEl.textContent = atm ? "ATM" : (itm ? "ITM" : "OTM");
      moneyEl.className = "val " + (atm ? "" : (itm ? "good" : "bad"));
      moneyEl.style.fontSize = "1.1rem";
      const be = type === "call" ? K + prem : K - prem;
      beEl.textContent = be.toFixed(1);

      const who = side === 1 ? "You bought" : "You sold";
      const dir = type === "call" ? "rises above" : "falls below";
      msg.innerHTML = `${who} a ${type}. ` + (side === 1
        ? `Risk is capped at the &minus;${prem.toFixed(1)} premium; you profit once S<sub>T</sub> ${dir} the breakeven ${be.toFixed(1)}.`
        : `You keep the ${prem.toFixed(1)} premium unless S<sub>T</sub> ${dir} ${be.toFixed(1)} — then losses run the other way.`);
    }

    bCall.onclick = () => { type = "call"; setBtns(); draw(); };
    bPut.onclick = () => { type = "put"; setBtns(); draw(); };
    bLong.onclick = () => { side = 1; setBtns(); draw(); };
    bShort.onclick = () => { side = -1; setBtns(); draw(); };
    [sS, kS, pS].forEach(el => el.addEventListener("input", draw));
    setBtns(); onTheme(draw); draw();
  })();

  function niceStep(raw) {
    const p = Math.pow(10, Math.floor(Math.log10(raw)));
    const n = raw / p;
    return (n < 1.5 ? 1 : n < 3 ? 2 : n < 7 ? 5 : 10) * p;
  }

  // ============================================================
  // DEMO 2 — intrinsic vs time value
  // ============================================================
  (function () {
    const svg = document.getElementById("ot2-svg");
    const tS = document.getElementById("ot2-t"), volS = document.getElementById("ot2-vol");
    const tV = document.getElementById("ot2-tv"), volV = document.getElementById("ot2-volv");
    const priceEl = document.getElementById("ot2-price"), tvalEl = document.getElementById("ot2-tval"), msg = document.getElementById("ot2-msg");
    const K = 100, r = 0.02, f = frame(480, 260, 40, 16, 16, 38), SMAX = 200;

    function draw() {
      const T = +tS.value / 100, sig = +volS.value / 100;
      tV.textContent = T.toFixed(2); volV.textContent = sig.toFixed(2);
      const ymax = bs(SMAX, K, r, sig, T, "call").price * 1.05 || 100;
      const xT = [0, 50, 100, 150, 200].map(v => ({ v, x: f.xAt(v, 0, SMAX) }));
      const yT = []; const step = niceStep(ymax / 5);
      for (let v = 0; v <= ymax; v += step) yT.push({ v, y: f.yAt(v, 0, ymax) });

      const payPts = [], prPts = [], band = [];
      for (let st = 0; st <= SMAX; st += 2) {
        const intr = Math.max(st - K, 0), pr = bs(st, K, r, sig, T, "call").price;
        payPts.push([f.xAt(st, 0, SMAX), f.yAt(intr, 0, ymax)]);
        prPts.push([f.xAt(st, 0, SMAX), f.yAt(pr, 0, ymax)]);
      }
      // time-value band = area between price and payoff
      for (let st = 0; st <= SMAX; st += 2) band.push([f.xAt(st, 0, SMAX), f.yAt(bs(st, K, r, sig, T, "call").price, 0, ymax)]);
      for (let st = SMAX; st >= 0; st -= 2) band.push([f.xAt(st, 0, SMAX), f.yAt(Math.max(st - K, 0), 0, ymax)]);

      let out = axes(f, xT, yT, v => v, v => v.toFixed(0));
      out += `<path d="${pathOf(band)} Z" fill="${COL.warn}" opacity="0.14" stroke="none"/>`;
      out += `<path d="${pathOf(payPts)}" fill="none" stroke="${COL.muted}" stroke-width="1.6" stroke-dasharray="4 3"/>`;
      out += `<path d="${pathOf(prPts)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      out += `<text x="${(f.padL + f.W - f.padR) / 2}" y="${f.H - 4}" font-size="11" text-anchor="middle">spot S</text>`;
      svg.innerHTML = out;

      const atm = bs(K, K, r, sig, T, "call");
      priceEl.textContent = atm.price.toFixed(2);
      tvalEl.textContent = atm.price.toFixed(2); // ATM intrinsic ≈ 0
      tvalEl.className = "val warn";
      msg.innerHTML = T < 0.05
        ? "Almost no time left: the curve has collapsed onto the payoff. Time value &asymp; 0."
        : `K = 100, r = 2%. At expiry the whole orange gap (time value) decays to zero.`;
    }
    [tS, volS].forEach(el => el.addEventListener("input", draw));
    onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 3 — volatility / risk-neutral expectation
  // ============================================================
  (function () {
    const svg = document.getElementById("ot3-svg");
    const volS = document.getElementById("ot3-vol"), volV = document.getElementById("ot3-volv");
    const numEl = document.getElementById("ot3-num"), bsEl = document.getElementById("ot3-bs");
    const S0 = 100, K = 100, r = 0.02, T = 1, f = frame(480, 260, 40, 16, 16, 38), SMAX = 260;

    function draw() {
      const sig = +volS.value / 100; volV.textContent = sig.toFixed(2);
      const vt = sig * Math.sqrt(T);
      const mu = Math.log(S0) + (r - 0.5 * sig * sig) * T;       // lognormal params of S_T
      const dens = x => x <= 0 ? 0 : Math.exp(-Math.pow(Math.log(x) - mu, 2) / (2 * vt * vt)) / (x * vt * SQRT2PI);

      // numeric discounted E[payoff]
      let acc = 0; const dx = 0.5;
      for (let x = dx / 2; x < 600; x += dx) acc += Math.max(x - K, 0) * dens(x) * dx;
      const numV = Math.exp(-r * T) * acc;
      numEl.textContent = numV.toFixed(2);
      bsEl.textContent = bs(S0, K, r, sig, T, "call").price.toFixed(2);

      // scale density to plot height; value curve = payoff*density (own scale, shown relative)
      let dmax = 0; for (let x = 1; x <= SMAX; x += 2) dmax = Math.max(dmax, dens(x));
      const dPts = [], vPts = []; let vmax = 0;
      for (let x = 0; x <= SMAX; x += 2) { const vv = Math.max(x - K, 0) * dens(x); if (vv > vmax) vmax = vv; }
      const xT = [0, 50, 100, 150, 200, 250].map(v => ({ v, x: f.xAt(v, 0, SMAX) }));
      for (let x = 0; x <= SMAX; x += 2) {
        dPts.push([f.xAt(x, 0, SMAX), f.yAt(dens(x) / dmax, 0, 1.08)]);
        vPts.push([f.xAt(x, 0, SMAX), f.yAt((Math.max(x - K, 0) * dens(x)) / (vmax || 1) * 0.85, 0, 1.08)]);
      }
      // shaded value area
      const area = vPts.slice();
      area.push([f.xAt(SMAX, 0, SMAX), f.yAt(0, 0, 1.08)]);
      area.push([f.xAt(0, 0, SMAX), f.yAt(0, 0, 1.08)]);

      const kx = f.xAt(K, 0, SMAX);
      let out = axes(f, xT, [], v => v, v => v);
      out += `<path d="${pathOf(area)} Z" fill="${COL.good}" opacity="0.16" stroke="none"/>`;
      out += `<line x1="${kx}" y1="${f.padT}" x2="${kx}" y2="${f.H - f.padB}" stroke="${COL.bad}" stroke-width="1.4" stroke-dasharray="3 3"/>`;
      out += `<text x="${kx}" y="${f.padT - 3}" font-size="10" fill="${COL.bad}" text-anchor="middle" font-weight="700">K</text>`;
      out += `<path d="${pathOf(dPts)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      out += `<path d="${pathOf(vPts)}" fill="none" stroke="${COL.good}" stroke-width="1.8"/>`;
      out += `<text x="${(f.padL + f.W - f.padR) / 2}" y="${f.H - 4}" font-size="11" text-anchor="middle">terminal price Sₜ</text>`;
      svg.innerHTML = out;
    }
    volS.addEventListener("input", draw); onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 4 — Black–Scholes pricing
  // ============================================================
  (function () {
    const svg = document.getElementById("ot4-svg");
    const sS = document.getElementById("ot4-s"), kS = document.getElementById("ot4-k"), volS = document.getElementById("ot4-vol"), tS = document.getElementById("ot4-t"), rS = document.getElementById("ot4-r");
    const sV = document.getElementById("ot4-sv"), kV = document.getElementById("ot4-kv"), volV = document.getElementById("ot4-volv"), tV = document.getElementById("ot4-tv"), rV = document.getElementById("ot4-rv");
    const priceEl = document.getElementById("ot4-price"), d1El = document.getElementById("ot4-d1"), d2El = document.getElementById("ot4-d2"), nd1El = document.getElementById("ot4-nd1"), nd2El = document.getElementById("ot4-nd2");
    const bCall = document.getElementById("ot4-call"), bPut = document.getElementById("ot4-put");
    let type = "call";
    const f = frame(480, 260, 40, 16, 16, 38), SMAX = 200;
    const setBtns = () => { bCall.className = type === "call" ? "se-btn" : "se-btn sec"; bPut.className = type === "put" ? "se-btn" : "se-btn sec"; };

    function draw() {
      const S = +sS.value, K = +kS.value, sig = +volS.value / 100, T = +tS.value / 100, r = +rS.value / 1000;
      sV.textContent = S; kV.textContent = K; volV.textContent = sig.toFixed(2); tV.textContent = T.toFixed(2); rV.textContent = r.toFixed(3);
      const o = bs(S, K, r, sig, T, type);
      priceEl.textContent = o.price.toFixed(2);
      d1El.textContent = isNaN(o.d1) ? "—" : o.d1.toFixed(2);
      d2El.textContent = isNaN(o.d2) ? "—" : o.d2.toFixed(2);
      nd1El.textContent = isNaN(o.Nd1) ? "—" : o.Nd1.toFixed(3);
      nd2El.textContent = isNaN(o.Nd2) ? "—" : o.Nd2.toFixed(3);

      const intr = st => type === "call" ? Math.max(st - K, 0) : Math.max(K - st, 0);
      let ymax = 0; for (let st = 0; st <= SMAX; st += 4) ymax = Math.max(ymax, bs(st, K, r, sig, T, type).price, intr(st));
      ymax = ymax * 1.08 || 100;
      const xT = [0, 50, 100, 150, 200].map(v => ({ v, x: f.xAt(v, 0, SMAX) }));
      const yT = []; const step = niceStep(ymax / 5);
      for (let v = 0; v <= ymax; v += step) yT.push({ v, y: f.yAt(v, 0, ymax) });

      const prPts = [], payPts = [];
      for (let st = 0; st <= SMAX; st += 2) { prPts.push([f.xAt(st, 0, SMAX), f.yAt(bs(st, K, r, sig, T, type).price, 0, ymax)]); payPts.push([f.xAt(st, 0, SMAX), f.yAt(intr(st), 0, ymax)]); }
      const sx = f.xAt(S, 0, SMAX);
      let out = axes(f, xT, yT, v => v, v => v.toFixed(0));
      out += `<path d="${pathOf(payPts)}" fill="none" stroke="${COL.muted}" stroke-width="1.6" stroke-dasharray="4 3"/>`;
      out += `<path d="${pathOf(prPts)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      out += `<line x1="${sx}" y1="${f.padT}" x2="${sx}" y2="${f.H - f.padB}" stroke="${COL.bad}" stroke-width="1.2" stroke-dasharray="3 3"/>`;
      out += `<circle cx="${sx}" cy="${f.yAt(o.price, 0, ymax)}" r="4" fill="${COL.accent}"/>`;
      out += `<text x="${(f.padL + f.W - f.padR) / 2}" y="${f.H - 4}" font-size="11" text-anchor="middle">spot S</text>`;
      svg.innerHTML = out;
    }
    bCall.onclick = () => { type = "call"; setBtns(); draw(); };
    bPut.onclick = () => { type = "put"; setBtns(); draw(); };
    [sS, kS, volS, tS, rS].forEach(el => el.addEventListener("input", draw));
    setBtns(); onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 5 — the Greeks
  // ============================================================
  (function () {
    const svg = document.getElementById("ot5-svg");
    const sS = document.getElementById("ot5-s"), volS = document.getElementById("ot5-vol"), tS = document.getElementById("ot5-t");
    const sV = document.getElementById("ot5-sv"), volV = document.getElementById("ot5-volv"), tV = document.getElementById("ot5-tv");
    const dEl = document.getElementById("ot5-delta"), gEl = document.getElementById("ot5-gamma"), thEl = document.getElementById("ot5-theta"), vEl = document.getElementById("ot5-vega"), rhEl = document.getElementById("ot5-rho");
    const K = 100, r = 0.02, f = frame(480, 260, 40, 16, 16, 38), SMIN = 40, SMAX = 160;

    function draw() {
      const S = +sS.value, sig = +volS.value / 100, T = +tS.value / 100;
      sV.textContent = S; volV.textContent = sig.toFixed(2); tV.textContent = T.toFixed(2);
      const o = bs(S, K, r, sig, T, "call");
      dEl.textContent = o.delta.toFixed(3);
      gEl.textContent = o.gamma.toFixed(4);
      thEl.textContent = (o.theta / 365).toFixed(3); thEl.className = "val bad";
      vEl.textContent = (o.vega / 100).toFixed(3);
      rhEl.textContent = (o.rho / 100).toFixed(3);

      let ymax = 0; for (let st = SMIN; st <= SMAX; st += 2) ymax = Math.max(ymax, bs(st, K, r, sig, T, "call").price);
      ymax *= 1.1;
      const xT = [40, 70, 100, 130, 160].map(v => ({ v, x: f.xAt(v, SMIN, SMAX) }));
      const yT = []; const step = niceStep(ymax / 5);
      for (let v = 0; v <= ymax; v += step) yT.push({ v, y: f.yAt(v, 0, ymax) });

      const prPts = [];
      for (let st = SMIN; st <= SMAX; st += 1) prPts.push([f.xAt(st, SMIN, SMAX), f.yAt(bs(st, K, r, sig, T, "call").price, 0, ymax)]);

      // tangent at S: line through (S, price) with slope delta (in price units per spot unit)
      const x1 = SMIN, x2 = SMAX;
      const y1 = o.price + o.delta * (x1 - S), y2 = o.price + o.delta * (x2 - S);
      const sx = f.xAt(S, SMIN, SMAX);

      let out = axes(f, xT, yT, v => v, v => v.toFixed(0));
      out += `<line x1="${f.xAt(x1, SMIN, SMAX)}" y1="${f.yAt(y1, 0, ymax)}" x2="${f.xAt(x2, SMIN, SMAX)}" y2="${f.yAt(y2, 0, ymax)}" stroke="${COL.warn}" stroke-width="1.6" stroke-dasharray="5 4"/>`;
      out += `<path d="${pathOf(prPts)}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>`;
      out += `<line x1="${sx}" y1="${f.padT}" x2="${sx}" y2="${f.H - f.padB}" stroke="${COL.bad}" stroke-width="1.2" stroke-dasharray="3 3"/>`;
      out += `<circle cx="${sx}" cy="${f.yAt(o.price, 0, ymax)}" r="4.5" fill="${COL.accent}"/>`;
      out += `<text x="${(f.padL + f.W - f.padR) / 2}" y="${f.H - 4}" font-size="11" text-anchor="middle">spot S</text>`;
      svg.innerHTML = out;
    }
    [sS, volS, tS].forEach(el => el.addEventListener("input", draw));
    onTheme(draw); draw();
  })();

  // ============================================================
  // DEMO 6 — Monte Carlo convergence
  // ============================================================
  (function () {
    const svg = document.getElementById("ot6-svg");
    const bStart = document.getElementById("ot6-start"), bStep = document.getElementById("ot6-step"), bReset = document.getElementById("ot6-reset"), speed = document.getElementById("ot6-speed");
    const nEl = document.getElementById("ot6-n"), estEl = document.getElementById("ot6-est"), bsEl = document.getElementById("ot6-bs"), errEl = document.getElementById("ot6-err");
    const S0 = 100, K = 100, r = 0.02, sig = 0.25, T = 1;
    const C = bs(S0, K, r, sig, T, "call").price;
    const disc = Math.exp(-r * T), drift = (r - 0.5 * sig * sig) * T, vol = sig * Math.sqrt(T);
    const f = frame(480, 260, 44, 16, 16, 38);
    const NMAX = 100000;

    let n = 0, sum = 0, running = [];      // running[]: [N, estimate]
    const NBINS = 40, binLo = 20, binHi = 240, bins = new Array(NBINS).fill(0); let binMax = 0;
    let timer = null;

    function sampleOne() {
      const st = S0 * Math.exp(drift + vol * randn());
      sum += Math.max(st - K, 0); n++;
      const bi = Math.max(0, Math.min(NBINS - 1, Math.floor((st - binLo) / (binHi - binLo) * NBINS)));
      bins[bi]++; if (bins[bi] > binMax) binMax = bins[bi];
    }
    function batch(k) {
      for (let i = 0; i < k && n < NMAX; i++) sampleOne();
      const est = disc * sum / Math.max(n, 1);
      // log-spaced sampling of the running curve for a stable plot
      if (running.length === 0 || n >= running[running.length - 1][0] * 1.04 || n >= NMAX) running.push([n, est]);
      update(est);
    }
    function update(est) {
      nEl.textContent = n.toLocaleString();
      estEl.textContent = n ? est.toFixed(3) : "—";
      bsEl.textContent = C.toFixed(3);
      errEl.textContent = n ? (100 * Math.abs(est - C) / C).toFixed(2) + "%" : "—";
      draw();
    }

    function draw() {
      // top 2/3: convergence curve (log x). bottom 1/3: histogram.
      const splitY = f.padT + (f.H - f.padT - f.padB) * 0.62;
      const xLo = Math.log10(10), xHi = Math.log10(NMAX);
      const cy = { yAt(v, lo, hi) { return splitY - (v - lo) / (hi - lo) * (splitY - f.padT); } };
      const cx = v => f.padL + (Math.log10(Math.max(v, 10)) - xLo) / (xHi - xLo) * (f.W - f.padL - f.padR);

      const yLo = C * 0.4, yHi = C * 1.6;
      let out = "";
      // y grid for convergence
      [C * 0.5, C * 0.75, C, C * 1.25, C * 1.5].forEach(v => {
        const y = cy.yAt(v, yLo, yHi);
        out += `<line x1="${f.padL}" y1="${y}" x2="${f.W - f.padR}" y2="${y}" stroke="${COL.grid}" stroke-width="1"/>`;
        out += `<text x="${f.padL - 6}" y="${y + 3}" font-size="9" text-anchor="end">${v.toFixed(1)}</text>`;
      });
      [10, 100, 1000, 10000, 100000].forEach(v => {
        out += `<text x="${cx(v)}" y="${splitY + 13}" font-size="9" text-anchor="middle">${v >= 1000 ? (v / 1000) + "k" : v}</text>`;
      });
      // BS target line
      const tY = cy.yAt(C, yLo, yHi);
      out += `<line x1="${f.padL}" y1="${tY}" x2="${f.W - f.padR}" y2="${tY}" stroke="${COL.accent}" stroke-width="1.6" stroke-dasharray="6 4"/>`;
      out += `<text x="${f.W - f.padR}" y="${tY - 4}" font-size="9" fill="${COL.accent}" text-anchor="end">C = ${C.toFixed(2)}</text>`;
      // running estimate
      if (running.length > 1) {
        const pts = running.map(p => [cx(p[0]), cy.yAt(Math.max(yLo, Math.min(yHi, p[1])), yLo, yHi)]);
        out += `<path d="${pathOf(pts)}" fill="none" stroke="${COL.good}" stroke-width="2"/>`;
        const last = pts[pts.length - 1];
        out += `<circle cx="${last[0]}" cy="${last[1]}" r="3.5" fill="${COL.good}"/>`;
      }
      out += `<text x="${f.padL}" y="${f.padT + 2}" font-size="9" fill="${COL.muted}">estimate vs samples (log scale)</text>`;

      // histogram (bottom)
      const hTop = splitY + 22, hBot = f.H - f.padB + 0;
      const bw = (f.W - f.padL - f.padR) / NBINS;
      for (let i = 0; i < NBINS; i++) {
        if (!bins[i]) continue;
        const h = bins[i] / binMax * (hBot - hTop);
        out += `<rect x="${(f.padL + i * bw).toFixed(1)}" y="${(hBot - h).toFixed(1)}" width="${(bw - 1).toFixed(1)}" height="${h.toFixed(1)}" fill="${COL.muted}" opacity="0.5"/>`;
      }
      const kx = f.padL + (K - binLo) / (binHi - binLo) * (f.W - f.padL - f.padR);
      out += `<line x1="${kx}" y1="${hTop}" x2="${kx}" y2="${hBot}" stroke="${COL.bad}" stroke-width="1.2" stroke-dasharray="3 3"/>`;
      out += `<text x="${kx}" y="${hBot + 12}" font-size="9" fill="${COL.bad}" text-anchor="middle">K  (Sₜ →)</text>`;
      svg.innerHTML = out;
    }

    function loop() {
      const k = [0, 30, 80, 160, 300, 500, 800, 1200, 1800, 2600, 4000][+speed.value];
      batch(k);
      if (n >= NMAX) { stop(); return; }
      timer = requestAnimationFrame(loop);
    }
    function start() { if (timer) return; bStart.textContent = "⏸ Pause"; timer = requestAnimationFrame(loop); }
    function stop() { if (timer) cancelAnimationFrame(timer); timer = null; bStart.textContent = n >= NMAX ? "▶ Start" : "▶ Resume"; }
    function reset() { stop(); n = 0; sum = 0; running = []; bins.fill(0); binMax = 0; bStart.textContent = "▶ Start"; update(0); }

    bStart.onclick = () => { timer ? stop() : start(); };
    bStep.onclick = () => { stop(); batch(500); };
    bReset.onclick = reset;
    onTheme(draw);
    reset();
  })();

})();
</script>

</div>]]></content><author><name></name></author><category term="Finance" /><category term="Interactive" /><summary type="html"><![CDATA[An option price looks like an act of faith — N(d₁), N(d₂), a formula nobody re-derives twice. This is the refresher I wish I'd had: an option's price is just the discounted, risk-neutral expectation of its payoff, and volatility is the whole game. Drag the strike, widen the volatility, watch a Monte-Carlo average crawl onto the Black–Scholes price, and feel the Greeks move as slopes and curvature — live in your browser.]]></summary></entry><entry><title type="html">Shannon Entropy, Visually: Surprise, Uncertainty, and Bits</title><link href="https://alexdruso.github.io/shannon-entropy-visually/" rel="alternate" type="text/html" title="Shannon Entropy, Visually: Surprise, Uncertainty, and Bits" /><published>2026-06-16T00:00:00+00:00</published><updated>2026-06-16T00:00:00+00:00</updated><id>https://alexdruso.github.io/shannon-entropy-visually</id><content type="html" xml:base="https://alexdruso.github.io/shannon-entropy-visually/"><![CDATA[<style>
/* ============================================================
   Study Notes — shared design system
   Sleek/futuristic: cyan-teal accent, hairline borders, flat
   single-accent controls, monospace numerals, glow in dark only.
   Adaptive light + dark via tokens. Every selector is scoped
   under .study-note so nothing leaks to the rest of the site.
   ============================================================ */
.study-note {
  --sn-accent: #0891b2;
  --sn-accent-strong: #06b6d4;
  --sn-accent-soft: rgba(8, 145, 178, 0.10);
  --sn-accent-line: rgba(8, 145, 178, 0.30);
  --sn-on-accent: #ffffff;

  --sn-panel: #ffffff;
  --sn-panel-inset: #f6f8fa;
  --sn-section: #f7f9fb;
  --sn-border: rgba(15, 23, 42, 0.08);
  --sn-grid: rgba(15, 23, 42, 0.10);

  --sn-text: #1f2933;
  --sn-muted: #64748b;

  --sn-good: #0f9d8c;  --sn-good-bg: rgba(15, 157, 140, 0.10);
  --sn-warn: #b07d18;  --sn-warn-bg: rgba(176, 125, 24, 0.10);
  --sn-bad:  #b4544b;  --sn-bad-bg:  rgba(180, 84, 75, 0.10);

  --sn-radius: 12px;
  --sn-radius-sm: 8px;
  --sn-shadow: 0 1px 2px rgba(15, 23, 42, 0.04), 0 4px 16px rgba(15, 23, 42, 0.05);
  --sn-ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --sn-glow: none;
  --sn-mono: ui-monospace, "SF Mono", "JetBrains Mono", "Roboto Mono", Menlo, Consolas, monospace;
}
.dark-theme .study-note {
  --sn-accent: #22d3ee;
  --sn-accent-strong: #67e8f9;
  --sn-accent-soft: rgba(34, 211, 238, 0.12);
  --sn-accent-line: rgba(34, 211, 238, 0.35);
  --sn-on-accent: #06141a;

  --sn-panel: #11131c;
  --sn-panel-inset: #161a26;
  --sn-section: #14161f;
  --sn-border: rgba(148, 163, 184, 0.14);
  --sn-grid: rgba(148, 163, 184, 0.16);

  --sn-text: #e2e8f0;
  --sn-muted: #94a3b8;

  --sn-good: #34d399;  --sn-good-bg: rgba(52, 211, 153, 0.12);
  --sn-warn: #e2b04a;  --sn-warn-bg: rgba(226, 176, 74, 0.12);
  --sn-bad:  #f0857c;  --sn-bad-bg:  rgba(240, 133, 124, 0.12);

  --sn-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
  --sn-glow: 0 0 0 1px var(--sn-accent-line), 0 0 16px rgba(34, 211, 238, 0.18);
}

/* ---- sections / cards ---- */
.study-note .se-section,
.study-note .bm-section,
.study-note .mdp-section {
  margin: 2rem 0;
  padding: 1.5rem;
  border-radius: var(--sn-radius);
  background: var(--sn-section);
  border: 1px solid var(--sn-border);
  box-shadow: var(--sn-shadow);
}
.study-note .se-section h3,
.study-note .bm-section h3,
.study-note .mdp-section h3 {
  margin-top: 0;
  color: var(--sn-accent);
  letter-spacing: -0.01em;
}

/* ---- equation boxes ---- */
.study-note .se-equation,
.study-note .bm-equation,
.study-note .equation-box {
  padding: 1rem 1.5rem;
  margin: 1rem 0;
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  font-size: 1.1rem;
  text-align: center;
  overflow-x: auto;
}
.study-note .se-equation .big,
.study-note .bm-equation .big { font-size: 1.25rem; }

/* ---- callout / note ---- */
.study-note .se-note,
.study-note .bm-note {
  background: var(--sn-accent-soft);
  border-radius: var(--sn-radius-sm);
  border-left: 3px solid var(--sn-accent);
  padding: 0.9rem 1.1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- example box ---- */
.study-note .se-example,
.study-note .bm-example,
.study-note .example-box {
  background: var(--sn-panel-inset);
  border: 1px solid var(--sn-border);
  border-left: 3px solid var(--sn-warn);
  border-radius: var(--sn-radius-sm);
  padding: 1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- chart containers ---- */
.study-note .se-chartwrap,
.study-note .bm-chartwrap,
.study-note .graph-wrap {
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.5rem;
}
.study-note .graph-wrap { overflow-x: auto; }

/* ---- primary buttons ---- */
.study-note .se-btn,
.study-note .bm-btn,
.study-note .vi-btn {
  padding: 0.5rem 1.1rem;
  border: none;
  border-radius: var(--sn-radius-sm);
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-on-accent);
  background: var(--sn-accent);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.2s var(--sn-ease), background 0.2s var(--sn-ease);
}
.study-note .se-btn:hover:not(:disabled),
.study-note .bm-btn:hover:not(:disabled),
.study-note .vi-btn:hover:not(:disabled) {
  background: var(--sn-accent-strong);
  transform: translateY(-1px);
  box-shadow: var(--sn-glow);
}
.study-note .se-btn:disabled,
.study-note .bm-btn:disabled,
.study-note .vi-btn:disabled { opacity: 0.4; cursor: not-allowed; }
/* secondary variant */
.study-note .se-btn.sec,
.study-note .bm-btn.sec,
.study-note .vi-btn.sec {
  background: transparent;
  color: var(--sn-accent);
  box-shadow: inset 0 0 0 1px var(--sn-accent-line);
}
.study-note .se-btn.sec:hover:not(:disabled),
.study-note .bm-btn.sec:hover:not(:disabled),
.study-note .vi-btn.sec:hover:not(:disabled) {
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
}

/* ---- ghost / reset buttons ---- */
.study-note .se-reset,
.study-note .bm-reset,
.study-note .reset-link {
  background: none;
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.4rem 0.85rem;
  font-size: 0.85rem;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-muted);
  transition: background 0.15s var(--sn-ease), border-color 0.15s var(--sn-ease), color 0.15s var(--sn-ease);
}
.study-note .se-reset:hover,
.study-note .bm-reset:hover,
.study-note .reset-link:hover {
  background: var(--sn-accent-soft);
  border-color: var(--sn-accent-line);
  color: var(--sn-accent);
}

/* ---- range sliders: flat, single-accent ---- */
.study-note input[type="range"] {
  -webkit-appearance: none;
  appearance: none;
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
  outline: none;
  cursor: pointer;
}
.study-note input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.15s var(--sn-ease);
}
.study-note input[type="range"]::-webkit-slider-thumb:hover { transform: scale(1.15); }
.study-note input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
}
.study-note input[type="range"]::-moz-range-track {
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
}

/* ---- status pills ---- */
.study-note .se-tag,
.study-note .bm-tag {
  display: inline-block;
  padding: 0.22rem 0.65rem;
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 0.01em;
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
  transition: background 0.2s var(--sn-ease), color 0.2s var(--sn-ease);
}
.study-note .se-tag.good, .study-note .bm-tag.good { background: var(--sn-good-bg); color: var(--sn-good); }
.study-note .se-tag.warn, .study-note .bm-tag.warn { background: var(--sn-warn-bg); color: var(--sn-warn); }
.study-note .se-tag.hot,  .study-note .bm-tag.hot  { background: var(--sn-bad-bg);  color: var(--sn-bad); }

/* ---- numerals: monospace, tabular, accent (size kept per-note) ---- */
.study-note .se-stat .val,
.study-note .bm-stat .val,
.study-note .score-item .sval,
.study-note .compare-score {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  color: var(--sn-accent);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.02em;
}
.study-note .se-readout,
.study-note .bm-temp-readout {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  font-weight: 600;
  color: var(--sn-accent);
}

/* ---- svg text (scoped; replaces the old leaky global rule) ---- */
.study-note svg text { fill: var(--sn-muted); }
</style>


<div class="study-note">

<style>
/* Note-specific layout. Shared components (sections, buttons, sliders,
   tags, equations, notes, charts, numerals) live in study-note-styles.html. */
.se-section h4 { margin-bottom: 0.5rem; }

.se-controls { display: flex; gap: 0.6rem; align-items: center; flex-wrap: wrap; margin: 0.75rem 0; }
.se-readout { font-size: 1.05rem; min-width: 4.5rem; display: inline-block; }

.se-flexwrap { display: flex; gap: 1.25rem; flex-wrap: wrap; align-items: flex-start; }
.se-panel { flex: 1; min-width: 240px; }

.se-stat { text-align: center; }
.se-stat .lab { font-size: 0.68rem; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.55; }
.se-stat .val { font-size: 1.7rem; }
.se-statrow { display: flex; gap: 2rem; justify-content: center; margin: 0.5rem 0; }

.se-legend { font-size: 0.78rem; opacity: 0.75; display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 0.4rem; }
.se-legend span::before { content: "■ "; }

/* surprise meter */
.se-meter { background: var(--sn-panel-inset); border: 1px solid var(--sn-border); border-radius: 8px; height: 26px; overflow: hidden; position: relative; }
.se-meter > div { height: 100%; border-radius: 8px; transition: width 0.12s ease, background 0.12s ease; }

/* distribution sliders */
.se-distrow { display: grid; grid-template-columns: 90px 1fr 56px; gap: 0.6rem; align-items: center; margin: 0.45rem 0; }
.se-distrow .nm { font-size: 0.9rem; }
.se-distrow .pv { font-size: 0.85rem; text-align: right; opacity: 0.75; font-variant-numeric: tabular-nums; }

@keyframes sepop { from { transform: scale(0.6); opacity: 0; } to { transform: scale(1); opacity: 1; } }
.se-pop { animation: sepop 0.25s ease; }
</style>

<p>
  When I first met <strong>Shannon entropy</strong> at university it arrived the way it usually does —
  as a formula, <em>H = −&Sigma; p log p</em>, scribbled on a board, followed by "and this measures
  information." I copied it down, passed the exam, and lost every shred of intuition by the following
  week. This post is the refresher I wish I'd had: built around one idea you can poke, drag, and run.
</p>

<p>Here's the whole story in one sentence, and everything below just unpacks it:</p>

<div class="se-note">
  <strong>Entropy is average surprise.</strong> A rare outcome is surprising and carries a lot of
  information; a near-certain one carries almost none. Entropy is just the <em>expected</em> surprise
  of a random draw — and because we measure surprise in <strong>bits</strong>, it literally counts the
  average number of yes/no questions you'd need to pin down the outcome.
</div>

<!-- ============================================================ -->
<!-- 1. SURPRISE / INFORMATION CONTENT                            -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>😲 The atom: surprise of a single outcome</h3>
  <p style="margin-top:0">
    Before averaging anything, look at one outcome. If an event has probability <em>p</em>, how
    <em>surprised</em> should you be when it happens? Shannon's answer — the only one satisfying a few
    natural rules — is the <strong>information content</strong>:
  </p>
  <div class="se-equation">
    <span class="big">h(x) = log<sub>2</sub> ( 1 / p ) = −log<sub>2</sub> p</span> &nbsp; bits
  </div>
  <p style="margin-top:0">
    Why this shape? A <strong>certain</strong> event (p = 1) tells you nothing new → 0 bits. Halve a
    probability and you add exactly <strong>one bit</strong> of surprise (one extra yes/no question).
    So p = ½ is 1 bit, p = ¼ is 2 bits, p = 1/8 is 3 bits. Drag the probability and watch the surprise:
  </p>

  <div class="se-flexwrap">
    <div class="se-panel" style="flex:2">
      <div class="se-chartwrap">
        <svg id="se1-svg" width="100%" height="240" viewBox="0 0 460 240"></svg>
      </div>
    </div>
    <div class="se-panel">
      <div class="se-controls" style="margin-top:0">
        <span style="font-size:0.85rem">p(x)</span>
        <input type="range" id="se1-p" class="se-slider" min="1" max="1000" value="125" style="flex:1">
        <span class="se-readout" id="se1-pval">0.125</span>
      </div>
      <div class="se-statrow">
        <div class="se-stat"><div class="lab">surprise</div><div class="val" id="se1-bits">3.00</div><div class="lab" style="opacity:0.4">bits</div></div>
      </div>
      <div class="se-meter" style="margin:0.6rem 0"><div id="se1-meter" style="width:30%;background:var(--sn-accent)"></div></div>
      <div id="se1-msg" style="font-size:0.85rem;opacity:0.85;text-align:center;min-height:3.2em"></div>
    </div>
  </div>

  <div class="se-flexwrap" style="margin-top:0.5rem">
    <div class="se-example" style="flex:1;margin:0">
      <strong>"The sun rose today" (p≈1):</strong> ≈ 0 bits. No information — you already knew it.
    </div>
    <div class="se-example" style="flex:1;margin:0">
      <strong>"I flipped 7 heads in a row" (p = 1/128):</strong> 7 bits. Genuinely surprising, and it
      takes 7 yes/no answers to single it out.
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 2. BIASED COIN / BERNOULLI ENTROPY                           -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🪙 Average surprise: the biased coin</h3>
  <p style="margin-top:0">
    Now average. A coin lands heads with probability <em>p</em>, tails with <em>1 − p</em>. Sometimes
    you get the surprising outcome, sometimes the likely one. Weight each surprise by how often it
    happens and you get the <strong>entropy</strong> of the coin:
  </p>
  <div class="se-equation">
    <span class="big">H(p) = −p log<sub>2</sub> p − (1−p) log<sub>2</sub>(1−p)</span> &nbsp; bits
  </div>
  <p style="margin-top:0">
    Drag the bias and watch the curve. It's <strong>maximal at p = ½</strong> — a fair coin is the most
    uncertain, worth a full <strong>1 bit</strong> — and collapses to <strong>0</strong> at either end,
    where the coin is rigged and there's nothing left to be surprised about.
  </p>

  <div class="se-flexwrap">
    <div class="se-panel" style="flex:2">
      <div class="se-chartwrap">
        <svg id="se2-svg" width="100%" height="260" viewBox="0 0 460 260"></svg>
      </div>
    </div>
    <div class="se-panel">
      <div class="se-controls" style="margin-top:0">
        <span style="font-size:0.85rem">p(heads)</span>
        <input type="range" id="se2-p" class="se-slider" min="1" max="999" value="500" style="flex:1">
        <span class="se-readout" id="se2-pval">0.50</span>
      </div>
      <div class="se-statrow">
        <div class="se-stat"><div class="lab">entropy H</div><div class="val" id="se2-h">1.00</div><div class="lab" style="opacity:0.4">bits</div></div>
      </div>
      <div style="text-align:center;margin:0.4rem 0"><span id="se2-tag" class="se-tag warn">maximally uncertain</span></div>
      <div id="se2-msg" style="font-size:0.85rem;opacity:0.85;text-align:center;min-height:3em"></div>
    </div>
  </div>

  <div class="se-note" style="margin-bottom:0">
    <strong>Notice the asymmetry of certainty:</strong> moving from p = 0.5 to 0.6 barely dents the
    entropy, but moving from 0.9 to 0.99 sheds a lot. The last scraps of uncertainty are the cheapest
    to remove — which is exactly why confident predictions are so informative.
  </div>
</div>

<!-- ============================================================ -->
<!-- 3. DISTRIBUTION EXPLORER                                     -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🎛️ More than two outcomes: shape a distribution</h3>
  <p style="margin-top:0">
    Coins are just the two-outcome case. For any distribution over outcomes <em>x</em>, entropy is the
    same average-surprise idea, summed over everything that can happen:
  </p>
  <div class="se-equation">
    <span class="big">H = &Sigma;<sub>x</sub> p(x) · log<sub>2</sub> ( 1 / p(x) ) = −&Sigma;<sub>x</sub> p(x) log<sub>2</sub> p(x)</span>
  </div>
  <p style="margin-top:0">
    Below are tomorrow's weather odds. Drag the sliders to re-shape the distribution (the bars
    renormalise to sum to 1) and watch the entropy meter. Two limits bracket everything:
    <strong>0 bits</strong> when one outcome is certain, and <strong>log<sub>2</sub>N</strong> bits when
    all <em>N</em> outcomes are equally likely — maximum ignorance.
  </p>

  <div class="se-flexwrap">
    <div class="se-panel" style="flex:0 0 300px">
      <div id="se3-sliders"></div>
      <div class="se-controls" style="justify-content:center;margin-top:0.8rem">
        <button class="se-btn" id="se3-uniform">⚖️ Make uniform</button>
        <button class="se-reset" id="se3-certain">🎯 Make certain</button>
      </div>
    </div>
    <div class="se-panel">
      <div class="se-chartwrap">
        <svg id="se3-svg" width="100%" height="220" viewBox="0 0 420 220"></svg>
      </div>
      <div class="se-statrow" style="margin-top:0.6rem">
        <div class="se-stat"><div class="lab">entropy H</div><div class="val" id="se3-h">—</div><div class="lab" style="opacity:0.4">bits</div></div>
        <div class="se-stat"><div class="lab">max (log<sub>2</sub>N)</div><div class="val" id="se3-max" style="color:var(--sn-muted)">—</div><div class="lab" style="opacity:0.4">bits</div></div>
      </div>
      <div style="text-align:center"><span id="se3-tag" class="se-tag warn">—</span></div>
    </div>
  </div>

  <div class="se-flexwrap" style="margin-top:0.5rem">
    <div class="se-example" style="flex:1;margin:0">
      <strong>Make it certain</strong> (push one slider to the top): H → 0. If you already know it'll be
      sunny, a forecast tells you nothing.
    </div>
    <div class="se-example" style="flex:1;margin:0">
      <strong>Make it uniform:</strong> H peaks at log<sub>2</sub>N. With <em>N</em> equally likely
      states, that's the most a forecast could ever tell you.
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 4. SAMPLER / LAW OF LARGE NUMBERS                            -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🎲 "Average surprise" is not a metaphor</h3>
  <p style="margin-top:0">
    Entropy is an <em>expectation</em>, so let's actually take the average. Using the distribution you
    shaped above, we'll draw outcomes one at a time, score each draw by its surprise
    −log<sub>2</sub>p(x), and track the <strong>running average</strong>. By the law of large numbers it
    must home in on the entropy H — the dashed line — no matter where it starts.
  </p>

  <div class="se-controls">
    <button class="se-btn" id="se4-run">▶ Run sampler</button>
    <button class="se-btn sec" id="se4-step">Draw 25</button>
    <button class="se-reset" id="se4-reset">↺ Reset</button>
  </div>

  <div class="se-chartwrap">
    <svg id="se4-svg" width="100%" height="200" viewBox="0 0 720 200" preserveAspectRatio="none" style="display:block"></svg>
  </div>
  <div class="se-legend">
    <span style="color:var(--sn-accent)">running average surprise</span>
    <span style="color:var(--sn-good)">entropy H (target)</span>
  </div>

  <div class="se-statrow" style="margin-top:0.6rem">
    <div class="se-stat"><div class="lab">draws</div><div class="val" id="se4-n">0</div></div>
    <div class="se-stat"><div class="lab">avg surprise</div><div class="val" id="se4-avg">—</div></div>
    <div class="se-stat"><div class="lab">entropy H</div><div class="val" id="se4-h" style="color:var(--sn-good)">—</div></div>
  </div>
  <div class="se-note" style="margin-bottom:0">
    <strong>Try this:</strong> shape a lopsided distribution above, then run the sampler — the average
    surprise still converges onto H, just to a lower value. Then make it uniform and watch it climb to
    log<sub>2</sub>N. The wiggling early on is small-sample noise; it always settles.
  </div>
</div>

<!-- ============================================================ -->
<!-- WRAP UP                                                      -->
<!-- ============================================================ -->
<div class="se-section">
  <h3>🎯 The whole story in one breath</h3>
  <ul style="line-height:1.7">
    <li><strong>Surprise</strong> of one outcome is −log<sub>2</sub>p: rare = many bits, certain = zero.</li>
    <li><strong>Entropy</strong> is the average surprise — the expected bits per draw, <em>H = −&Sigma; p log<sub>2</sub> p</em>.</li>
    <li>It's <strong>maximised by uniformity</strong> (log<sub>2</sub>N bits — total ignorance) and <strong>zero under certainty</strong>.</li>
    <li>Measured in <strong>bits</strong>, it's the average number of optimal yes/no questions — and the floor on lossless compression (Shannon's source coding theorem).</li>
  </ul>
  <p style="margin-bottom:0">
    Same single idea everywhere: <strong>information is resolved uncertainty</strong>. Entropy just
    counts, in bits, how much there was to resolve. Once you see −log p as "surprise," the formula stops
    being a magic incantation and starts being obvious.
  </p>
</div>

<script>
(function () {
  "use strict";
  // theme-aware colors: read CSS tokens, re-read + redraw on light/dark toggle
  const SN = document.querySelector(".study-note");
  const readCols = () => {
    const cs = getComputedStyle(SN);
    const c = n => cs.getPropertyValue(n).trim();
    return { accent: c("--sn-accent"), good: c("--sn-good"), bad: c("--sn-bad"),
             muted: c("--sn-muted"), grid: c("--sn-grid") };
  };
  let COL = readCols();
  const redrawFns = [];
  const onTheme = fn => { redrawFns.push(fn); return fn; };
  new MutationObserver(() => { COL = readCols(); redrawFns.forEach(f => f()); })
    .observe(document.body, { attributes: true, attributeFilter: ["class"] });

  const log2 = x => Math.log(x) / Math.LN2;
  const clamp = (x, a, b) => Math.max(a, Math.min(b, x));
  // surprise of an event, guarded against p<=0
  const surprise = p => (p > 0 ? -log2(p) : 0);
  // entropy of a distribution (array of probabilities)
  const entropy = ps => ps.reduce((h, p) => h + (p > 0 ? -p * log2(p) : 0), 0);

  // ============================================================
  // DEMO 1 — surprise / information content  h = -log2 p
  // ============================================================
  (function () {
    const svg = document.getElementById("se1-svg");
    const pSlider = document.getElementById("se1-p");
    const pval = document.getElementById("se1-pval");
    const bitsEl = document.getElementById("se1-bits");
    const meter = document.getElementById("se1-meter");
    const msg = document.getElementById("se1-msg");

    const W = 460, H = 240, padL = 44, padR = 16, padT = 16, padB = 38;
    const pMin = 0.001, pMax = 1;
    const X = p => padL + (p - pMin) / (pMax - pMin) * (W - padL - padR);
    const hMax = surprise(0.03);                 // ~5 bits, top of plotted range
    const Y = h => H - padB - clamp(h, 0, hMax) / hMax * (H - padT - padB);

    const pOf = () => clamp(pSlider.value / 1000, pMin, 1);

    function draw() {
      const p = pOf();
      const h = surprise(p);
      pval.textContent = p.toFixed(3);
      bitsEl.textContent = h.toFixed(2);
      meter.style.width = clamp(h / hMax * 100, 2, 100).toFixed(1) + "%";
      meter.style.background = h < 1 ? COL.good : h < 3 ? COL.accent : COL.bad;

      // curve h = -log2 p
      let path = "";
      for (let k = 0; k <= 220; k++) {
        const pp = pMin + (pMax - pMin) * k / 220;
        path += (k === 0 ? "M" : "L") + X(pp).toFixed(1) + " " + Y(surprise(pp)).toFixed(1) + " ";
      }
      // gridlines: bits
      let grid = "";
      [0, 1, 2, 3, 4, 5].forEach(b => {
        if (b > hMax) return;
        grid += `<line x1="${padL}" y1="${Y(b)}" x2="${W - padR}" y2="${Y(b)}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${padL - 6}" y="${Y(b) + 3}" font-size="10" text-anchor="end">${b}</text>`;
      });
      [0, 0.25, 0.5, 0.75, 1].forEach(px => {
        grid += `<line x1="${X(px)}" y1="${padT}" x2="${X(px)}" y2="${H - padB}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${X(px)}" y="${H - padB + 16}" font-size="10" text-anchor="middle">${px}</text>`;
      });
      const marker =
        `<line x1="${X(p)}" y1="${Y(h)}" x2="${X(p)}" y2="${H - padB}" stroke="${COL.bad}" stroke-width="1.5" stroke-dasharray="3 3"/>` +
        `<circle cx="${X(p)}" cy="${Y(h)}" r="6" fill="${COL.bad}"/>` +
        `<text x="${clamp(X(p) + 9, padL, W - padR - 60)}" y="${clamp(Y(h) - 6, padT + 12, H - padB)}" font-size="12" fill="${COL.bad}" font-weight="700">${h.toFixed(2)} bits</text>`;
      svg.innerHTML =
        grid +
        `<path d="${path}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>` +
        marker +
        `<text x="${X(0.5)}" y="${H - 4}" font-size="11" text-anchor="middle">probability of the outcome  p(x)</text>` +
        `<text x="13" y="${padT + 84}" font-size="11" transform="rotate(-90 13 ${padT + 84})" text-anchor="middle">surprise (bits)</text>`;

      if (p > 0.9) msg.innerHTML = "Almost certain — barely any surprise. A forecast like this tells you next to nothing.";
      else if (p > 0.45 && p < 0.55) msg.innerHTML = "A coin-flip outcome: <strong>exactly 1 bit</strong> of surprise.";
      else if (p < 0.05) msg.innerHTML = "Rare and shocking — many bits. It'd take this many yes/no questions to single it out.";
      else msg.innerHTML = "Halve the probability and the surprise goes up by exactly one bit.";
    }
    pSlider.addEventListener("input", draw);
    onTheme(draw);
    draw();
  })();

  // ============================================================
  // DEMO 2 — Bernoulli entropy  H(p)
  // ============================================================
  (function () {
    const svg = document.getElementById("se2-svg");
    const pSlider = document.getElementById("se2-p");
    const pval = document.getElementById("se2-pval");
    const hEl = document.getElementById("se2-h");
    const tag = document.getElementById("se2-tag");
    const msg = document.getElementById("se2-msg");

    const W = 460, H = 260, padL = 44, padR = 16, padT = 16, padB = 38;
    const X = p => padL + p * (W - padL - padR);
    const Y = h => H - padB - h * (H - padT - padB);     // H ranges 0..1
    const Hbern = p => entropy([p, 1 - p]);

    const pOf = () => clamp(pSlider.value / 1000, 0.001, 0.999);

    function draw() {
      const p = pOf();
      const h = Hbern(p);
      pval.textContent = p.toFixed(2);
      hEl.textContent = h.toFixed(2);

      let path = "";
      for (let k = 0; k <= 200; k++) {
        const pp = clamp(k / 200, 0.0005, 0.9995);
        path += (k === 0 ? "M" : "L") + X(pp).toFixed(1) + " " + Y(Hbern(pp)).toFixed(1) + " ";
      }
      let grid = "";
      [0, 0.25, 0.5, 0.75, 1].forEach(b => {
        grid += `<line x1="${padL}" y1="${Y(b)}" x2="${W - padR}" y2="${Y(b)}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${padL - 6}" y="${Y(b) + 3}" font-size="10" text-anchor="end">${b.toFixed(2)}</text>`;
      });
      [0, 0.25, 0.5, 0.75, 1].forEach(px => {
        grid += `<line x1="${X(px)}" y1="${padT}" x2="${X(px)}" y2="${H - padB}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${X(px)}" y="${H - padB + 16}" font-size="10" text-anchor="middle">${px}</text>`;
      });
      const peak = `<line x1="${X(0.5)}" y1="${Y(1)}" x2="${X(0.5)}" y2="${H - padB}" stroke="#bbb" stroke-dasharray="4 4"/>` +
        `<text x="${X(0.5)}" y="${Y(1) - 4}" font-size="10" text-anchor="middle" opacity="0.7">1 bit max</text>`;
      const marker =
        `<line x1="${X(p)}" y1="${Y(h)}" x2="${X(p)}" y2="${H - padB}" stroke="${COL.bad}" stroke-width="1.5" stroke-dasharray="3 3"/>` +
        `<circle cx="${X(p)}" cy="${Y(h)}" r="6" fill="${COL.bad}"/>`;
      svg.innerHTML =
        grid + peak +
        `<path d="${path}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>` +
        marker +
        `<text x="${X(0.5)}" y="${H - 4}" font-size="11" text-anchor="middle">p(heads)</text>` +
        `<text x="13" y="${padT + 92}" font-size="11" transform="rotate(-90 13 ${padT + 92})" text-anchor="middle">entropy H (bits)</text>`;

      if (h > 0.95) { tag.textContent = "maximally uncertain"; tag.className = "se-tag warn"; }
      else if (h < 0.2) { tag.textContent = "nearly rigged"; tag.className = "se-tag good"; }
      else { tag.textContent = "biased coin"; tag.className = "se-tag hot"; }

      if (p > 0.45 && p < 0.55) msg.innerHTML = "Fair coin — the most uncertain it can be, worth a full <strong>1 bit</strong>.";
      else if (h < 0.1) msg.innerHTML = "The coin is almost rigged. You can predict it, so there's almost nothing left to learn.";
      else msg.innerHTML = "Bias the coin and entropy falls: outcomes are easier to guess, so each carries fewer bits on average.";
    }
    pSlider.addEventListener("input", draw);
    onTheme(draw);
    draw();
  })();

  // ============================================================
  // SHARED distribution state for demos 3 & 4
  // ============================================================
  const dist = {
    names: ["☀️ Sunny", "⛅ Cloudy", "🌧️ Rain", "⛈️ Storm", "❄️ Snow"],
    colors: ["#0891b2", "#0ea5b8", "#22b8c9", "#67cdd9", "#a5e3ea"],
    raw: [5, 3, 2, 1, 0.6],   // unnormalised slider weights
    probs() { const s = this.raw.reduce((a, b) => a + b, 0); return this.raw.map(w => w / s); },
    listeners: [],
    notify() { this.listeners.forEach(fn => fn()); }
  };

  // ============================================================
  // DEMO 3 — distribution explorer
  // ============================================================
  (function () {
    const N = dist.names.length;
    const slidersEl = document.getElementById("se3-sliders");
    const svg = document.getElementById("se3-svg");
    const hEl = document.getElementById("se3-h");
    const maxEl = document.getElementById("se3-max");
    const tag = document.getElementById("se3-tag");
    const maxH = log2(N);

    // build sliders
    const inputs = [];
    for (let i = 0; i < N; i++) {
      const row = document.createElement("div");
      row.className = "se-distrow";
      const nm = document.createElement("span"); nm.className = "nm"; nm.textContent = dist.names[i];
      const inp = document.createElement("input");
      inp.type = "range"; inp.className = "se-slider"; inp.min = "0"; inp.max = "100";
      inp.value = Math.round(dist.raw[i] / 5 * 100);
      inp.style.flex = "1";
      const pv = document.createElement("span"); pv.className = "pv";
      row.appendChild(nm); row.appendChild(inp); row.appendChild(pv);
      slidersEl.appendChild(row);
      inputs.push({ inp, pv });
      inp.addEventListener("input", () => {
        dist.raw[i] = Math.max(0.0001, inp.value / 100 * 5);
        dist.notify();
      });
    }

    const W = 420, H = 220, padT = 14, padB = 30, padL = 8;
    function draw() {
      const probs = dist.probs();
      const h = entropy(probs);
      hEl.textContent = h.toFixed(2);
      maxEl.textContent = maxH.toFixed(2);
      // sync slider readouts + reflect external changes (uniform/certain buttons)
      for (let i = 0; i < N; i++) {
        inputs[i].pv.textContent = (probs[i] * 100).toFixed(0) + "%";
        const v = Math.round(dist.raw[i] / 5 * 100);
        if (+inputs[i].inp.value !== v) inputs[i].inp.value = v;
      }
      // bars
      const slot = (W - padL) / N, barW = slot * 0.64, top = H - padT - padB;
      let out = "";
      [0.25, 0.5, 0.75, 1].forEach(f => {
        const y = H - padB - f * top;
        out += `<line x1="${padL}" y1="${y}" x2="${W}" y2="${y}" stroke="${COL.grid}" stroke-width="1"/>`;
      });
      probs.forEach((p, i) => {
        const x = padL + i * slot + (slot - barW) / 2;
        const bh = p * top, yTop = H - padB - bh;
        out += `<rect x="${x.toFixed(1)}" y="${yTop.toFixed(1)}" width="${barW.toFixed(1)}" height="${bh.toFixed(1)}" rx="4" fill="${dist.colors[i]}"/>`;
        out += `<text x="${(x + barW / 2).toFixed(1)}" y="${(yTop - 5).toFixed(1)}" font-size="10" text-anchor="middle">${(p * 100).toFixed(0)}%</text>`;
        out += `<text x="${(x + barW / 2).toFixed(1)}" y="${H - padB + 16}" font-size="13" text-anchor="middle">${dist.names[i].split(" ")[0]}</text>`;
      });
      svg.innerHTML = out;

      if (h > maxH - 0.03) { tag.textContent = "uniform → max entropy"; tag.className = "se-tag warn"; }
      else if (h < 0.15) { tag.textContent = "near-certain → ~0 bits"; tag.className = "se-tag good"; }
      else { tag.textContent = (h / maxH * 100).toFixed(0) + "% of maximum"; tag.className = "se-tag hot"; }
    }

    document.getElementById("se3-uniform").addEventListener("click", () => {
      for (let i = 0; i < N; i++) dist.raw[i] = 1;
      dist.notify();
    });
    document.getElementById("se3-certain").addEventListener("click", () => {
      for (let i = 0; i < N; i++) dist.raw[i] = 0.0001;
      dist.raw[0] = 5;
      dist.notify();
    });

    dist.listeners.push(draw);
    onTheme(draw);
    draw();
  })();

  // ============================================================
  // DEMO 4 — sampler: running average surprise -> entropy
  // ============================================================
  (function () {
    const svg = document.getElementById("se4-svg");
    const nEl = document.getElementById("se4-n");
    const avgEl = document.getElementById("se4-avg");
    const hEl = document.getElementById("se4-h");
    const runBtn = document.getElementById("se4-run");

    let n = 0, sumSurprise = 0;
    const hist = [];          // running-average surprise after each draw
    let running = false, raf = null;

    // cumulative distribution for sampling current dist
    function sampleIndex(probs) {
      let r = Math.random(), acc = 0;
      for (let i = 0; i < probs.length; i++) { acc += probs[i]; if (r <= acc) return i; }
      return probs.length - 1;
    }

    function reset() {
      n = 0; sumSurprise = 0; hist.length = 0;
      stop();
      draw();
    }
    function drawOne() {
      const probs = dist.probs();
      const i = sampleIndex(probs);
      sumSurprise += surprise(probs[i]);
      n++;
      hist.push(sumSurprise / n);
      if (hist.length > 2000) hist.shift();
    }

    const W = 720, H = 200, padL = 38, padR = 12, padT = 12, padB = 26;
    function yScaleMax() { return Math.max(log2(dist.names.length), 0.5) * 1.08; }
    function draw() {
      const probs = dist.probs();
      const Htarget = entropy(probs);
      const yMax = yScaleMax();
      const X = k => padL + (hist.length > 1 ? k / (hist.length - 1) : 0) * (W - padL - padR);
      const Y = v => H - padB - clamp(v, 0, yMax) / yMax * (H - padT - padB);

      nEl.textContent = n;
      avgEl.textContent = n > 0 ? hist[hist.length - 1].toFixed(3) : "—";
      hEl.textContent = Htarget.toFixed(3);

      // gridlines
      let grid = "";
      const ticks = [0, 0.5, 1, 1.5, 2, 2.5].filter(t => t <= yMax);
      ticks.forEach(t => {
        grid += `<line x1="${padL}" y1="${Y(t)}" x2="${W - padR}" y2="${Y(t)}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${padL - 6}" y="${Y(t) + 3}" font-size="9" text-anchor="end">${t}</text>`;
      });
      // target entropy line
      const targetLine =
        `<line x1="${padL}" y1="${Y(Htarget)}" x2="${W - padR}" y2="${Y(Htarget)}" stroke="${COL.good}" stroke-width="2" stroke-dasharray="6 4"/>` +
        `<text x="${W - padR}" y="${Y(Htarget) - 5}" font-size="10" text-anchor="end" fill="${COL.good}">H = ${Htarget.toFixed(2)}</text>`;
      // running-average path
      let d = "";
      hist.forEach((v, k) => { d += (k === 0 ? "M" : "L") + X(k).toFixed(1) + " " + Y(v).toFixed(1) + " "; });
      const trace = hist.length ? `<path d="${d}" fill="none" stroke="${COL.accent}" stroke-width="2"/>` : "";
      const dot = hist.length ? `<circle cx="${X(hist.length - 1).toFixed(1)}" cy="${Y(hist[hist.length - 1]).toFixed(1)}" r="3.5" fill="${COL.accent}"/>` : "";
      svg.innerHTML = grid + targetLine + trace + dot +
        `<text x="${padL}" y="${H - 4}" font-size="9" opacity="0.6">draws →</text>`;
    }

    let acc = 0, last = 0;
    function loop(t) {
      if (!running) return;
      if (!last) last = t;
      acc += t - last; last = t;
      while (acc > 18) { drawOne(); acc -= 18; }   // ~ a handful of draws per frame
      draw();
      raf = requestAnimationFrame(loop);
    }
    function start() { running = true; runBtn.textContent = "⏸ Pause"; last = 0; acc = 0; raf = requestAnimationFrame(loop); }
    function stop() { running = false; runBtn.textContent = "▶ Run sampler"; if (raf) cancelAnimationFrame(raf); }

    runBtn.addEventListener("click", () => { running ? stop() : start(); });
    document.getElementById("se4-step").addEventListener("click", () => { for (let i = 0; i < 25; i++) drawOne(); draw(); });
    document.getElementById("se4-reset").addEventListener("click", reset);

    // when the distribution changes, the target line moves; redraw (keeps history for contrast)
    dist.listeners.push(draw);
    onTheme(draw);
    draw();
  })();
})();
</script>

</div>]]></content><author><name></name></author><category term="Information Theory" /><category term="Interactive" /><summary type="html"><![CDATA[Entropy gets introduced as a formula — −Σ p log p — and the intuition evaporates on contact. This is the refresher I wish I'd had: entropy is just average surprise, measured in yes/no questions. Drag a coin's bias, reshape a distribution, and watch a sampler's running surprise converge onto the entropy, live in your browser.]]></summary></entry><entry><title type="html">Boltzmann Machines, Visually: From Hopfield Nets to Stochastic Neurons</title><link href="https://alexdruso.github.io/boltzmann-machines-visually/" rel="alternate" type="text/html" title="Boltzmann Machines, Visually: From Hopfield Nets to Stochastic Neurons" /><published>2026-05-30T00:00:00+00:00</published><updated>2026-05-30T00:00:00+00:00</updated><id>https://alexdruso.github.io/boltzmann-machines-visually</id><content type="html" xml:base="https://alexdruso.github.io/boltzmann-machines-visually/"><![CDATA[<style>
/* ============================================================
   Study Notes — shared design system
   Sleek/futuristic: cyan-teal accent, hairline borders, flat
   single-accent controls, monospace numerals, glow in dark only.
   Adaptive light + dark via tokens. Every selector is scoped
   under .study-note so nothing leaks to the rest of the site.
   ============================================================ */
.study-note {
  --sn-accent: #0891b2;
  --sn-accent-strong: #06b6d4;
  --sn-accent-soft: rgba(8, 145, 178, 0.10);
  --sn-accent-line: rgba(8, 145, 178, 0.30);
  --sn-on-accent: #ffffff;

  --sn-panel: #ffffff;
  --sn-panel-inset: #f6f8fa;
  --sn-section: #f7f9fb;
  --sn-border: rgba(15, 23, 42, 0.08);
  --sn-grid: rgba(15, 23, 42, 0.10);

  --sn-text: #1f2933;
  --sn-muted: #64748b;

  --sn-good: #0f9d8c;  --sn-good-bg: rgba(15, 157, 140, 0.10);
  --sn-warn: #b07d18;  --sn-warn-bg: rgba(176, 125, 24, 0.10);
  --sn-bad:  #b4544b;  --sn-bad-bg:  rgba(180, 84, 75, 0.10);

  --sn-radius: 12px;
  --sn-radius-sm: 8px;
  --sn-shadow: 0 1px 2px rgba(15, 23, 42, 0.04), 0 4px 16px rgba(15, 23, 42, 0.05);
  --sn-ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --sn-glow: none;
  --sn-mono: ui-monospace, "SF Mono", "JetBrains Mono", "Roboto Mono", Menlo, Consolas, monospace;
}
.dark-theme .study-note {
  --sn-accent: #22d3ee;
  --sn-accent-strong: #67e8f9;
  --sn-accent-soft: rgba(34, 211, 238, 0.12);
  --sn-accent-line: rgba(34, 211, 238, 0.35);
  --sn-on-accent: #06141a;

  --sn-panel: #11131c;
  --sn-panel-inset: #161a26;
  --sn-section: #14161f;
  --sn-border: rgba(148, 163, 184, 0.14);
  --sn-grid: rgba(148, 163, 184, 0.16);

  --sn-text: #e2e8f0;
  --sn-muted: #94a3b8;

  --sn-good: #34d399;  --sn-good-bg: rgba(52, 211, 153, 0.12);
  --sn-warn: #e2b04a;  --sn-warn-bg: rgba(226, 176, 74, 0.12);
  --sn-bad:  #f0857c;  --sn-bad-bg:  rgba(240, 133, 124, 0.12);

  --sn-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
  --sn-glow: 0 0 0 1px var(--sn-accent-line), 0 0 16px rgba(34, 211, 238, 0.18);
}

/* ---- sections / cards ---- */
.study-note .se-section,
.study-note .bm-section,
.study-note .mdp-section {
  margin: 2rem 0;
  padding: 1.5rem;
  border-radius: var(--sn-radius);
  background: var(--sn-section);
  border: 1px solid var(--sn-border);
  box-shadow: var(--sn-shadow);
}
.study-note .se-section h3,
.study-note .bm-section h3,
.study-note .mdp-section h3 {
  margin-top: 0;
  color: var(--sn-accent);
  letter-spacing: -0.01em;
}

/* ---- equation boxes ---- */
.study-note .se-equation,
.study-note .bm-equation,
.study-note .equation-box {
  padding: 1rem 1.5rem;
  margin: 1rem 0;
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  font-size: 1.1rem;
  text-align: center;
  overflow-x: auto;
}
.study-note .se-equation .big,
.study-note .bm-equation .big { font-size: 1.25rem; }

/* ---- callout / note ---- */
.study-note .se-note,
.study-note .bm-note {
  background: var(--sn-accent-soft);
  border-radius: var(--sn-radius-sm);
  border-left: 3px solid var(--sn-accent);
  padding: 0.9rem 1.1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- example box ---- */
.study-note .se-example,
.study-note .bm-example,
.study-note .example-box {
  background: var(--sn-panel-inset);
  border: 1px solid var(--sn-border);
  border-left: 3px solid var(--sn-warn);
  border-radius: var(--sn-radius-sm);
  padding: 1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- chart containers ---- */
.study-note .se-chartwrap,
.study-note .bm-chartwrap,
.study-note .graph-wrap {
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.5rem;
}
.study-note .graph-wrap { overflow-x: auto; }

/* ---- primary buttons ---- */
.study-note .se-btn,
.study-note .bm-btn,
.study-note .vi-btn {
  padding: 0.5rem 1.1rem;
  border: none;
  border-radius: var(--sn-radius-sm);
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-on-accent);
  background: var(--sn-accent);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.2s var(--sn-ease), background 0.2s var(--sn-ease);
}
.study-note .se-btn:hover:not(:disabled),
.study-note .bm-btn:hover:not(:disabled),
.study-note .vi-btn:hover:not(:disabled) {
  background: var(--sn-accent-strong);
  transform: translateY(-1px);
  box-shadow: var(--sn-glow);
}
.study-note .se-btn:disabled,
.study-note .bm-btn:disabled,
.study-note .vi-btn:disabled { opacity: 0.4; cursor: not-allowed; }
/* secondary variant */
.study-note .se-btn.sec,
.study-note .bm-btn.sec,
.study-note .vi-btn.sec {
  background: transparent;
  color: var(--sn-accent);
  box-shadow: inset 0 0 0 1px var(--sn-accent-line);
}
.study-note .se-btn.sec:hover:not(:disabled),
.study-note .bm-btn.sec:hover:not(:disabled),
.study-note .vi-btn.sec:hover:not(:disabled) {
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
}

/* ---- ghost / reset buttons ---- */
.study-note .se-reset,
.study-note .bm-reset,
.study-note .reset-link {
  background: none;
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.4rem 0.85rem;
  font-size: 0.85rem;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-muted);
  transition: background 0.15s var(--sn-ease), border-color 0.15s var(--sn-ease), color 0.15s var(--sn-ease);
}
.study-note .se-reset:hover,
.study-note .bm-reset:hover,
.study-note .reset-link:hover {
  background: var(--sn-accent-soft);
  border-color: var(--sn-accent-line);
  color: var(--sn-accent);
}

/* ---- range sliders: flat, single-accent ---- */
.study-note input[type="range"] {
  -webkit-appearance: none;
  appearance: none;
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
  outline: none;
  cursor: pointer;
}
.study-note input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.15s var(--sn-ease);
}
.study-note input[type="range"]::-webkit-slider-thumb:hover { transform: scale(1.15); }
.study-note input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
}
.study-note input[type="range"]::-moz-range-track {
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
}

/* ---- status pills ---- */
.study-note .se-tag,
.study-note .bm-tag {
  display: inline-block;
  padding: 0.22rem 0.65rem;
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 0.01em;
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
  transition: background 0.2s var(--sn-ease), color 0.2s var(--sn-ease);
}
.study-note .se-tag.good, .study-note .bm-tag.good { background: var(--sn-good-bg); color: var(--sn-good); }
.study-note .se-tag.warn, .study-note .bm-tag.warn { background: var(--sn-warn-bg); color: var(--sn-warn); }
.study-note .se-tag.hot,  .study-note .bm-tag.hot  { background: var(--sn-bad-bg);  color: var(--sn-bad); }

/* ---- numerals: monospace, tabular, accent (size kept per-note) ---- */
.study-note .se-stat .val,
.study-note .bm-stat .val,
.study-note .score-item .sval,
.study-note .compare-score {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  color: var(--sn-accent);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.02em;
}
.study-note .se-readout,
.study-note .bm-temp-readout {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  font-weight: 600;
  color: var(--sn-accent);
}

/* ---- svg text (scoped; replaces the old leaky global rule) ---- */
.study-note svg text { fill: var(--sn-muted); }
</style>


<div class="study-note">

<style>
/* Note-specific layout. Shared components (sections, buttons, sliders,
   tags, equations, notes, charts, numerals) live in study-note-styles.html. */
.bm-section h4 { margin-bottom: 0.5rem; }

.bm-controls { display: flex; gap: 0.6rem; align-items: center; flex-wrap: wrap; margin: 0.75rem 0; }
.bm-temp-readout { font-size: 1.05rem; min-width: 4.5rem; display: inline-block; }

/* ===== grids of binary units ===== */
.bm-grid { display: grid; gap: 4px; }
.bm-cell {
  border-radius: 6px;
  border: 1px solid var(--sn-border);
  background: var(--sn-panel-inset);
  cursor: pointer;
  transition: background 0.18s var(--sn-ease), border-color 0.18s, transform 0.1s;
  padding: 0;
}
.bm-cell.on { background: var(--sn-accent); border-color: var(--sn-accent); }
.bm-cell:active { transform: scale(0.93); }
.bm-cell.flash { box-shadow: 0 0 0 3px var(--sn-accent-line); }

.bm-thumb { display: grid; gap: 2px; }
.bm-thumb .tc { border-radius: 2px; background: var(--sn-panel-inset); }
.bm-thumb .tc.on { background: var(--sn-accent); }

.bm-flexwrap { display: flex; gap: 1.25rem; flex-wrap: wrap; align-items: flex-start; }
.bm-panel { flex: 1; min-width: 240px; }

.bm-stat { text-align: center; }
.bm-stat .lab { font-size: 0.68rem; text-transform: uppercase; letter-spacing: 0.05em; opacity: 0.55; }
.bm-stat .val { font-size: 1.7rem; }
.bm-statrow { display: flex; gap: 2rem; justify-content: center; margin: 0.5rem 0; }

.bm-segbtns { display: inline-flex; border-radius: var(--sn-radius-sm); overflow: hidden; border: 1px solid var(--sn-accent-line); }
.bm-segbtns button {
  background: transparent; border: none; padding: 0.45rem 0.9rem; cursor: pointer;
  font-family: inherit; font-size: 0.85rem; font-weight: 600; color: var(--sn-accent);
  transition: background 0.15s var(--sn-ease), color 0.15s var(--sn-ease);
}
.bm-segbtns button.active { background: var(--sn-accent); color: var(--sn-on-accent); }

.bm-legend { font-size: 0.78rem; opacity: 0.75; display: flex; gap: 1rem; flex-wrap: wrap; margin-top: 0.4rem; }
.bm-legend span::before { content: "■ "; }

@keyframes bmpop { from { transform: scale(0.6); opacity: 0; } to { transform: scale(1); opacity: 1; } }
.bm-pop { animation: bmpop 0.25s ease; }
</style>

<p>
  When I first met Boltzmann machines at university, they arrived as a footnote to <strong>Hopfield networks</strong>: "now make the neurons flip a coin instead of following the rule deterministically." I nodded, passed the exam, and promptly lost the intuition. This post is the refresher I wish I'd had — built around things you can poke, drag, and run.
</p>

<p>
  Here's the one-sentence story, and everything below just unpacks it:
</p>

<div class="bm-note">
  A Hopfield network rolls <em>downhill</em> on an energy landscape until it gets stuck in the nearest valley. A <strong>Boltzmann machine</strong> adds a "temperature" that lets neurons occasionally roll <em>uphill</em> — so instead of freezing in the first local minimum, the network <em>samples</em> states with probability set by their energy. Cool the temperature slowly and it settles into deep valleys; learn the energy landscape from data and it becomes a generative model.
</div>

<!-- ============================================================ -->
<!-- 1. RECAP: HOPFIELD / ENERGY                                  -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>⛰️ The setup you already know: energy</h3>
  <p style="margin-top:0">
    Take <em>N</em> units, each either <strong>on (+1)</strong> or <strong>off (−1)</strong>. Connect them symmetrically with weights <em>w<sub>ij</sub></em> (and give each a bias <em>b<sub>i</sub></em>). The whole configuration <strong>s</strong> has a single scalar <strong>energy</strong>:
  </p>
  <div class="bm-equation">
    <span class="big">E(<strong>s</strong>) = −&Sigma;<sub>i&lt;j</sub> w<sub>ij</sub> s<sub>i</sub> s<sub>j</sub> &nbsp;−&nbsp; &Sigma;<sub>i</sub> b<sub>i</sub> s<sub>i</sub></span>
  </div>
  <p>
    Two units that "want" to agree have <em>w<sub>ij</sub> &gt; 0</em>: matching signs lower the energy. The whole point of an associative memory is to <strong>carve valleys</strong> into this landscape at the patterns we want to remember. The classic <strong>Hebbian rule</strong> — <em>w<sub>ij</sub> = &Sigma;<sub>patterns</sub> &xi;<sub>i</sub> &xi;<sub>j</sub></em> — does exactly that: each stored pattern <strong>&xi;</strong> becomes a local minimum.
  </p>
  <p>
    In a Hopfield network, recall is deterministic: repeatedly set each unit to whatever sign lowers the energy. You slide downhill and stop. That's great — until "downhill" dumps you into a shallow <em>spurious</em> valley that isn't any real memory, with no way back out. Hold that thought.
  </p>
</div>

<!-- ============================================================ -->
<!-- 2. CENTERPIECE: STOCHASTIC NEURONS + TEMPERATURE             -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>🌡️ The one new ingredient: temperature</h3>
  <p style="margin-top:0">
    Below is a tiny network of <strong>9 units</strong> drawn as a 3×3 grid, with two patterns stored by the Hebbian rule: a <strong>T</strong> and an <strong>L</strong>. Instead of flipping units deterministically, each update is a coin flip biased by the energy:
  </p>
  <div class="bm-equation">
    <span class="big">p(s<sub>i</sub> = +1) = &sigma;( &Delta;E<sub>i</sub> / T )</span>
    &nbsp;&nbsp;where&nbsp; &Delta;E<sub>i</sub> = 2(&Sigma;<sub>j</sub> w<sub>ij</sub> s<sub>j</sub> + b<sub>i</sub>),&nbsp; &sigma;(x)=1/(1+e<sup>−x</sup>)
  </div>
  <p style="margin-top:0">
    &Delta;E<sub>i</sub> is simply how much energy the network <em>saves</em> by switching unit <em>i</em> on. Drag the temperature and watch the dynamics change completely:
  </p>

  <div class="bm-flexwrap">
    <div class="bm-panel" style="flex:0 0 auto">
      <div id="bmA-grid" class="bm-grid" style="grid-template-columns:repeat(3,56px);grid-template-rows:repeat(3,56px);margin:0 auto"></div>
      <div style="text-align:center;margin-top:0.6rem">
        <span id="bmA-status" class="bm-tag warn">exploring</span>
      </div>
      <div style="display:flex;gap:0.75rem;justify-content:center;margin-top:0.75rem;align-items:center">
        <div style="text-align:center">
          <div style="font-size:0.7rem;opacity:0.6">stored: T</div>
          <div id="bmA-thT" class="bm-thumb" style="grid-template-columns:repeat(3,12px);grid-template-rows:repeat(3,12px)"></div>
        </div>
        <div style="text-align:center">
          <div style="font-size:0.7rem;opacity:0.6">stored: L</div>
          <div id="bmA-thL" class="bm-thumb" style="grid-template-columns:repeat(3,12px);grid-template-rows:repeat(3,12px)"></div>
        </div>
      </div>
    </div>

    <div class="bm-panel">
      <div class="bm-controls" style="margin-top:0">
        <span style="font-size:0.85rem">🌡️ Temperature</span>
        <input type="range" id="bmA-temp" class="bm-slider" min="2" max="400" value="20" style="flex:1;min-width:120px">
        <span class="bm-temp-readout" id="bmA-tval">0.20</span>
      </div>
      <div class="bm-controls">
        <button class="bm-btn" id="bmA-run">▶ Run</button>
        <button class="bm-btn sec" id="bmA-step">Step (1 sweep)</button>
        <button class="bm-reset" id="bmA-corrupt">✦ Corrupt a memory</button>
        <button class="bm-reset" id="bmA-rand">🎲 Randomize</button>
      </div>
      <div class="bm-statrow">
        <div class="bm-stat"><div class="lab">Energy</div><div class="val" id="bmA-energy">0.0</div></div>
        <div class="bm-stat"><div class="lab">Updates</div><div class="val" id="bmA-count">0</div></div>
      </div>
      <div class="bm-chartwrap">
        <svg id="bmA-chart" width="100%" height="120" viewBox="0 0 400 120" preserveAspectRatio="none" style="display:block"></svg>
      </div>
      <div class="bm-legend"><span style="color:var(--sn-accent)">energy over time</span></div>
    </div>
  </div>

  <div class="bm-note" style="margin-bottom:0">
    <strong>Try this:</strong> set temperature very low (slide left) and hit <em>Run</em> — the network freezes into T or L, exactly like Hopfield recall. Now crank it high: the grid <em>boils</em>, energy stays high, nothing settles. The interesting regime is in between. Hit <em>Corrupt a memory</em> at low temperature to watch <strong>pattern completion</strong> happen live.
  </div>
</div>

<!-- ============================================================ -->
<!-- 3. THE SIGMOID RULE                                          -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>🎲 Why a sigmoid? Temperature interpolates between a thermostat and a coin</h3>
  <p style="margin-top:0">
    The update rule is the whole personality of the network, so it's worth staring at. The probability a unit turns on is a sigmoid of <em>(energy saved) / temperature</em>. The plot shows that curve; drag temperature to reshape it.
  </p>

  <div class="bm-flexwrap">
    <div class="bm-panel" style="flex:2">
      <div class="bm-chartwrap">
        <svg id="bmB-svg" width="100%" height="260" viewBox="0 0 460 260"></svg>
      </div>
    </div>
    <div class="bm-panel">
      <div class="bm-controls" style="margin-top:0">
        <span style="font-size:0.85rem">🌡️ T</span>
        <input type="range" id="bmB-temp" class="bm-slider" min="3" max="600" value="100" style="flex:1">
        <span class="bm-temp-readout" id="bmB-tval">1.00</span>
      </div>
      <div class="bm-controls">
        <span style="font-size:0.85rem">&Delta;E</span>
        <input type="range" id="bmB-de" class="bm-slider" min="-60" max="60" value="20" style="flex:1">
        <span class="bm-temp-readout" id="bmB-deval">+2.0</span>
      </div>
      <div class="bm-statrow">
        <div class="bm-stat"><div class="lab">p(turn on)</div><div class="val" id="bmB-prob">0.88</div></div>
      </div>
      <div id="bmB-msg" style="font-size:0.85rem;opacity:0.8;text-align:center;min-height:2.4em"></div>
    </div>
  </div>

  <div class="bm-flexwrap" style="margin-top:1rem">
    <div class="bm-example" style="flex:1;margin:0">
      <strong>T → 0 (cold):</strong> the sigmoid becomes a step. p≈1 whenever turning on lowers energy, p≈0 otherwise. This is exactly a Hopfield update — a deterministic thermostat. Great for locking in, hopeless for escaping.
    </div>
    <div class="bm-example" style="flex:1;margin:0">
      <strong>T → ∞ (hot):</strong> the sigmoid flattens to 0.5 everywhere. The unit ignores the energy entirely and flips a fair coin. Maximum exploration, zero memory.
    </div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 4. ENERGY -> PROBABILITY (Boltzmann distribution)            -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>📊 Energy becomes probability: the Boltzmann distribution</h3>
  <p style="margin-top:0">
    Here's the payoff that gives the machine its name. If you let those stochastic units run forever, the network doesn't converge to a single state — it visits every configuration <strong>s</strong> with a probability that depends only on its energy:
  </p>
  <div class="bm-equation">
    <span class="big">p(<strong>s</strong>) = e<sup>−E(<strong>s</strong>)/T</sup> / Z</span>,
    &nbsp;&nbsp; Z = &Sigma;<sub><strong>s'</strong></sub> e<sup>−E(<strong>s'</strong>)/T</sup>
  </div>
  <p style="margin-top:0">
    Low energy → high probability. The network spends most of its time in the deepest valleys. To <em>see</em> this, here's a small 4-unit network — only <strong>2<sup>4</sup> = 16</strong> possible states, so we can plot the entire distribution at once. Each bar is one full configuration (shown as 4 cells); height is its probability. Drag temperature and watch the distribution morph.
  </p>

  <div class="bm-controls">
    <span style="font-size:0.85rem">🌡️ Temperature</span>
    <input type="range" id="bmC-temp" class="bm-slider" min="5" max="500" value="100" style="flex:1;max-width:340px">
    <span class="bm-temp-readout" id="bmC-tval">1.00</span>
  </div>
  <div class="bm-chartwrap">
    <svg id="bmC-svg" width="100%" height="240" viewBox="0 0 720 240"></svg>
  </div>
  <div class="bm-legend">
    <span style="color:var(--sn-accent)">low energy (likely)</span>
    <span style="color:var(--sn-bad)">high energy (rare)</span>
  </div>

  <div class="bm-flexwrap" style="margin-top:1rem">
    <div class="bm-example" style="flex:1;margin:0">
      <strong>Cold:</strong> almost all probability mass piles onto the single lowest-energy state. The machine is effectively a deterministic memory — it "knows the answer."
    </div>
    <div class="bm-example" style="flex:1;margin:0">
      <strong>Hot:</strong> the bars flatten toward a uniform distribution — every state is roughly equally likely. The machine has "forgotten" its structure.
    </div>
  </div>
  <p>
    This is the deep idea: <strong>shaping the weights shapes the energy, which shapes a probability distribution over states.</strong> Learning a Boltzmann machine means molding that distribution to match your data.
  </p>
</div>

<!-- ============================================================ -->
<!-- 5. SIMULATED ANNEALING                                       -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>❄️ Simulated annealing: start hot, cool slowly, land deep</h3>
  <p style="margin-top:0">
    Now the practical magic. To find a <em>deep</em> valley (a real memory, not a spurious one), don't go straight to T≈0 — you'll freeze wherever you happen to be. Instead, start <strong>hot</strong> so the network roams freely over the whole landscape, then <strong>cool gradually</strong> so it settles into the deepest basin it found. This is <em>simulated annealing</em>, borrowed straight from metallurgy.
  </p>
  <p style="margin-top:0">Pick a strategy, then <em>Run</em> a single descent and watch the energy trace. Then race 200 runs of each to see who reliably reaches a stored memory.</p>

  <div class="bm-controls">
    <div class="bm-segbtns" id="bmD-mode">
      <button data-mode="greedy">Greedy (cold)</button>
      <button data-mode="hot">Always hot</button>
      <button data-mode="anneal" class="active">Annealed ❄️</button>
    </div>
    <button class="bm-btn" id="bmD-run">▶ Run once</button>
    <button class="bm-btn sec" id="bmD-race">🏁 Race 200 runs each</button>
  </div>

  <div class="bm-flexwrap">
    <div class="bm-panel" style="flex:0 0 auto;text-align:center">
      <div id="bmD-grid" class="bm-grid" style="grid-template-columns:repeat(3,46px);grid-template-rows:repeat(3,46px);margin:0 auto"></div>
      <div style="margin-top:0.5rem"><span id="bmD-result" class="bm-tag warn">—</span></div>
    </div>
    <div class="bm-panel">
      <div class="bm-chartwrap">
        <svg id="bmD-svg" width="100%" height="180" viewBox="0 0 420 180"></svg>
      </div>
      <div class="bm-legend">
        <span style="color:var(--sn-accent)">energy</span>
        <span style="color:var(--sn-warn)">temperature</span>
      </div>
    </div>
  </div>

  <div id="bmD-race-wrap" style="display:none;margin-top:1rem">
    <h4>🏁 Out of 200 runs from random starts, how often did each reach a stored memory?</h4>
    <div id="bmD-race-bars"></div>
  </div>
</div>

<!-- ============================================================ -->
<!-- 6. LEARNING                                                  -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>🧠 Learning: two phases, one elegant rule</h3>
  <p style="margin-top:0">
    So far we <em>hand-built</em> the valleys with the Hebbian rule. The real question is: can the network <strong>learn</strong> its own energy landscape so that its Boltzmann distribution matches a dataset? Yes — and the rule is beautifully symmetric. To make data likely, nudge the weights by the difference between two correlation measurements:
  </p>
  <div class="bm-equation">
    <span class="big">&Delta;w<sub>ij</sub> = &epsilon;&nbsp;( &nbsp;&langle; s<sub>i</sub> s<sub>j</sub> &rangle;<sub>data</sub> &nbsp;−&nbsp; &langle; s<sub>i</sub> s<sub>j</sub> &rangle;<sub>model</sub>&nbsp; )</span>
  </div>
  <div class="bm-flexwrap">
    <div class="bm-example" style="flex:1;margin:0">
      <strong>Positive phase ("wake"):</strong> clamp the visible units to a real data example and measure how often unit <em>i</em> and <em>j</em> fire together. This <em>digs valleys under the data</em>.
    </div>
    <div class="bm-example" style="flex:1;margin:0">
      <strong>Negative phase ("sleep"):</strong> let the network run free — dreaming — and measure the same correlation under its own distribution. This <em>fills in valleys the model invented</em> on its own.
    </div>
  </div>
  <p>
    When the two correlations match, the model's distribution equals the data's and learning stops. The catch: that negative-phase expectation requires sampling from the full network, which is exponentially expensive. That cost is exactly what the <strong>Restricted</strong> Boltzmann machine was invented to dodge.
  </p>
</div>

<!-- ============================================================ -->
<!-- 7. RBM                                                       -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>🔗 Restricted Boltzmann Machines: make the sampling cheap</h3>
  <p style="margin-top:0">
    The trick is structural. Split the units into a <strong>visible</strong> layer (the data) and a <strong>hidden</strong> layer (learned features), and <em>forbid</em> connections within a layer. The graph becomes bipartite:
  </p>
  <div class="bm-chartwrap" style="margin-bottom:1rem">
    <svg id="bmE-arch" width="100%" height="170" viewBox="0 0 460 170"></svg>
  </div>
  <p style="margin-top:0">
    Because no two hidden units talk to each other, <strong>given the visible layer every hidden unit is independent</strong> — you can sample the whole hidden layer in one shot, and vice versa. That makes a single round-trip <strong>v → h → v′</strong> (the heart of <em>Contrastive Divergence</em>) trivially fast, and it's all you need for a good gradient estimate.
  </p>
  <p style="margin-top:0">
    Below, an RBM with 9 visible units (a 3×3 image) and 4 hidden units learns three little shapes — <strong>T, L, and a square-ish O</strong> — via CD-1, live in your browser. Watch reconstruction error drop and the hidden units grow into feature detectors.
    <em style="opacity:0.7">(RBMs conventionally use 0/1 units, so this section switches from ±1 to {0,1}.)</em>
  </p>

  <div class="bm-controls">
    <button class="bm-btn" id="bmE-train">🚀 Train (CD-1)</button>
    <button class="bm-reset" id="bmE-resetw">↺ Reset weights</button>
    <span id="bmE-epoch" style="font-size:0.85rem;opacity:0.75"></span>
  </div>

  <div class="bm-flexwrap">
    <div class="bm-panel">
      <h4 style="margin-top:0">Reconstruction error</h4>
      <div class="bm-chartwrap">
        <svg id="bmE-err" width="100%" height="150" viewBox="0 0 400 150"></svg>
      </div>
    </div>
    <div class="bm-panel">
      <h4 style="margin-top:0">Hidden-unit receptive fields</h4>
      <div id="bmE-fields" style="display:flex;gap:0.8rem;flex-wrap:wrap"></div>
      <div class="bm-legend">
        <span style="color:var(--sn-bad)">negative weight</span>
        <span style="color:var(--sn-accent)">positive weight</span>
      </div>
    </div>
  </div>

  <h4>Reconstruct a pattern: v → h → v′</h4>
  <p style="margin-top:0;font-size:0.92rem">
    Click cells to draw on the visible layer (or load a corrupted shape), then push it once through the hidden layer and back. A trained RBM <strong>cleans up the input</strong>, snapping it toward a shape it knows.
  </p>
  <div class="bm-flexwrap" style="align-items:center;justify-content:center">
    <div style="text-align:center">
      <div style="font-size:0.75rem;opacity:0.6;margin-bottom:0.3rem">visible (click to edit)</div>
      <div id="bmE-vis" class="bm-grid" style="grid-template-columns:repeat(3,46px);grid-template-rows:repeat(3,46px)"></div>
    </div>
    <div style="text-align:center">
      <div style="font-size:0.75rem;opacity:0.6;margin-bottom:0.3rem">hidden activations</div>
      <div id="bmE-hid" style="display:flex;flex-direction:column;gap:6px"></div>
    </div>
    <div style="text-align:center">
      <div style="font-size:0.75rem;opacity:0.6;margin-bottom:0.3rem">reconstruction v′</div>
      <div id="bmE-rec" class="bm-grid" style="grid-template-columns:repeat(3,46px);grid-template-rows:repeat(3,46px)"></div>
    </div>
  </div>
  <div class="bm-controls" style="justify-content:center">
    <button class="bm-btn" id="bmE-recon">↻ v → h → v′</button>
    <button class="bm-reset" id="bmE-corrupt">✦ Load corrupted T</button>
    <button class="bm-reset" id="bmE-clear">Clear</button>
  </div>
</div>

<!-- ============================================================ -->
<!-- WRAP UP                                                      -->
<!-- ============================================================ -->
<div class="bm-section">
  <h3>🎯 The whole story in one breath</h3>
  <ul style="line-height:1.7">
    <li><strong>Energy</strong> turns a network's weights into a landscape; stored patterns are valleys.</li>
    <li><strong>Temperature + a sigmoid</strong> turn deterministic descent (Hopfield) into stochastic sampling.</li>
    <li>Run long enough and states appear with the <strong>Boltzmann probability</strong> <em>e<sup>−E/T</sup>/Z</em>.</li>
    <li><strong>Annealing</strong> — hot then cold — escapes spurious minima and lands in deep ones.</li>
    <li><strong>Learning</strong> matches data correlations (wake) to model correlations (sleep).</li>
    <li><strong>RBMs</strong> restrict the graph to a bipartite one, making that sampling fast — the ancestor of deep belief nets and a cornerstone of the early deep-learning revival.</li>
  </ul>
  <p style="margin-bottom:0">
    Same energy idea you met behind Hopfield networks — but once you let the neurons gamble, a deterministic memory becomes a generative model. That's the Boltzmann machine.
  </p>
</div>

<script>
(function () {
  "use strict";
  // theme-aware colors: read CSS tokens, re-read + redraw on light/dark toggle
  const SN = document.querySelector(".study-note");
  const hex2rgb = h => { h = h.replace("#", ""); if (h.length === 3) h = h.split("").map(x => x + x).join(""); return [parseInt(h.slice(0, 2), 16), parseInt(h.slice(2, 4), 16), parseInt(h.slice(4, 6), 16)]; };
  const mix = (a, b, t) => { const A = hex2rgb(a), B = hex2rgb(b); return `rgb(${A.map((v, i) => Math.round(v + (B[i] - v) * t)).join(",")})`; };
  const readCols = () => {
    const cs = getComputedStyle(SN);
    const c = n => cs.getPropertyValue(n).trim();
    return { accent: c("--sn-accent"), good: c("--sn-good"), bad: c("--sn-bad"),
             warn: c("--sn-warn"), muted: c("--sn-muted"), grid: c("--sn-grid"),
             inset: c("--sn-panel-inset"), line: c("--sn-accent-line") };
  };
  let COL = readCols();
  const redrawFns = [];
  const onTheme = fn => { redrawFns.push(fn); return fn; };
  new MutationObserver(() => { COL = readCols(); redrawFns.forEach(f => f()); })
    .observe(document.body, { attributes: true, attributeFilter: ["class"] });

  const sigmoid = x => 1 / (1 + Math.exp(-x));
  const clamp = (x, a, b) => Math.max(a, Math.min(b, x));

  // Patterns on a 3x3 grid, in {-1,+1}
  const mk = a => a.map(v => (v ? 1 : -1));
  const PAT_T = mk([1,1,1, 0,1,0, 0,1,0]);
  const PAT_L = mk([1,0,0, 1,0,0, 1,1,1]);
  const N = 9;

  // Hebbian weights from the two stored patterns
  function buildHebbian(patterns) {
    const W = Array.from({length: N}, () => new Array(N).fill(0));
    const b = new Array(N).fill(0);
    for (const p of patterns) {
      for (let i = 0; i < N; i++) {
        b[i] += p[i];
        for (let j = 0; j < N; j++) if (i !== j) W[i][j] += p[i] * p[j];
      }
    }
    for (let i = 0; i < N; i++) { b[i] /= N; for (let j = 0; j < N; j++) W[i][j] /= N; }
    return { W, b };
  }
  const HEB = buildHebbian([PAT_T, PAT_L]);

  function energy(s, W, b) {
    let e = 0;
    for (let i = 0; i < N; i++) {
      e -= b[i] * s[i];
      for (let j = i + 1; j < N; j++) e -= W[i][j] * s[i] * s[j];
    }
    return e;
  }
  function field(s, i, W, b) {
    let h = b[i];
    for (let j = 0; j < N; j++) if (j !== i) h += W[i][j] * s[j];
    return h;
  }
  function samePattern(s, p) { for (let i = 0; i < N; i++) if (s[i] !== p[i]) return false; return true; }

  function makeGrid(el, n, cellClass) {
    const cells = [];
    el.innerHTML = "";
    for (let i = 0; i < n; i++) {
      const c = document.createElement("button");
      c.className = cellClass || "bm-cell";
      c.type = "button";
      el.appendChild(c);
      cells.push(c);
    }
    return cells;
  }
  function paintThumb(el, pat) {
    el.innerHTML = "";
    for (let i = 0; i < N; i++) {
      const c = document.createElement("div");
      c.className = "tc" + (pat[i] === 1 ? " on" : "");
      el.appendChild(c);
    }
  }

  // ============================================================
  // DEMO A — stochastic neurons + temperature
  // ============================================================
  (function () {
    const gridEl = document.getElementById("bmA-grid");
    const cells = makeGrid(gridEl, N);
    paintThumb(document.getElementById("bmA-thT"), PAT_T);
    paintThumb(document.getElementById("bmA-thL"), PAT_L);

    let s = PAT_T.slice();
    let count = 0;
    let running = false, raf = null;
    const hist = [];
    const tempSlider = document.getElementById("bmA-temp");
    const tval = document.getElementById("bmA-tval");

    const tempOf = () => tempSlider.value / 100;

    function paint() {
      for (let i = 0; i < N; i++) cells[i].classList.toggle("on", s[i] === 1);
      const e = energy(s, HEB.W, HEB.b);
      document.getElementById("bmA-energy").textContent = e.toFixed(2);
      document.getElementById("bmA-count").textContent = count;
      const status = document.getElementById("bmA-status");
      if (samePattern(s, PAT_T)) { status.textContent = "locked: memory T ✓"; status.className = "bm-tag good"; }
      else if (samePattern(s, PAT_L)) { status.textContent = "locked: memory L ✓"; status.className = "bm-tag good"; }
      else if (tempOf() > 1.5) { status.textContent = "boiling 🔥"; status.className = "bm-tag hot"; }
      else { status.textContent = "exploring"; status.className = "bm-tag warn"; }
      drawChart();
    }

    function drawChart() {
      const svg = document.getElementById("bmA-chart");
      const W_ = 400, H_ = 120, pad = 6;
      const lo = -5.2, hi = 3.2; // energy range
      const xs = hist.length > 1 ? (W_ - 2 * pad) / (hist.length - 1) : 0;
      const y = e => H_ - pad - ((e - lo) / (hi - lo)) * (H_ - 2 * pad);
      let d = "";
      hist.forEach((e, k) => { d += (k === 0 ? "M" : "L") + (pad + k * xs).toFixed(1) + " " + y(e).toFixed(1) + " "; });
      const zeroY = y(0);
      svg.innerHTML =
        `<line x1="${pad}" y1="${zeroY}" x2="${W_-pad}" y2="${zeroY}" stroke="${COL.grid}" stroke-dasharray="3 3"/>` +
        `<text x="${pad+2}" y="${zeroY-3}" font-size="9" opacity="0.6">E=0</text>` +
        `<path d="${d}" fill="none" stroke="${COL.accent}" stroke-width="2"/>`;
    }

    function pushHist(e) { hist.push(e); if (hist.length > 120) hist.shift(); }

    function asyncUpdate() {
      // one random unit (Glauber dynamics)
      const i = (Math.random() * N) | 0;
      const dE = 2 * field(s, i, HEB.W, HEB.b);
      s[i] = Math.random() < sigmoid(dE / tempOf()) ? 1 : -1;
      count++;
      pushHist(energy(s, HEB.W, HEB.b));
    }
    function sweep() {
      const order = [...Array(N).keys()].sort(() => Math.random() - 0.5);
      for (const i of order) {
        const dE = 2 * field(s, i, HEB.W, HEB.b);
        s[i] = Math.random() < sigmoid(dE / tempOf()) ? 1 : -1;
        count++;
      }
      pushHist(energy(s, HEB.W, HEB.b));
    }

    let acc = 0, last = 0;
    function loop(t) {
      if (!running) return;
      if (!last) last = t;
      acc += t - last; last = t;
      // throttle ~ updates so the chart is readable
      while (acc > 40) { asyncUpdate(); acc -= 40; }
      paint();
      raf = requestAnimationFrame(loop);
    }

    const runBtn = document.getElementById("bmA-run");
    runBtn.addEventListener("click", () => {
      running = !running;
      runBtn.textContent = running ? "⏸ Pause" : "▶ Run";
      if (running) { last = 0; acc = 0; raf = requestAnimationFrame(loop); }
      else if (raf) cancelAnimationFrame(raf);
    });
    document.getElementById("bmA-step").addEventListener("click", () => { sweep(); paint(); });
    document.getElementById("bmA-rand").addEventListener("click", () => {
      s = Array.from({length: N}, () => (Math.random() < 0.5 ? 1 : -1));
      hist.length = 0; pushHist(energy(s, HEB.W, HEB.b)); paint();
    });
    document.getElementById("bmA-corrupt").addEventListener("click", () => {
      const base = Math.random() < 0.5 ? PAT_T : PAT_L;
      s = base.slice();
      const flips = new Set();
      while (flips.size < 3) flips.add((Math.random() * N) | 0);
      flips.forEach(i => { s[i] = -s[i]; cells[i].classList.add("flash"); setTimeout(() => cells[i].classList.remove("flash"), 600); });
      hist.length = 0; pushHist(energy(s, HEB.W, HEB.b)); paint();
    });
    cells.forEach((c, i) => c.addEventListener("click", () => { s[i] = -s[i]; pushHist(energy(s, HEB.W, HEB.b)); paint(); }));
    tempSlider.addEventListener("input", () => { tval.textContent = tempOf().toFixed(2); paint(); });

    tval.textContent = tempOf().toFixed(2);
    pushHist(energy(s, HEB.W, HEB.b));
    onTheme(paint);
    paint();
  })();

  // ============================================================
  // DEMO B — the sigmoid rule
  // ============================================================
  (function () {
    const svg = document.getElementById("bmB-svg");
    const tempSlider = document.getElementById("bmB-temp");
    const deSlider = document.getElementById("bmB-de");
    const tval = document.getElementById("bmB-tval");
    const deval = document.getElementById("bmB-deval");
    const probEl = document.getElementById("bmB-prob");
    const msgEl = document.getElementById("bmB-msg");

    const W_ = 460, H_ = 260, padL = 44, padR = 16, padT = 16, padB = 36;
    const xMin = -6, xMax = 6;
    const X = x => padL + (x - xMin) / (xMax - xMin) * (W_ - padL - padR);
    const Y = p => H_ - padB - p * (H_ - padT - padB);

    function draw() {
      const T = tempSlider.value / 100;
      const dE = deSlider.value / 10;
      tval.textContent = T.toFixed(2);
      deval.textContent = (dE >= 0 ? "+" : "") + dE.toFixed(1);
      const p = sigmoid(dE / T);
      probEl.textContent = p.toFixed(2);

      let path = "";
      for (let px = 0; px <= 200; px++) {
        const x = xMin + (xMax - xMin) * px / 200;
        const yy = sigmoid(x / T);
        path += (px === 0 ? "M" : "L") + X(x).toFixed(1) + " " + Y(yy).toFixed(1) + " ";
      }
      // gridlines + axes
      let grid = "";
      [0, 0.25, 0.5, 0.75, 1].forEach(p2 => {
        grid += `<line x1="${padL}" y1="${Y(p2)}" x2="${W_-padR}" y2="${Y(p2)}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${padL-6}" y="${Y(p2)+3}" font-size="10" text-anchor="end">${p2}</text>`;
      });
      [-6,-3,0,3,6].forEach(x => {
        grid += `<line x1="${X(x)}" y1="${padT}" x2="${X(x)}" y2="${H_-padB}" stroke="${COL.grid}" stroke-width="1"/>`;
        grid += `<text x="${X(x)}" y="${H_-padB+16}" font-size="10" text-anchor="middle">${x>0?"+":""}${x}</text>`;
      });
      const half = `<line x1="${padL}" y1="${Y(0.5)}" x2="${W_-padR}" y2="${Y(0.5)}" stroke="${COL.muted}" stroke-dasharray="4 4"/>`;
      const marker =
        `<line x1="${X(dE)}" y1="${padT}" x2="${X(dE)}" y2="${H_-padB}" stroke="${COL.bad}" stroke-width="1.5" stroke-dasharray="3 3"/>` +
        `<circle cx="${X(dE)}" cy="${Y(p)}" r="6" fill="${COL.bad}"/>` +
        `<text x="${X(dE)+9}" y="${Y(p)-6}" font-size="12" fill="${COL.bad}" font-weight="700">p=${p.toFixed(2)}</text>`;
      svg.innerHTML =
        grid + half +
        `<path d="${path}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>` +
        marker +
        `<text x="${X(0)}" y="${H_-4}" font-size="11" text-anchor="middle">energy saved by turning on  (&Delta;E)</text>` +
        `<text x="14" y="${padT+90}" font-size="11" transform="rotate(-90 14 ${padT+90})" text-anchor="middle">p(unit on)</text>`;

      if (T < 0.2) msgEl.innerHTML = "Nearly a step function — this is a <strong>deterministic Hopfield update</strong>.";
      else if (T > 3) msgEl.innerHTML = "Almost flat at 0.5 — the unit is basically <strong>flipping a coin</strong>.";
      else msgEl.innerHTML = "A soft threshold: lowering energy is likely but not certain — room to explore.";
    }
    tempSlider.addEventListener("input", draw);
    deSlider.addEventListener("input", draw);
    onTheme(draw);
    draw();
  })();

  // ============================================================
  // DEMO C — Boltzmann distribution over a 4-unit network
  // ============================================================
  (function () {
    const n = 4;
    // weights with a unique global minimum, some frustration
    const W = [[0,2,-1,0.6],[2,0,0.4,-1],[-1,0.4,0,1.8],[0.6,-1,1.8,0]];
    const b = [0.5,-0.3,0.15,-0.1];
    function E(st) {
      let e = 0;
      for (let i = 0; i < n; i++) { e -= b[i]*st[i]; for (let j = i+1; j < n; j++) e -= W[i][j]*st[i]*st[j]; }
      return e;
    }
    const states = [];
    for (let m = 0; m < (1 << n); m++) {
      const st = [];
      for (let i = 0; i < n; i++) st.push(((m >> i) & 1) ? 1 : -1);
      states.push({ st, e: E(st) });
    }
    states.sort((a, b2) => a.e - b2.e); // sort by energy ascending
    const eLo = states[0].e, eHi = states[states.length - 1].e;

    const svg = document.getElementById("bmC-svg");
    const tempSlider = document.getElementById("bmC-temp");
    const tval = document.getElementById("bmC-tval");
    const W_ = 720, H_ = 240, padT = 12, padB = 44;
    const slot = W_ / states.length;
    const barW = slot * 0.7;

    function energyColor(e) {
      const t = (e - eLo) / (eHi - eLo + 1e-9); // 0 low ... 1 high
      // interpolate accent (low energy) -> muted slate (high energy)
      return mix(COL.accent, COL.muted, t);
    }

    function draw() {
      const T = tempSlider.value / 100;
      tval.textContent = T.toFixed(2);
      const ps = states.map(s => Math.exp(-s.e / T));
      const Z = ps.reduce((a, x) => a + x, 0);
      const probs = ps.map(p => p / Z);
      const pMax = Math.max(...probs);
      const top = (H_ - padT - padB);

      let out = "";
      states.forEach((s, k) => {
        const x = k * slot + (slot - barW) / 2;
        const h = (probs[k] / pMax) * top;
        const yTop = H_ - padB - h;
        out += `<rect x="${x.toFixed(1)}" y="${yTop.toFixed(1)}" width="${barW.toFixed(1)}" height="${h.toFixed(1)}" rx="3" fill="${energyColor(s.e)}"/>`;
        // probability label on tallest few
        if (probs[k] > 0.04) out += `<text x="${(x+barW/2).toFixed(1)}" y="${(yTop-4).toFixed(1)}" font-size="9" text-anchor="middle">${(probs[k]*100).toFixed(0)}%</text>`;
        // mini 2x2 state glyph under bar
        const gx = k * slot + slot/2, gy = H_ - padB + 8, cs = 7;
        for (let i = 0; i < n; i++) {
          const cx = gx - cs + (i % 2) * cs, cy = gy + Math.floor(i / 2) * cs;
          out += `<rect x="${cx}" y="${cy}" width="${cs-1.5}" height="${cs-1.5}" rx="1" fill="${s.st[i]===1?COL.accent:COL.inset}"/>`;
        }
      });
      // label global min
      out += `<text x="${(0*slot+slot/2).toFixed(1)}" y="${H_-2}" font-size="9" text-anchor="middle" fill="${COL.accent}" font-weight="700">min E</text>`;
      svg.innerHTML = out;
    }
    tempSlider.addEventListener("input", draw);
    onTheme(draw);
    draw();
  })();

  // ============================================================
  // DEMO D — simulated annealing
  // ============================================================
  (function () {
    const cells = makeGrid(document.getElementById("bmD-grid"), N);
    const svg = document.getElementById("bmD-svg");
    let mode = "anneal";

    document.querySelectorAll("#bmD-mode button").forEach(btn => {
      btn.addEventListener("click", () => {
        document.querySelectorAll("#bmD-mode button").forEach(b => b.classList.remove("active"));
        btn.classList.add("active");
        mode = btn.dataset.mode;
      });
    });

    function tempSchedule(mode, k, K) {
      if (mode === "greedy") return 0.05;
      if (mode === "hot") return 2.0;
      return 3.0 * Math.pow(0.06 / 3.0, k / K); // anneal 3.0 -> 0.06
    }

    function runOnce(mode, record) {
      let s = Array.from({length: N}, () => (Math.random() < 0.5 ? 1 : -1));
      const K = 50;
      const eTrace = [], tTrace = [];
      for (let k = 0; k < K; k++) {
        const T = tempSchedule(mode, k, K);
        const order = [...Array(N).keys()].sort(() => Math.random() - 0.5);
        for (const i of order) {
          const dE = 2 * field(s, i, HEB.W, HEB.b);
          s[i] = Math.random() < sigmoid(dE / T) ? 1 : -1;
        }
        if (record) { eTrace.push(energy(s, HEB.W, HEB.b)); tTrace.push(T); }
      }
      return { s, eTrace, tTrace };
    }

    function paintGrid(s) { for (let i = 0; i < N; i++) cells[i].classList.toggle("on", s[i] === 1); }

    let lastE = [], lastT = [];
    function drawTraces(eTrace, tTrace) {
      lastE = eTrace; lastT = tTrace;
      const W_ = 420, H_ = 180, pad = 8;
      const K = eTrace.length;
      const eLo = -5.2, eHi = 3.2, tHi = 3.2;
      const xs = K > 1 ? (W_ - 2*pad) / (K - 1) : 0;
      const yE = e => H_ - pad - (e - eLo) / (eHi - eLo) * (H_ - 2*pad);
      const yT = t => H_ - pad - (t / tHi) * (H_ - 2*pad);
      let de = "", dt = "";
      eTrace.forEach((e, k) => { de += (k===0?"M":"L") + (pad+k*xs).toFixed(1) + " " + yE(e).toFixed(1) + " "; });
      tTrace.forEach((t, k) => { dt += (k===0?"M":"L") + (pad+k*xs).toFixed(1) + " " + yT(t).toFixed(1) + " "; });
      svg.innerHTML =
        `<path d="${dt}" fill="none" stroke="${COL.warn}" stroke-width="1.5" stroke-dasharray="4 3" opacity="0.8"/>` +
        `<path d="${de}" fill="none" stroke="${COL.accent}" stroke-width="2.5"/>` +
        `<text x="${W_-pad}" y="${yE(-5)}" font-size="9" text-anchor="end" opacity="0.6">deep memory</text>`;
    }

    function setResult(s) {
      const r = document.getElementById("bmD-result");
      if (samePattern(s, PAT_T)) { r.textContent = "reached memory T ✓"; r.className = "bm-tag good"; }
      else if (samePattern(s, PAT_L)) { r.textContent = "reached memory L ✓"; r.className = "bm-tag good"; }
      else { r.textContent = "stuck (spurious / unsettled)"; r.className = "bm-tag warn"; }
    }

    // animated single run
    document.getElementById("bmD-run").addEventListener("click", () => {
      const { s, eTrace, tTrace } = runOnce(mode, true);
      // animate the trace growing
      let k = 1;
      const tick = () => {
        drawTraces(eTrace.slice(0, k), tTrace.slice(0, k));
        if (k < eTrace.length) { k++; requestAnimationFrame(tick); }
        else { paintGrid(s); setResult(s); }
      };
      tick();
    });

    document.getElementById("bmD-race").addEventListener("click", () => {
      const modes = ["greedy", "hot", "anneal"];
      const labels = { greedy: "Greedy (cold)", hot: "Always hot", anneal: "Annealed ❄️" };
      const colors = { greedy: COL.bad, hot: COL.warn, anneal: COL.good };
      const res = {};
      modes.forEach(m => {
        let solved = 0;
        for (let t = 0; t < 200; t++) {
          const { s } = runOnce(m, false);
          if (samePattern(s, PAT_T) || samePattern(s, PAT_L)) solved++;
        }
        res[m] = solved / 200;
      });
      const wrap = document.getElementById("bmD-race-wrap");
      wrap.style.display = "block";
      const bars = document.getElementById("bmD-race-bars");
      bars.innerHTML = modes.map(m => {
        const pct = Math.round(res[m] * 100);
        return `<div style="display:flex;align-items:center;gap:0.6rem;margin:0.4rem 0">
          <span style="width:130px;font-size:0.85rem">${labels[m]}</span>
          <div style="flex:1;background:${COL.inset};border-radius:6px;overflow:hidden;height:22px;max-width:360px">
            <div class="bm-pop" style="width:${pct}%;height:100%;background:${colors[m]};border-radius:6px"></div>
          </div>
          <strong style="width:3rem">${pct}%</strong>
        </div>`;
      }).join("");
    });

    // initial
    const init = runOnce("anneal", true);
    paintGrid(init.s); drawTraces(init.eTrace, init.tTrace); setResult(init.s);
    onTheme(() => drawTraces(lastE, lastT));
  })();

  // ============================================================
  // DEMO E — RBM (0/1 units), live CD-1 training
  // ============================================================
  (function () {
    const V = 9, H = 4, sig = sigmoid;
    // training data in {0,1}: T, L, O(ring-ish)
    const DATA = [
      [1,1,1, 0,1,0, 0,1,0], // T
      [1,0,0, 1,0,0, 1,1,1], // L
      [1,1,1, 1,0,1, 1,1,1]  // O
    ];
    let Wt, bv, bh, trained = false;

    function initW() {
      Wt = Array.from({length: V}, () => Array.from({length: H}, () => (Math.random()*2-1)*0.1));
      bv = new Array(V).fill(0);
      bh = new Array(H).fill(0);
      trained = false;
    }
    initW();

    const propUp = v => { const h = []; for (let j=0;j<H;j++){ let a=bh[j]; for(let i=0;i<V;i++) a+=v[i]*Wt[i][j]; h.push(sig(a)); } return h; };
    const propDown = h => { const v = []; for (let i=0;i<V;i++){ let a=bv[i]; for(let j=0;j<H;j++) a+=h[j]*Wt[i][j]; v.push(sig(a)); } return v; };
    const bern = p => (Math.random() < p ? 1 : 0);

    function cdStep(lr) {
      let sqErr = 0;
      for (const v0 of DATA) {
        const h0p = propUp(v0);
        const h0 = h0p.map(bern);
        const v1p = propDown(h0);
        const v1 = v1p.map(bern);
        const h1p = propUp(v1);
        for (let i=0;i<V;i++) for (let j=0;j<H;j++) Wt[i][j] += lr*(v0[i]*h0p[j] - v1p[i]*h1p[j]);
        for (let i=0;i<V;i++) bv[i] += lr*(v0[i]-v1p[i]);
        for (let j=0;j<H;j++) bh[j] += lr*(h0p[j]-h1p[j]);
        for (let i=0;i<V;i++) sqErr += (v0[i]-v1p[i])*(v0[i]-v1p[i]);
      }
      return sqErr / DATA.length;
    }

    // ---- viz: receptive fields ----
    const fieldsEl = document.getElementById("bmE-fields");
    function drawFields() {
      let maxAbs = 0.0001;
      for (let i=0;i<V;i++) for (let j=0;j<H;j++) maxAbs = Math.max(maxAbs, Math.abs(Wt[i][j]));
      fieldsEl.innerHTML = "";
      for (let j=0;j<H;j++) {
        const wrap = document.createElement("div");
        wrap.style.textAlign = "center";
        const g = document.createElement("div");
        g.className = "bm-grid";
        g.style.gridTemplateColumns = "repeat(3,15px)";
        g.style.gridTemplateRows = "repeat(3,15px)";
        for (let i=0;i<V;i++) {
          const c = document.createElement("div");
          const w = Wt[i][j] / maxAbs; // -1..1
          let col;
          if (w >= 0) { col = mix(COL.inset, COL.accent, w); }
          else { col = mix(COL.inset, COL.bad, -w); }
          c.style.background = col;
          c.style.borderRadius = "2px";
          g.appendChild(c);
        }
        const lab = document.createElement("div");
        lab.style.fontSize = "0.65rem"; lab.style.opacity = "0.6";
        lab.textContent = "h" + (j+1);
        wrap.appendChild(g); wrap.appendChild(lab);
        fieldsEl.appendChild(wrap);
      }
    }

    // ---- viz: error curve ----
    const errSvg = document.getElementById("bmE-err");
    const errHist = [];
    function drawErr() {
      const W_=400,H_=150,pad=10;
      if (errHist.length < 2) { errSvg.innerHTML = `<text x="${W_/2}" y="${H_/2}" font-size="12" text-anchor="middle" opacity="0.5">press Train</text>`; return; }
      const hi = Math.max(...errHist), lo = 0;
      const xs = (W_-2*pad)/(errHist.length-1);
      const y = e => H_-pad - (e-lo)/(hi-lo+1e-9)*(H_-2*pad);
      let d="";
      errHist.forEach((e,k)=>{ d += (k===0?"M":"L")+(pad+k*xs).toFixed(1)+" "+y(e).toFixed(1)+" "; });
      errSvg.innerHTML =
        `<path d="${d}" fill="none" stroke="${COL.accent}" stroke-width="2"/>` +
        `<text x="${W_-pad}" y="${y(errHist[errHist.length-1])-6}" font-size="10" text-anchor="end" fill="${COL.accent}">${errHist[errHist.length-1].toFixed(2)}</text>` +
        `<text x="${pad}" y="${H_-2}" font-size="9" opacity="0.6">epochs &rarr;</text>`;
    }

    // ---- training loop (chunked across frames) ----
    const epochEl = document.getElementById("bmE-epoch");
    let training = false;
    function train() {
      if (training) return;
      training = true;
      errHist.length = 0;
      const total = 4000, chunk = 120;
      let ep = 0;
      const step = () => {
        for (let c = 0; c < chunk && ep < total; c++, ep++) {
          const err = cdStep(0.1);
          if (ep % 40 === 0) errHist.push(err);
        }
        drawErr(); drawFields();
        epochEl.textContent = "epoch " + ep + " / " + total;
        if (ep < total) requestAnimationFrame(step);
        else { training = false; trained = true; epochEl.textContent = "trained ✓ (" + total + " epochs)"; reconstruct(); }
      };
      step();
    }

    document.getElementById("bmE-train").addEventListener("click", train);
    document.getElementById("bmE-resetw").addEventListener("click", () => {
      initW(); errHist.length = 0; drawErr(); drawFields(); epochEl.textContent = "weights reset"; clearHidden();
    });

    // ---- reconstruction panel ----
    const visCells = makeGrid(document.getElementById("bmE-vis"), V);
    const recCells = makeGrid(document.getElementById("bmE-rec"), V);
    const hidEl = document.getElementById("bmE-hid");
    let vState = DATA[0].slice();

    function paintVis() { for (let i=0;i<V;i++) visCells[i].classList.toggle("on", vState[i]===1); }
    function clearHidden() {
      hidEl.innerHTML = "";
      for (let j=0;j<H;j++){ const b=document.createElement("div"); b.style.cssText=`width:70px;height:14px;border-radius:7px;background:${COL.inset}`; hidEl.appendChild(b); }
      recCells.forEach(c=>{ c.classList.remove("on"); c.style.background=""; });
    }
    function reconstruct() {
      const hp = propUp(vState);
      hidEl.innerHTML = "";
      for (let j=0;j<H;j++) {
        const outer = document.createElement("div");
        outer.style.cssText = `width:70px;height:14px;border-radius:7px;background:${COL.inset};overflow:hidden`;
        const inner = document.createElement("div");
        inner.style.cssText = `width:${Math.round(hp[j]*100)}%;height:100%;background:${COL.accent};border-radius:7px`;
        outer.appendChild(inner); hidEl.appendChild(outer);
      }
      const h = hp.map(bern);
      const vp = propDown(h);
      for (let i=0;i<V;i++) {
        const on = vp[i] > 0.5;
        recCells[i].classList.toggle("on", on);
      }
    }

    visCells.forEach((c,i)=>c.addEventListener("click", ()=>{ vState[i] = vState[i]?0:1; paintVis(); }));
    document.getElementById("bmE-recon").addEventListener("click", reconstruct);
    document.getElementById("bmE-corrupt").addEventListener("click", () => {
      vState = [1,1,1, 0,0,0, 0,1,0]; // T with broken stem
      paintVis(); if (trained) reconstruct(); else clearHidden();
    });
    document.getElementById("bmE-clear").addEventListener("click", () => { vState = new Array(V).fill(0); paintVis(); clearHidden(); });

    // ---- architecture diagram ----
    function drawArch() {
      const svg = document.getElementById("bmE-arch");
      const W_=460,H_=170;
      const vx = 120, hx = 340;
      const vys = [], hys = [];
      for (let i=0;i<V;i++) vys.push(20 + i*(H_-40)/(V-1));
      for (let j=0;j<H;j++) hys.push(40 + j*(H_-80)/(H-1));
      let s = "";
      for (let i=0;i<V;i++) for (let j=0;j<H;j++)
        s += `<line x1="${vx}" y1="${vys[i]}" x2="${hx}" y2="${hys[j]}" stroke="${COL.line}" stroke-width="0.6" opacity="0.7"/>`;
      vys.forEach(y => s += `<circle cx="${vx}" cy="${y}" r="7" fill="${COL.accent}"/>`);
      hys.forEach(y => s += `<circle cx="${hx}" cy="${y}" r="9" fill="${COL.warn}"/>`);
      s += `<text x="${vx}" y="12" font-size="12" text-anchor="middle" font-weight="700">visible (data)</text>`;
      s += `<text x="${hx}" y="22" font-size="12" text-anchor="middle" font-weight="700">hidden (features)</text>`;
      s += `<text x="${vx}" y="${H_-2}" font-size="10" text-anchor="middle" opacity="0.7">9 units</text>`;
      s += `<text x="${hx}" y="${H_-2}" font-size="10" text-anchor="middle" opacity="0.7">4 units · no in-layer links</text>`;
      svg.innerHTML = s;
    }
    drawArch();

    drawErr(); drawFields(); paintVis(); clearHidden();
    onTheme(() => { drawErr(); drawFields(); drawArch(); });
  })();
})();
</script>

</div>]]></content><author><name></name></author><category term="Machine Learning" /><category term="Interactive" /><summary type="html"><![CDATA[You already know Hopfield networks slide downhill into the nearest memory — and get stuck. Add a single ingredient, temperature, and that deterministic descent becomes a Boltzmann machine that samples, escapes local minima, and learns. A hands-on, visual refresher with live energy landscapes, annealing, and a trainable RBM.]]></summary></entry><entry><title type="html">A Day in Luca’s Life: Finding Optimal Decisions with MDPs</title><link href="https://alexdruso.github.io/finding-optimal-policies-mdp/" rel="alternate" type="text/html" title="A Day in Luca’s Life: Finding Optimal Decisions with MDPs" /><published>2026-05-21T00:00:00+00:00</published><updated>2026-05-21T00:00:00+00:00</updated><id>https://alexdruso.github.io/finding-optimal-policies-mdp</id><content type="html" xml:base="https://alexdruso.github.io/finding-optimal-policies-mdp/"><![CDATA[<style>
/* ============================================================
   Study Notes — shared design system
   Sleek/futuristic: cyan-teal accent, hairline borders, flat
   single-accent controls, monospace numerals, glow in dark only.
   Adaptive light + dark via tokens. Every selector is scoped
   under .study-note so nothing leaks to the rest of the site.
   ============================================================ */
.study-note {
  --sn-accent: #0891b2;
  --sn-accent-strong: #06b6d4;
  --sn-accent-soft: rgba(8, 145, 178, 0.10);
  --sn-accent-line: rgba(8, 145, 178, 0.30);
  --sn-on-accent: #ffffff;

  --sn-panel: #ffffff;
  --sn-panel-inset: #f6f8fa;
  --sn-section: #f7f9fb;
  --sn-border: rgba(15, 23, 42, 0.08);
  --sn-grid: rgba(15, 23, 42, 0.10);

  --sn-text: #1f2933;
  --sn-muted: #64748b;

  --sn-good: #0f9d8c;  --sn-good-bg: rgba(15, 157, 140, 0.10);
  --sn-warn: #b07d18;  --sn-warn-bg: rgba(176, 125, 24, 0.10);
  --sn-bad:  #b4544b;  --sn-bad-bg:  rgba(180, 84, 75, 0.10);

  --sn-radius: 12px;
  --sn-radius-sm: 8px;
  --sn-shadow: 0 1px 2px rgba(15, 23, 42, 0.04), 0 4px 16px rgba(15, 23, 42, 0.05);
  --sn-ease: cubic-bezier(0.22, 0.61, 0.36, 1);
  --sn-glow: none;
  --sn-mono: ui-monospace, "SF Mono", "JetBrains Mono", "Roboto Mono", Menlo, Consolas, monospace;
}
.dark-theme .study-note {
  --sn-accent: #22d3ee;
  --sn-accent-strong: #67e8f9;
  --sn-accent-soft: rgba(34, 211, 238, 0.12);
  --sn-accent-line: rgba(34, 211, 238, 0.35);
  --sn-on-accent: #06141a;

  --sn-panel: #11131c;
  --sn-panel-inset: #161a26;
  --sn-section: #14161f;
  --sn-border: rgba(148, 163, 184, 0.14);
  --sn-grid: rgba(148, 163, 184, 0.16);

  --sn-text: #e2e8f0;
  --sn-muted: #94a3b8;

  --sn-good: #34d399;  --sn-good-bg: rgba(52, 211, 153, 0.12);
  --sn-warn: #e2b04a;  --sn-warn-bg: rgba(226, 176, 74, 0.12);
  --sn-bad:  #f0857c;  --sn-bad-bg:  rgba(240, 133, 124, 0.12);

  --sn-shadow: 0 1px 2px rgba(0, 0, 0, 0.4);
  --sn-glow: 0 0 0 1px var(--sn-accent-line), 0 0 16px rgba(34, 211, 238, 0.18);
}

/* ---- sections / cards ---- */
.study-note .se-section,
.study-note .bm-section,
.study-note .mdp-section {
  margin: 2rem 0;
  padding: 1.5rem;
  border-radius: var(--sn-radius);
  background: var(--sn-section);
  border: 1px solid var(--sn-border);
  box-shadow: var(--sn-shadow);
}
.study-note .se-section h3,
.study-note .bm-section h3,
.study-note .mdp-section h3 {
  margin-top: 0;
  color: var(--sn-accent);
  letter-spacing: -0.01em;
}

/* ---- equation boxes ---- */
.study-note .se-equation,
.study-note .bm-equation,
.study-note .equation-box {
  padding: 1rem 1.5rem;
  margin: 1rem 0;
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  font-size: 1.1rem;
  text-align: center;
  overflow-x: auto;
}
.study-note .se-equation .big,
.study-note .bm-equation .big { font-size: 1.25rem; }

/* ---- callout / note ---- */
.study-note .se-note,
.study-note .bm-note {
  background: var(--sn-accent-soft);
  border-radius: var(--sn-radius-sm);
  border-left: 3px solid var(--sn-accent);
  padding: 0.9rem 1.1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- example box ---- */
.study-note .se-example,
.study-note .bm-example,
.study-note .example-box {
  background: var(--sn-panel-inset);
  border: 1px solid var(--sn-border);
  border-left: 3px solid var(--sn-warn);
  border-radius: var(--sn-radius-sm);
  padding: 1rem;
  font-size: 0.9rem;
  margin: 1rem 0;
}

/* ---- chart containers ---- */
.study-note .se-chartwrap,
.study-note .bm-chartwrap,
.study-note .graph-wrap {
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.5rem;
}
.study-note .graph-wrap { overflow-x: auto; }

/* ---- primary buttons ---- */
.study-note .se-btn,
.study-note .bm-btn,
.study-note .vi-btn {
  padding: 0.5rem 1.1rem;
  border: none;
  border-radius: var(--sn-radius-sm);
  font-size: 0.9rem;
  font-weight: 600;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-on-accent);
  background: var(--sn-accent);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.2s var(--sn-ease), background 0.2s var(--sn-ease);
}
.study-note .se-btn:hover:not(:disabled),
.study-note .bm-btn:hover:not(:disabled),
.study-note .vi-btn:hover:not(:disabled) {
  background: var(--sn-accent-strong);
  transform: translateY(-1px);
  box-shadow: var(--sn-glow);
}
.study-note .se-btn:disabled,
.study-note .bm-btn:disabled,
.study-note .vi-btn:disabled { opacity: 0.4; cursor: not-allowed; }
/* secondary variant */
.study-note .se-btn.sec,
.study-note .bm-btn.sec,
.study-note .vi-btn.sec {
  background: transparent;
  color: var(--sn-accent);
  box-shadow: inset 0 0 0 1px var(--sn-accent-line);
}
.study-note .se-btn.sec:hover:not(:disabled),
.study-note .bm-btn.sec:hover:not(:disabled),
.study-note .vi-btn.sec:hover:not(:disabled) {
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
}

/* ---- ghost / reset buttons ---- */
.study-note .se-reset,
.study-note .bm-reset,
.study-note .reset-link {
  background: none;
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.4rem 0.85rem;
  font-size: 0.85rem;
  font-family: inherit;
  cursor: pointer;
  color: var(--sn-muted);
  transition: background 0.15s var(--sn-ease), border-color 0.15s var(--sn-ease), color 0.15s var(--sn-ease);
}
.study-note .se-reset:hover,
.study-note .bm-reset:hover,
.study-note .reset-link:hover {
  background: var(--sn-accent-soft);
  border-color: var(--sn-accent-line);
  color: var(--sn-accent);
}

/* ---- range sliders: flat, single-accent ---- */
.study-note input[type="range"] {
  -webkit-appearance: none;
  appearance: none;
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
  outline: none;
  cursor: pointer;
}
.study-note input[type="range"]::-webkit-slider-thumb {
  -webkit-appearance: none;
  appearance: none;
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
  transition: transform 0.15s var(--sn-ease), box-shadow 0.15s var(--sn-ease);
}
.study-note input[type="range"]::-webkit-slider-thumb:hover { transform: scale(1.15); }
.study-note input[type="range"]::-moz-range-thumb {
  width: 16px;
  height: 16px;
  border-radius: 50%;
  background: var(--sn-accent);
  border: none;
  box-shadow: 0 0 0 4px var(--sn-accent-soft);
}
.study-note input[type="range"]::-moz-range-track {
  height: 4px;
  border-radius: 999px;
  background: var(--sn-border);
}

/* ---- status pills ---- */
.study-note .se-tag,
.study-note .bm-tag {
  display: inline-block;
  padding: 0.22rem 0.65rem;
  border-radius: 999px;
  font-size: 0.78rem;
  font-weight: 600;
  letter-spacing: 0.01em;
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
  transition: background 0.2s var(--sn-ease), color 0.2s var(--sn-ease);
}
.study-note .se-tag.good, .study-note .bm-tag.good { background: var(--sn-good-bg); color: var(--sn-good); }
.study-note .se-tag.warn, .study-note .bm-tag.warn { background: var(--sn-warn-bg); color: var(--sn-warn); }
.study-note .se-tag.hot,  .study-note .bm-tag.hot  { background: var(--sn-bad-bg);  color: var(--sn-bad); }

/* ---- numerals: monospace, tabular, accent (size kept per-note) ---- */
.study-note .se-stat .val,
.study-note .bm-stat .val,
.study-note .score-item .sval,
.study-note .compare-score {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  color: var(--sn-accent);
  font-weight: 700;
  line-height: 1.1;
  letter-spacing: -0.02em;
}
.study-note .se-readout,
.study-note .bm-temp-readout {
  font-family: var(--sn-mono);
  font-variant-numeric: tabular-nums;
  font-weight: 600;
  color: var(--sn-accent);
}

/* ---- svg text (scoped; replaces the old leaky global rule) ---- */
.study-note svg text { fill: var(--sn-muted); }
</style>


<div class="study-note">

<style>
/* Note-specific layout. Shared components (sections, buttons, equations,
   example boxes, ghost buttons, numerals, charts) live in study-note-styles.html. */
.concept-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(210px, 1fr));
  gap: 0.75rem;
  margin: 1rem 0;
}
.concept-card {
  padding: 0.9rem;
  border-radius: var(--sn-radius-sm);
  border: 1px solid var(--sn-border);
  border-left: 3px solid var(--sn-accent);
  background: var(--sn-panel);
  font-size: 0.9rem;
}
.concept-card .clabel {
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  font-weight: 700;
  opacity: 0.55;
  margin-bottom: 0.4rem;
}

.state-badge {
  display: inline-flex;
  align-items: center;
  gap: 0.35rem;
  padding: 0.25rem 0.7rem;
  border-radius: 999px;
  font-size: 0.85rem;
  font-weight: 600;
  background: var(--sn-accent-soft);
  color: var(--sn-accent);
  border: 1px solid var(--sn-accent-line);
  cursor: pointer;
  transition: all 0.15s var(--sn-ease);
  font-family: inherit;
}
.state-badge:hover, .state-badge.active {
  background: var(--sn-accent);
  color: var(--sn-on-accent);
  border-color: var(--sn-accent);
}

.action-btn {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  padding: 0.55rem 1.1rem;
  border-radius: var(--sn-radius-sm);
  border: 1px solid var(--sn-border);
  font-size: 0.95rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.15s var(--sn-ease);
  background: var(--sn-panel-inset);
  color: var(--sn-text);
  font-family: inherit;
}
.action-btn:hover:not(:disabled) { border-color: var(--sn-accent-line); }
.action-btn:disabled { opacity: 0.4; cursor: not-allowed; }
.action-btn[data-action="0"] { border-color: var(--sn-accent-line); }
.action-btn[data-action="0"].active { background: var(--sn-accent); color: var(--sn-on-accent); border-color: var(--sn-accent); }
.action-btn[data-action="1"] { border-color: rgba(180, 84, 75, 0.4); }
.action-btn[data-action="1"].active { background: var(--sn-bad); color: #fff; border-color: var(--sn-bad); }
.action-btn[data-action="2"] { border-color: rgba(15, 157, 140, 0.4); }
.action-btn[data-action="2"].active { background: var(--sn-good); color: #fff; border-color: var(--sn-good); }

.transition-info {
  margin-top: 0.75rem;
  padding: 0.85rem 1rem;
  border-radius: var(--sn-radius-sm);
  background: var(--sn-accent-soft);
  border: 1px solid var(--sn-accent-line);
  min-height: 70px;
  font-size: 0.9rem;
}

.prob-row { display: flex; align-items: center; gap: 0.5rem; margin: 0.25rem 0; }
.prob-bar { height: 16px; border-radius: 4px; transition: width 0.3s; opacity: 0.85; }

.play-display {
  text-align: center;
  padding: 1.25rem;
  border-radius: var(--sn-radius);
  background: var(--sn-panel);
  border: 1px solid var(--sn-border);
  margin-bottom: 0.75rem;
  transition: border-color 0.3s;
}
.play-emoji { font-size: 3.5rem; line-height: 1; }
.play-name { font-size: 1.4rem; font-weight: 700; margin: 0.4rem 0 0.2rem; }
.play-desc { opacity: 0.6; font-size: 0.85rem; }

.score-row {
  display: flex;
  justify-content: center;
  gap: 2.5rem;
  margin: 0.75rem 0;
}
.score-item { text-align: center; }
.score-item .slabel { font-size: 0.7rem; text-transform: uppercase; opacity: 0.55; }
.score-item .sval { font-size: 2rem; }

.action-row { display: flex; gap: 0.6rem; justify-content: center; flex-wrap: wrap; margin: 0.75rem 0; }

.play-log {
  max-height: 180px;
  overflow-y: auto;
  border: 1px solid var(--sn-border);
  border-radius: var(--sn-radius-sm);
  padding: 0.4rem 0.6rem;
  font-size: 0.82rem;
  font-family: var(--sn-mono);
}
.log-entry { padding: 0.15rem 0; border-bottom: 1px solid var(--sn-border); }
.lg { color: var(--sn-good); font-weight: 600; }
.lbad { color: var(--sn-bad); }

.vi-controls { display: flex; gap: 0.6rem; align-items: center; flex-wrap: wrap; margin-bottom: 0.75rem; }

.vi-chart {
  display: flex;
  gap: 0.4rem;
  align-items: flex-end;
  height: 160px;
  padding-bottom: 0.25rem;
  border-bottom: 1px solid var(--sn-border);
  margin-bottom: 0.75rem;
}
.vi-bar-group { flex: 1; display: flex; flex-direction: column; align-items: center; gap: 0.2rem; }
.vi-bar { width: 100%; border-radius: 4px 4px 0 0; transition: height 0.35s ease, background 0.35s; min-height: 2px; }
.vi-bv { font-size: 0.72rem; font-weight: 700; text-align: center; font-family: var(--sn-mono); }
.vi-be { font-size: 1rem; text-align: center; }
.vi-bn { font-size: 0.62rem; opacity: 0.65; text-align: center; }

.vi-table { width: 100%; border-collapse: collapse; font-size: 0.82rem; margin-top: 0.5rem; }
.vi-table th, .vi-table td { padding: 0.35rem 0.5rem; border: 1px solid var(--sn-border); text-align: center; }
.vi-table th { background: var(--sn-panel-inset); font-weight: 600; }
.vi-table td.best { background: var(--sn-good-bg); color: var(--sn-good); font-weight: 700; }

.policy-table { width: 100%; border-collapse: collapse; font-size: 0.88rem; }
.policy-table th, .policy-table td { padding: 0.45rem 0.65rem; border: 1px solid var(--sn-border); text-align: center; }
.policy-table th { background: var(--sn-panel-inset); }
.policy-table td:last-child { text-align: left; font-size: 0.78rem; opacity: 0.75; }

.compare-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-top: 1rem; }
.compare-box { padding: 1rem; border-radius: var(--sn-radius); border: 1px solid var(--sn-border); text-align: center; }
.compare-box.yours { border-color: rgba(180, 84, 75, 0.45); }
.compare-box.opt { border-color: rgba(15, 157, 140, 0.45); }
.compare-score { font-size: 2.5rem; margin: 0.4rem 0; }
.compare-box.yours .compare-score { color: var(--sn-bad); }
.compare-box.opt .compare-score { color: var(--sn-good); }

.flex-row { display: flex; align-items: center; gap: 0.6rem; flex-wrap: wrap; }
.mb1 { margin-bottom: 0.5rem; }

@keyframes popin { from { transform: scale(0.85); opacity: 0; } to { transform: scale(1); opacity: 1; } }
.popin { animation: popin 0.25s ease; }

.iter-label { font-size: 0.85rem; opacity: 0.65; }
</style>

<p>
  Meet Luca. He's 9 years old, loves dinosaurs, and has exactly one decision to make each afternoon when he gets home from school: <strong>should he study, play video games, or take a nap?</strong>
</p>

<p>
  This feels like a simple question — and for any single afternoon, it kind of is. But what if Luca wants to <em>maximize his happiness over the whole school year?</em> Suddenly it's not obvious at all. Today's fun (🎮) costs energy that hurts tomorrow's performance. Today's studying (📚) feels boring but could mean great test scores next week. Napping (💤) skips both.
</p>

<p>
  This is exactly the kind of problem that <strong>Markov Decision Processes (MDPs)</strong> were designed to solve — and the algorithm for solving them is <em>surprisingly elegant</em>. By the end of this post you'll have built intuition for how AI agents learn to make optimal decisions under uncertainty, using nothing more than Luca's homework dilemma as a guide.
</p>

<!-- ==================== MDP CONCEPT ==================== -->
<div class="mdp-section">
  <h3>🧱 The building blocks of an MDP</h3>
  <p style="margin-top:0">Every MDP is defined by four things. Here's how they map to Luca's life:</p>

  <div class="concept-grid">
    <div class="concept-card" style="border-left-color:var(--sn-accent)">
      <div class="clabel">States S</div>
      Luca's situation, captured as his <strong>energy &amp; grade level</strong>.<br><br>
      😴 Exhausted &rarr; 😐 Tired &rarr; 🙂 Balanced &rarr; 💪 Focused &rarr; ⭐ Star
    </div>
    <div class="concept-card" style="border-left-color:var(--sn-bad)">
      <div class="clabel">Actions A &amp; Rewards R(s,a)</div>
      Three choices each afternoon — but the happiness they bring <strong>depends on Luca's state</strong>. A Star Student enjoys everything more:<br><br>
      <small>
      <table style="border-collapse:collapse;width:100%;font-size:0.8rem">
        <tr><th style="text-align:left;padding:2px 4px"></th><th style="padding:2px 4px">📚 Study</th><th style="padding:2px 4px">🎮 Play</th><th style="padding:2px 4px">💤 Rest</th></tr>
        <tr><td style="padding:2px 4px">😴 Exhausted</td><td style="padding:2px 4px;text-align:center">+2</td><td style="padding:2px 4px;text-align:center">+4</td><td style="padding:2px 4px;text-align:center">0</td></tr>
        <tr><td style="padding:2px 4px">😐 Tired</td><td style="padding:2px 4px;text-align:center">+3</td><td style="padding:2px 4px;text-align:center">+5</td><td style="padding:2px 4px;text-align:center">+1</td></tr>
        <tr><td style="padding:2px 4px">🙂 Balanced</td><td style="padding:2px 4px;text-align:center">+5</td><td style="padding:2px 4px;text-align:center">+7</td><td style="padding:2px 4px;text-align:center">+3</td></tr>
        <tr><td style="padding:2px 4px">💪 Focused</td><td style="padding:2px 4px;text-align:center">+8</td><td style="padding:2px 4px;text-align:center">+10</td><td style="padding:2px 4px;text-align:center">+6</td></tr>
        <tr><td style="padding:2px 4px">⭐ Star</td><td style="padding:2px 4px;text-align:center"><strong>+12</strong></td><td style="padding:2px 4px;text-align:center"><strong>+14</strong></td><td style="padding:2px 4px;text-align:center"><strong>+10</strong></td></tr>
      </table>
      </small>
    </div>
    <div class="concept-card" style="border-left-color:var(--sn-warn)">
      <div class="clabel">Transitions P(s'|s,a)</div>
      Life is <strong>stochastic</strong>. Studying doesn't guarantee improvement — Luca might be too tired, or the test might be hard. Each action leads to next states with specific <em>probabilities</em>.
    </div>
    <div class="concept-card" style="border-left-color:var(--sn-good)">
      <div class="clabel">Discount factor γ = 0.9</div>
      Future happiness is worth slightly less than today's — not because it matters less, but because there's uncertainty about reaching that future. A reward tomorrow is worth 0.9× a reward today.
    </div>
  </div>
</div>

<!-- ==================== STATE GRAPH ==================== -->
<div class="mdp-section">
  <h3>🗺️ Explore the transition graph</h3>
  <p style="margin-top:0">Select a <strong>state</strong> (where Luca is) and an <strong>action</strong> (what he does) to see where he might end up — and with what probability.</p>

  <div class="flex-row mb1" id="state-sel"></div>
  <div class="flex-row mb1" id="action-sel"></div>

  <div class="graph-wrap">
    <svg id="mdp-svg" width="700" height="250" viewBox="0 0 700 250" style="max-width:100%;display:block;margin:0 auto"></svg>
  </div>
  <div class="transition-info" id="trans-info">
    <span style="opacity:0.45">↑ Select a state and an action above to see the transitions</span>
  </div>
</div>

<!-- ==================== BELLMAN ==================== -->
<div class="mdp-section">
  <h3>📐 The Bellman equation — what's a state really worth?</h3>
  <p style="margin-top:0">
    To find the best policy we need to know the <strong>value</strong> of each state: roughly, "how much total happiness will Luca accumulate from now on, if he plays optimally starting from here?" Call it <em>V*(s)</em>.
  </p>
  <p>
    The key insight (due to Richard Bellman, 1957) is that the value of a state can be defined <em>recursively</em>:
  </p>
  <div class="equation-box">
    V*(s) = max<sub>a</sub> [ R(s,a) + γ &middot; &Sigma;<sub>s'</sub> P(s'|s,a) &middot; V*(s') ]
  </div>
  <p>Reading left to right: the value of state <em>s</em> equals the best action <em>a</em> you can take, where "best" means: (immediate reward) + (discount &times; expected value of next state).</p>

  <div class="example-box">
    <strong>Iteration 1</strong> (V = 0 everywhere): Q(Balanced, 📚 Study) = 5 + 0 = <strong>5</strong>, Q(Balanced, 🎮 Play) = 7 + 0 = <strong>7</strong>, Q(Balanced, 💤 Rest) = 3 + 0 = <strong>3</strong>. Play wins on pure immediate reward.
    <br><br>
    <strong>After convergence</strong> (V approximately [70, 80, 94, 106, 113]):
    <br>
    Q(Balanced, 📚 Study) = 5 + 0.9&times;(0.1&times;80 + 0.3&times;94 + 0.6&times;106) = 5 + 89.8 = <strong>94.8 ← winner</strong><br>
    Q(Balanced, 🎮 Play)&nbsp; = 7 + 0.9&times;(0.4&times;80 + 0.4&times;94 + 0.2&times;106) = 7 + 81.7 = <strong>88.7</strong><br>
    Q(Balanced, 💤 Rest)&nbsp; = 3 + 0.9&times;(0.5&times;94 + 0.4&times;106 + 0.1&times;113) = 3 + 90.6 = <strong>93.6</strong>
    <br><br>
    Play's +2 immediate lead is overwhelmed because Study transitions toward Focused and Star — states where the daily reward is 10–14, not 7. The long-term math is decisive.
  </div>
</div>

<!-- ==================== PLAY AS LUCA ==================== -->
<div class="mdp-section">
  <h3>🎮 Play as Luca — try your own policy</h3>
  <p style="margin-top:0">Make Luca's choices for <strong>10 days</strong>. Try to maximize his total happiness. Your choices will be compared against the optimal policy afterward!</p>

  <div class="play-display" id="play-display">
    <div class="play-emoji" id="play-emoji">🙂</div>
    <div class="play-name" id="play-name">Balanced</div>
    <div class="play-desc" id="play-desc">Feeling okay, grades fine</div>
  </div>

  <div class="score-row">
    <div class="score-item"><div class="slabel">Day</div><div class="sval" id="play-day">1</div></div>
    <div class="score-item"><div class="slabel">Total Happiness</div><div class="sval" id="play-score">0</div></div>
  </div>

  <div class="action-row" id="play-btns"></div>

  <div class="play-log" id="play-log"></div>
  <div id="play-result" style="display:none;margin-top:0.75rem;padding:1rem;border-radius:8px;border:1px solid var(--sn-accent);text-align:center"></div>
  <div style="margin-top:0.6rem"><button class="reset-link" id="play-reset">↺ Reset</button></div>
</div>

<!-- ==================== VALUE ITERATION ==================== -->
<div class="mdp-section">
  <h3>⚙️ Value iteration — watch V(s) converge</h3>
  <p style="margin-top:0">Start with V(s) = 0 everywhere. Repeatedly apply the Bellman update. Each step gives a better estimate of how valuable each state truly is — the bar chart shows this live.</p>

  <div class="vi-controls">
    <button class="vi-btn" id="vi-step">Next iteration →</button>
    <button class="vi-btn sec" id="vi-run">Run to convergence</button>
    <button class="reset-link" id="vi-reset">↺ Reset</button>
    <span class="iter-label" id="vi-label">Iteration 0 &nbsp;|&nbsp; &Delta; = —</span>
  </div>

  <div class="vi-chart" id="vi-chart"></div>

  <details open>
    <summary style="cursor:pointer;font-weight:600;margin-bottom:0.5rem">Q-value table (current iteration)</summary>
    <div id="vi-qtable"></div>
  </details>

  <div id="vi-policy-wrap" style="display:none;margin-top:1.25rem">
    <h4 style="margin-bottom:0.6rem">🎯 Optimal policy</h4>
    <table class="policy-table" id="vi-policy"></table>
    <p style="font-size:0.8rem;opacity:0.65;margin-top:0.4rem" id="vi-conv-note"></p>
  </div>
</div>

<!-- ==================== COMPARISON ==================== -->
<div class="mdp-section" id="compare-section">
  <h3>🏆 How did you do vs. the optimal policy?</h3>
  <p id="compare-hint" style="margin-top:0;opacity:0.65">
    Complete 10 days above, then run value iteration to convergence — the comparison will appear here automatically.
  </p>
  <div id="compare-body" style="display:none">
    <div class="compare-grid">
      <div class="compare-box yours">
        <div style="font-weight:600">Your policy</div>
        <div class="compare-score" id="c-your"></div>
        <div style="font-size:0.8rem;opacity:0.65">happiness over 10 days</div>
      </div>
      <div class="compare-box opt">
        <div style="font-weight:600">Optimal policy</div>
        <div class="compare-score" id="c-opt"></div>
        <div style="font-size:0.8rem;opacity:0.65">same 10 days</div>
      </div>
    </div>
    <div id="c-table" style="margin-top:1rem;overflow-x:auto"></div>
    <p id="c-verdict" style="margin-top:0.75rem;font-weight:600;text-align:center"></p>
  </div>
</div>

<script>
// ─────────────────────────────────────────────────────────────
//  MDP DEFINITION
// ─────────────────────────────────────────────────────────────
// theme-aware colors: read CSS tokens, re-read + recolor on light/dark toggle
const SN = document.querySelector(".study-note");
const readCols = () => {
  const cs = getComputedStyle(SN);
  const c = n => cs.getPropertyValue(n).trim();
  return { accent: c("--sn-accent"), good: c("--sn-good"), bad: c("--sn-bad"),
           warn: c("--sn-warn"), muted: c("--sn-muted"), grid: c("--sn-grid"),
           softbg: c("--sn-accent-soft") };
};
let COL = readCols();
const redrawFns = [];
const onTheme = fn => { redrawFns.push(fn); return fn; };
function updateActionColors() {
  ACTIONS[0].color = COL.accent;
  ACTIONS[1].color = COL.bad;
  ACTIONS[2].color = COL.good;
}
new MutationObserver(() => { COL = readCols(); updateActionColors(); redrawFns.forEach(f => f()); })
  .observe(document.body, { attributes: true, attributeFilter: ["class"] });

const STATES = [
  { id: 0, name: "Exhausted", emoji: "😴", desc: "Burnt out, grades slipping" },
  { id: 1, name: "Tired",     emoji: "😐", desc: "Low energy, getting by" },
  { id: 2, name: "Balanced",  emoji: "🙂", desc: "Feeling okay, grades fine" },
  { id: 3, name: "Focused",   emoji: "💪", desc: "Full of energy, doing great" },
  { id: 4, name: "Star",      emoji: "⭐", desc: "Top of the class!" }
];

const ACTIONS = [
  { id: 0, name: "Study", emoji: "📚", color: COL.accent },
  { id: 1, name: "Play",  emoji: "🎮", color: COL.bad },
  { id: 2, name: "Rest",  emoji: "💤", color: COL.good }
];

// REWARDS[state][action]: happiness = state wellbeing + action base
// wellbeing: [0,1,3,6,10] | action base: study=2, play=4, rest=0
const REWARDS = [
  [ 2,  4,  0],  // Exhausted
  [ 3,  5,  1],  // Tired
  [ 5,  7,  3],  // Balanced
  [ 8, 10,  6],  // Focused
  [12, 14, 10]   // Star
];

const GAMMA = 0.9;

// T[state][action] = [{s, p}]
const T = [
  [ // 0: Exhausted
    [{s:0,p:.6},{s:1,p:.3},{s:2,p:.1}],
    [{s:0,p:.9},{s:1,p:.1}],
    [{s:0,p:.2},{s:1,p:.8}]
  ],
  [ // 1: Tired
    [{s:0,p:.1},{s:1,p:.4},{s:2,p:.5}],
    [{s:0,p:.5},{s:1,p:.4},{s:2,p:.1}],
    [{s:1,p:.4},{s:2,p:.5},{s:3,p:.1}]
  ],
  [ // 2: Balanced
    [{s:1,p:.1},{s:2,p:.3},{s:3,p:.6}],
    [{s:1,p:.4},{s:2,p:.4},{s:3,p:.2}],
    [{s:2,p:.5},{s:3,p:.4},{s:4,p:.1}]
  ],
  [ // 3: Focused
    [{s:2,p:.1},{s:3,p:.3},{s:4,p:.6}],
    [{s:1,p:.3},{s:2,p:.4},{s:3,p:.3}],
    [{s:3,p:.4},{s:4,p:.6}]
  ],
  [ // 4: Star
    [{s:3,p:.1},{s:4,p:.9}],
    [{s:2,p:.3},{s:3,p:.3},{s:4,p:.4}],
    [{s:3,p:.1},{s:4,p:.9}]
  ]
];

const NS = 'http://www.w3.org/2000/svg';
const POS = [
  {x:80, y:130}, {x:220,y:130}, {x:360,y:130}, {x:500,y:130}, {x:640,y:130}
];
const R = 42; // node radius

// ─────────────────────────────────────────────────────────────
//  BELLMAN HELPERS
// ─────────────────────────────────────────────────────────────
function qValues(V, s) {
  return ACTIONS.map((a, ai) => {
    let q = REWARDS[s][ai];
    for (const {s: sp, p} of T[s][ai]) q += GAMMA * p * V[sp];
    return q;
  });
}

function bellman(V) {
  return V.map((_, s) => Math.max(...qValues(V, s)));
}

function policy(V) {
  return V.map((_, s) => {
    const qs = qValues(V, s);
    return qs.indexOf(Math.max(...qs));
  });
}

// ─────────────────────────────────────────────────────────────
//  STATE GRAPH
// ─────────────────────────────────────────────────────────────
(function buildGraph() {
  const svg = document.getElementById('mdp-svg');
  let selState = null, selAction = null;

  // Arrowhead markers
  const defs = document.createElementNS(NS, 'defs');
  [...ACTIONS, {id:'gray', color: COL.muted}].forEach(a => {
    const m = document.createElementNS(NS, 'marker');
    m.setAttribute('id', `arr${a.id}`);
    m.setAttribute('markerWidth','10'); m.setAttribute('markerHeight','7');
    m.setAttribute('refX','9'); m.setAttribute('refY','3.5');
    m.setAttribute('orient','auto');
    const poly = document.createElementNS(NS, 'polygon');
    poly.setAttribute('points','0 0,10 3.5,0 7');
    poly.setAttribute('fill', a.color);
    m.appendChild(poly); defs.appendChild(m);
  });
  svg.appendChild(defs);

  const arrowLayer = document.createElementNS(NS, 'g');
  svg.appendChild(arrowLayer);

  // Draw nodes
  STATES.forEach(st => {
    const g = document.createElementNS(NS, 'g');
    g.style.cursor = 'pointer';
    g.addEventListener('click', () => pickState(st.id));

    const c = document.createElementNS(NS, 'circle');
    c.setAttribute('cx', POS[st.id].x); c.setAttribute('cy', POS[st.id].y);
    c.setAttribute('r', R); c.setAttribute('fill', COL.softbg);
    c.setAttribute('stroke', COL.accent); c.setAttribute('stroke-width','2');
    c.setAttribute('id', 'nc'+st.id);

    const em = document.createElementNS(NS, 'text');
    em.setAttribute('x', POS[st.id].x); em.setAttribute('y', POS[st.id].y - 10);
    em.setAttribute('text-anchor','middle'); em.setAttribute('dominant-baseline','middle');
    em.setAttribute('font-size','22'); em.textContent = st.emoji;

    const lbl = document.createElementNS(NS, 'text');
    lbl.setAttribute('x', POS[st.id].x); lbl.setAttribute('y', POS[st.id].y + 16);
    lbl.setAttribute('text-anchor','middle'); lbl.setAttribute('font-size','10');
    lbl.setAttribute('fill', COL.muted); lbl.setAttribute('id','nl'+st.id);
    lbl.textContent = st.name;

    g.appendChild(c); g.appendChild(em); g.appendChild(lbl);
    svg.appendChild(g);
  });

  // State buttons
  const sDiv = document.getElementById('state-sel');
  STATES.forEach(st => {
    const b = document.createElement('button');
    b.className = 'state-badge'; b.dataset.id = st.id;
    b.textContent = `${st.emoji} ${st.name}`;
    b.addEventListener('click', () => pickState(st.id));
    sDiv.appendChild(b);
  });

  // Action buttons
  const aDiv = document.getElementById('action-sel');
  ACTIONS.forEach(ac => {
    const b = document.createElement('button');
    b.className = 'action-btn'; b.dataset.action = ac.id;
    b.textContent = `${ac.emoji} ${ac.name}`;
    b.addEventListener('click', () => pickAction(ac.id));
    aDiv.appendChild(b);
  });

  function pickState(id) {
    selState = id;
    document.querySelectorAll('#state-sel .state-badge').forEach(b => {
      b.classList.toggle('active', +b.dataset.id === id);
    });
    STATES.forEach(st => {
      const nc = document.getElementById('nc'+st.id);
      const nl = document.getElementById('nl'+st.id);
      if (st.id === id) {
        nc.setAttribute('fill', COL.accent); nc.setAttribute('stroke', COL.accent);
        nl.setAttribute('fill','#fff');
      } else {
        nc.setAttribute('fill', COL.softbg); nc.setAttribute('stroke', COL.accent);
        nl.setAttribute('fill', COL.muted);
      }
    });
    render();
  }

  function pickAction(id) {
    selAction = id;
    document.querySelectorAll('#action-sel .action-btn').forEach(b => {
      b.classList.toggle('active', +b.dataset.action === id);
    });
    render();
  }

  function render() {
    arrowLayer.innerHTML = '';
    if (selState === null || selAction === null) return;

    const ac = ACTIONS[selAction];
    const trans = T[selState][selAction];
    const from = POS[selState];

    trans.forEach(({s: to, p}) => {
      const toPos = POS[to];
      if (to === selState) {
        drawSelf(from, p, ac);
      } else {
        drawEdge(from, toPos, p, ac, to > selState);
      }
    });

    // Info panel
    let html = `<strong>${STATES[selState].emoji} ${STATES[selState].name}</strong> takes action <strong>${ac.emoji} ${ac.name}</strong> (immediate reward: +${REWARDS[selState][selAction]}) and lands in:<br><br>`;
    trans.forEach(({s: to, p}) => {
      const pct = Math.round(p*100);
      html += `<div class="prob-row">
        <span style="width:130px;font-size:0.88rem">${STATES[to].emoji} ${STATES[to].name}</span>
        <div class="prob-bar" style="width:${pct*1.8}px;background:${ac.color}"></div>
        <strong>${pct}%</strong>
      </div>`;
    });
    document.getElementById('trans-info').innerHTML = html;
  }

  function drawSelf(pos, p, ac) {
    const path = document.createElementNS(NS, 'path');
    const r2 = 22;
    path.setAttribute('d', `M ${pos.x-r2} ${pos.y-R} A ${r2} ${r2} 0 1 1 ${pos.x+r2} ${pos.y-R}`);
    path.setAttribute('fill','none'); path.setAttribute('stroke', ac.color);
    path.setAttribute('stroke-width', Math.max(1.5, p*5));
    path.setAttribute('marker-end', `url(#arr${ac.id})`);
    path.setAttribute('opacity','0.8');
    arrowLayer.appendChild(path);

    const lbl = document.createElementNS(NS, 'text');
    lbl.setAttribute('x', pos.x); lbl.setAttribute('y', pos.y - R - r2*2 - 6);
    lbl.setAttribute('text-anchor','middle'); lbl.setAttribute('font-size','11');
    lbl.setAttribute('fill', ac.color); lbl.setAttribute('font-weight','700');
    lbl.textContent = Math.round(p*100)+'%';
    arrowLayer.appendChild(lbl);
  }

  function drawEdge(from, to, p, ac, goRight) {
    const dx = to.x-from.x, dy = to.y-from.y;
    const dist = Math.sqrt(dx*dx+dy*dy);
    const ux = dx/dist, uy = dy/dist;
    const sx = from.x + ux*R, sy = from.y + uy*R;
    const ex = to.x - ux*(R+8), ey = to.y - uy*(R+8);
    const offset = goRight ? -35 : 35;
    const mx = (sx+ex)/2 - uy*offset, my = (sy+ey)/2 + ux*offset;

    const path = document.createElementNS(NS, 'path');
    path.setAttribute('d', `M ${sx} ${sy} Q ${mx} ${my} ${ex} ${ey}`);
    path.setAttribute('fill','none'); path.setAttribute('stroke', ac.color);
    path.setAttribute('stroke-width', Math.max(1.5, p*5));
    path.setAttribute('marker-end', `url(#arr${ac.id})`);
    path.setAttribute('opacity','0.8');
    arrowLayer.appendChild(path);

    const t = 0.5;
    const lx = (1-t)*(1-t)*sx + 2*(1-t)*t*mx + t*t*ex;
    const ly = (1-t)*(1-t)*sy + 2*(1-t)*t*my + t*t*ey;
    const lbl = document.createElementNS(NS, 'text');
    lbl.setAttribute('x', lx); lbl.setAttribute('y', ly - 6);
    lbl.setAttribute('text-anchor','middle'); lbl.setAttribute('font-size','11');
    lbl.setAttribute('fill', ac.color); lbl.setAttribute('font-weight','700');
    lbl.textContent = Math.round(p*100)+'%';
    arrowLayer.appendChild(lbl);
  }

  onTheme(() => {
    ACTIONS.forEach(a => { const poly = document.querySelector(`#arr${a.id} polygon`); if (poly) poly.setAttribute('fill', a.color); });
    const gpoly = document.querySelector('#arrgray polygon'); if (gpoly) gpoly.setAttribute('fill', COL.muted);
    STATES.forEach(st => {
      const nc = document.getElementById('nc'+st.id);
      const nl = document.getElementById('nl'+st.id);
      if (st.id === selState) { nc.setAttribute('fill', COL.accent); nc.setAttribute('stroke', COL.accent); nl.setAttribute('fill', '#fff'); }
      else { nc.setAttribute('fill', COL.softbg); nc.setAttribute('stroke', COL.accent); nl.setAttribute('fill', COL.muted); }
    });
    render();
  });
})();

// ─────────────────────────────────────────────────────────────
//  PLAY AS LUCA
// ─────────────────────────────────────────────────────────────
const play = { state: 2, day: 1, score: 0, done: false, hist: [] };

function sample(s, a) {
  let r = Math.random(), cum = 0;
  for (const {s: sp, p} of T[s][a]) { cum += p; if (r < cum) return sp; }
  return T[s][a][T[s][a].length-1].s;
}

function updatePlayUI() {
  const st = STATES[play.state];
  document.getElementById('play-emoji').textContent = st.emoji;
  document.getElementById('play-name').textContent = st.name;
  document.getElementById('play-desc').textContent = st.desc;
  document.getElementById('play-day').textContent = play.day;
  document.getElementById('play-score').textContent = play.score;
}

function makeMove(aId) {
  if (play.done) return;
  const ac = ACTIONS[aId];
  const from = play.state;
  const next = sample(from, aId);
  const reward = REWARDS[from][aId];
  play.score += reward;
  play.hist.push({s: from, a: aId, ns: next, r: reward});

  const log = document.getElementById('play-log');
  const entry = document.createElement('div');
  entry.className = 'log-entry popin';
  const dir = next > from ? ' ↑' : next < from ? ` <span class="lbad">↓</span>` : '';
  entry.innerHTML = `<strong>Day ${play.day}</strong>: ${STATES[from].emoji}→${ac.emoji}→${STATES[next].emoji} <span class="lg">+${reward}</span>${dir}`;
  log.prepend(entry);

  play.state = next;
  play.day++;
  if (play.day > 10) {
    play.done = true;
    document.querySelectorAll('#play-btns .action-btn').forEach(b => b.disabled = true);
    const res = document.getElementById('play-result');
    res.style.display = 'block';
    res.innerHTML = `<strong>10 days done!</strong> Your total happiness: <span style="font-size:1.5rem;font-weight:700;color:${COL.accent}">${play.score}</span><br><span style="opacity:0.65;font-size:0.88rem">Run value iteration to convergence below, then scroll down to see how you compare!</span>`;
    tryShowComparison();
  }
  updatePlayUI();
}

// Inject play buttons
const pBtns = document.getElementById('play-btns');
ACTIONS.forEach(ac => {
  const b = document.createElement('button');
  b.className = 'action-btn'; b.dataset.action = ac.id;
  b.style.fontSize = '1rem'; b.style.padding = '0.65rem 1.3rem';
  b.innerHTML = `${ac.emoji} ${ac.name}`;
  b.addEventListener('click', () => makeMove(ac.id));
  pBtns.appendChild(b);
});

document.getElementById('play-reset').addEventListener('click', () => {
  Object.assign(play, {state:2, day:1, score:0, done:false, hist:[]});
  document.getElementById('play-log').innerHTML = '';
  document.getElementById('play-result').style.display = 'none';
  document.getElementById('compare-body').style.display = 'none';
  document.getElementById('compare-hint').style.display = '';
  document.querySelectorAll('#play-btns .action-btn').forEach(b => b.disabled = false);
  updatePlayUI();
});

updatePlayUI();

// ─────────────────────────────────────────────────────────────
//  VALUE ITERATION
// ─────────────────────────────────────────────────────────────
const vi = { V: new Array(5).fill(0), iter: 0, done: false };

function viStep() {
  if (vi.done) return;
  const newV = bellman(vi.V);
  const delta = Math.max(...newV.map((v,i) => Math.abs(v - vi.V[i])));
  vi.V = newV; vi.iter++;
  vi.done = delta < 0.001;

  document.getElementById('vi-label').innerHTML =
    `Iteration ${vi.iter} &nbsp;|&nbsp; &Delta; = ${delta.toFixed(4)}${vi.done ? ' &nbsp;✓ Converged!' : ''}`;

  renderChart(); renderQTable(); renderPolicy();

  if (vi.done) {
    document.getElementById('vi-step').disabled = true;
    document.getElementById('vi-run').disabled = true;
    document.getElementById('vi-conv-note').textContent =
      `Converged after ${vi.iter} iterations (Δ < 0.001).`;
    tryShowComparison();
  }
}

function renderChart() {
  const chart = document.getElementById('vi-chart');
  const maxV = Math.max(...vi.V, 0.01);
  const pol = policy(vi.V);
  chart.innerHTML = '';
  STATES.forEach((st, i) => {
    const g = document.createElement('div');
    g.className = 'vi-bar-group';
    const bar = document.createElement('div');
    bar.className = 'vi-bar';
    bar.style.height = Math.max((vi.V[i]/maxV)*130, 2) + 'px';
    bar.style.background = ACTIONS[pol[i]].color;
    const bv = document.createElement('div'); bv.className = 'vi-bv'; bv.textContent = vi.V[i].toFixed(1);
    const be = document.createElement('div'); be.className = 'vi-be'; be.textContent = st.emoji;
    const bn = document.createElement('div'); bn.className = 'vi-bn'; bn.textContent = st.name;
    g.appendChild(bar); g.appendChild(bv); g.appendChild(be); g.appendChild(bn);
    chart.appendChild(g);
  });
}

function renderQTable() {
  const pol = policy(vi.V);
  let html = `<table class="vi-table"><thead><tr>
    <th>State</th>${ACTIONS.map(a=>`<th>${a.emoji} ${a.name}</th>`).join('')}<th>V*(s)</th>
  </tr></thead><tbody>`;
  STATES.forEach((st, s) => {
    const qs = qValues(vi.V, s);
    const best = pol[s];
    html += `<tr><td>${st.emoji} ${st.name}</td>
      ${qs.map((q,a)=>`<td class="${a===best?'best':''}">${q.toFixed(2)}</td>`).join('')}
      <td><strong>${vi.V[s].toFixed(2)}</strong></td></tr>`;
  });
  html += '</tbody></table>';
  document.getElementById('vi-qtable').innerHTML = html;
}

function renderPolicy() {
  const pol = policy(vi.V);
  const wrap = document.getElementById('vi-policy-wrap');
  wrap.style.display = 'block';
  const qs_all = STATES.map((_, s) => qValues(vi.V, s));

  let html = `<thead><tr><th>State</th><th>Best action</th><th>Why (from Q-values)</th></tr></thead><tbody>`;
  STATES.forEach((st, s) => {
    const a = pol[s];
    const ac = ACTIONS[a];
    const qs = qs_all[s];
    const sorted = [...qs].map((q,i)=>({q,i})).sort((a,b)=>b.q-a.q);
    const margin = sorted[0].q - sorted[1].q;
    html += `<tr>
      <td>${st.emoji} ${st.name}</td>
      <td><strong>${ac.emoji} ${ac.name}</strong></td>
      <td>Q = ${qs[a].toFixed(2)}, leads by ${margin.toFixed(2)} over next best</td>
    </tr>`;
  });
  html += '</tbody>';
  document.getElementById('vi-policy').innerHTML = html;
}

document.getElementById('vi-step').addEventListener('click', viStep);
document.getElementById('vi-run').addEventListener('click', () => {
  document.getElementById('vi-run').disabled = true;
  document.getElementById('vi-step').disabled = true;
  const tick = setInterval(() => { viStep(); if (vi.done) clearInterval(tick); }, 55);
});
document.getElementById('vi-reset').addEventListener('click', () => {
  Object.assign(vi, {V: new Array(5).fill(0), iter: 0, done: false});
  document.getElementById('vi-label').innerHTML = 'Iteration 0 &nbsp;|&nbsp; &Delta; = —';
  document.getElementById('vi-step').disabled = false;
  document.getElementById('vi-run').disabled = false;
  document.getElementById('vi-conv-note').textContent = '';
  document.getElementById('vi-policy-wrap').style.display = 'none';
  renderChart(); renderQTable();
});

renderChart(); renderQTable();
onTheme(() => { renderChart(); renderQTable(); if (vi.iter > 0) renderPolicy(); });

// ─────────────────────────────────────────────────────────────
//  COMPARISON
// ─────────────────────────────────────────────────────────────
function tryShowComparison() {
  if (!play.done || !vi.done) return;
  const pol = policy(vi.V);

  let optScore = 0;
  const rows = play.hist.map((h, i) => {
    const oa = pol[h.s];
    const or = REWARDS[h.s][oa];
    optScore += or;
    return {day: i+1, s: h.s, ya: h.a, oa, ns: h.ns, yr: h.r, or};
  });

  document.getElementById('compare-hint').style.display = 'none';
  document.getElementById('compare-body').style.display = 'block';
  document.getElementById('c-your').textContent = play.score;
  document.getElementById('c-opt').textContent = optScore;

  const diff = optScore - play.score;
  const verdict = document.getElementById('c-verdict');
  if (diff <= 0) {
    verdict.textContent = `🎉 You matched or beat the optimal policy! (by ${Math.abs(diff)} points)`;
    verdict.style.color = COL.good;
  } else if (diff <= 6) {
    verdict.textContent = `👏 Very close! The optimal policy earned just ${diff} more points.`;
    verdict.style.color = COL.warn;
  } else {
    verdict.textContent = `💡 The optimal policy earned ${diff} more points — look at which states it handled differently.`;
    verdict.style.color = COL.bad;
  }

  let html = `<table class="vi-table"><thead><tr>
    <th>Day</th><th>State</th><th>You</th><th>Optimal</th><th>Your reward</th><th>Opt reward</th>
  </tr></thead><tbody>`;
  rows.forEach(row => {
    const changed = row.oa !== row.ya;
    html += `<tr>
      <td>${row.day}</td>
      <td>${STATES[row.s].emoji} ${STATES[row.s].name}</td>
      <td>${ACTIONS[row.ya].emoji} ${ACTIONS[row.ya].name}</td>
      <td class="${changed?'best':''}">${ACTIONS[row.oa].emoji} ${ACTIONS[row.oa].name}</td>
      <td>+${row.yr}</td>
      <td>${row.or > row.yr ? `<strong style="color:${COL.good}">+${row.or}</strong>` : `+${row.or}`}</td>
    </tr>`;
  });
  html += '</tbody></table>';
  document.getElementById('c-table').innerHTML = html;
}
</script>

</div>]]></content><author><name></name></author><category term="Reinforcement Learning" /><category term="Interactive" /><summary type="html"><![CDATA[What should a 3rd grader do after school — study, play, or rest? Turns out this is a Markov Decision Process, and we can solve it optimally. An interactive introduction to MDPs and value iteration.]]></summary></entry></feed>